# @nomicore/yjs-server

Standalone Hub/Peer composition root for Nomicore with a validated JSON config, NDJSON lifecycle/control channels, and the `nomicore-yjs-server` CLI. See `docs/integration/hub-peer-deployment.md` in the [repository](https://github.com/welltop-jim-wang/nomicore) for configuration and operations.
