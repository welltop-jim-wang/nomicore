# SA10 Spec 审查 — issue #314：VFSL 数字字面量拓宽（负号与小数，ADR 0020 决策 3）

- Dispatch：`sa-5eff6cdd-21d4-4aa8-bbf2-8239b1a3103d`（role `mabf-sa10`，phase spec-review，iteration 0）
- 审查对象：已提交交付 `724e58b feat(vfsl): support signed decimal literals`
- 权威基线：Parent PR base `docs/issue-310-vfsl-number-constraints` @ `29ff10f843a4d877866e963cedc8766d60194d33`
- Issue comments：REST `[]`；简报 `## Comments` 空——**无 owner 追加要求**，验收基线 = Issue 正文 + 5 条 AC
- 审查方式：独立静态 spec 符合性审查（读 Issue/契约/设计/规范/实际 diff 原文 + 只读校验命令复算）；
  按角色边界**不运行测试**、不修改任何被审对象
- 本报告为该任务首个 SA10 产物

---

## 1. Verdict

**`approve`** —— Issue 正文全部约束与 5 条 AC 逐项落实，SA6 契约 C1–C7 全量闭合，规范文档六处同步
编辑与 ADR 0020 决策 3/5/7/10 逐条一致；无遗漏、无部分实现、无错误实现、无 scope creep；
无关键 AC partial/unmet/unachievable。残余项全部为已登记的 MINOR（§7），不阻断。

PR 必须披露的已登记行为变化（非缺陷，见 §6）：`-1e3` 锚位迁移 (1,23)→(1,25)、`-0` 字面量新增
E100 条件（复用既有码）、自然 fallout 消息正文变化族、4 处既有测试断言载体替换。

## 2. Issue 正文要求落实（What to build 逐句）

| Issue 正文要求 | 实现证据 | 判定 |
| --- | --- | --- |
| 文法 `-? [0-9]+ ('.' [0-9]+)?` **全局生效** | `tokenizer.ts:209-238`：入口 = digit 或 `-`+紧邻 digit；先消费可选 `-`、`[0-9]+`、`.`（仅两侧皆 digit、每记号至多一个）+ `[0-9]+`；全局规则无上下文特判——C1 全局位置矩阵（别名 RHS / 对象字段 / 数组元素 / YLeaf / YArray / Record 值位）逐位 ok | ✅ |
| `type T = -1 \| 0.5 \| 2;` 端到端（解析/IR/derived/validate/codegen） | C1（parse + IR literal 值）、C3（derived enum 声明序 + `validateLogicalSnapshot` 命中/失配）、C4（`generateProjection` 发射 + 真实 tsc 0 诊断） | ✅ |
| 负号须紧邻数字、作为 number 记号一部分扫描 | 单码元前看于原始文本码元（`charCodeAt(i+1)`，越界 NaN → false），不跳 trivia；`- 1`/`-/*c*/1` 构造性落入既有未知字符路径 | ✅ |
| 裸 `-` 维持未知字符 E100 路径 | `-` 后不随 digit → 落 `:301` 未知字符分支（消息 `未知记号: -`、锚 `-` 所在列不变）；C2 钉死 | ✅ |
| `.5` / `1.` / `1e3` 维持 E100 | `.5`@23 / `1.`@24（`.` 不消费）/ `1e3`@24（`e3` 标识符语法错）；C2 码 + 行列 + 消息类别三钉 | ✅ |
| 超双精度（含负值）沿 §7.3 判 E100 | `parser.ts:418-420` 既有有限性闸门**原文零改动**；`Number('-'+'9'×309)` = -Infinity → 同闸门、同消息、同锚 (1,23)；C2 双形态钉 | ✅ |
| `-0`（含 `-0.0` 等值为 -0 的字面量）解析期 E100 | `parser.ts:421-427` `Object.is(tok.num, -0)` **值判定**闸门：覆盖 `-0`/`-0.0`/`-00` 与小数下溢形态（`'0'×323`/`'0'×400` 家族），锚记号起点，消息含 `-0` 且引导 `请改写为 0` | ✅ |

## 3. 验收标准（AC）逐条

| AC | 证据 | 判定 |
| --- | --- | --- |
| AC1 负整数/小数作联合成员并通过 validate 枚举判定（f64 严格相等） | C1 22 用例（11 单字面量含 f64 归一值 `±9007199254740992`/`1`/`-1e-323`/`±1e308` + 3 联合 + 5 全局位置 + 3 形状规则）；C3 8 用例：声明序 enum `[-1,0.5,2]`/`[0.1,0.2]`、命中 3 例、失配 4 例（`2.0000000000000004`/`0.5000000000000001`/`0.1+0.2`/非成员值，path + 消息断言，零 epsilon） | ✅ |
| AC2 `.5`/`1.`/`1e3`/裸 `-` 各负例维持 E100 及正确锚位 | C2 15 形态码 + 行列双钉 + 未知字符消息类别断言；含已登记锚位变化 `-1e3`@(1,25)（本审查独立列算术复核一致）；`-1 & string` 按设计勘误值 **(1,26)** 落地（SA6 契约原文 (1,25) 系误抄，设计 §6「B-补 2」已裁定） | ✅ |
| AC3 超双精度（含负值）E100 | C2 超域两形态（`±'9'×309`）同消息前缀 `数字字面量超出可序列化数值域`、同锚 (1,23) | ✅ |
| AC4 fixture 语义指纹零漂移；codegen 发射合法 TS | C5 三钉：`list()`/双指纹全值/`sha256:v1:` 前缀 + `generated.ts` sha256——**本审查独立复算 `domains/vfs3-assets/generated.ts` = `342d8c1f…6707`，与钉死值逐字符一致**；fixture 唯一 `-` 在 Pattern 字符串内（`schema.vfsl:8` `\\-`），记号化结构不变 ⇒ 零漂移构造性成立。C4：发射文本三段 + `Number(段) === IR` 逐位值等价 + **真实 TS 编译器 0 诊断**（非正则冒充）；`.test-d.ts` 类型级正/负例 fail-closed | ✅ |
| AC5 包测试、typecheck、`git diff --check` 全绿 | SA3 报告：focus 58/58、影响包 795/795、全仓 `pnpm test` 324 files/3445 tests exit 0、`pnpm typecheck`/`generate --check`/验收机检 22/22 exit 0；SA4 静态交叉计数（红灯 43 / 绿灯 58）精确吻合；**本审查复跑 `git diff --check 29ff10f..HEAD` = CLEAN**。动态门禁（全量套件/机检实跑）按角色边界未复跑，已在 §7-5 披露并归 SA7/CI 路由 | ✅（静态面独立核实，动态面凭 SA3 证据 + SA4 复核，路由已登记） |

## 4. SA6 验收契约闭合（C1–C7）

| 契约项 | 闭合情况 |
| --- | --- |
| C1（§12.2 正例 14 形态 + 全局位置 7 行） | 全覆盖：契约表逐行比对无缺（`00`/`00.5` 前导零正例、f64 归一矩阵、次正规 `-1e-323`、双边界 `±1e308` 均在）；断言只经公共接缝 `parseVfsl`，正例均为含 ROOT 完整模块（§12.0.2 纪律遵守） |
| C2（§12.3 负例 17 行） | 全覆盖：15 形态 `it.each` 码 + 行列双钉 + 超域两形态消息前缀钉 + `-0` 家族消息（含 `-0`、引导写 `0`）钉；下溢判别对（322 正例 vs 323/400 负例）杀死文本判定/ `===0` 弱实现 |
| C3（§12.4） | fixture 逐字一致；8 断言全落；运行期 -0 非目标按 §12.4 禁令未断言（文件头显式登记） |
| C4（§12.5） | fixture 逐字一致；发射文本/`Number(段)` 值等价/真 tsc 0 诊断三钉；`1e-7` 合法 TS 不要求 VFSL 回读（§12.5 明确非要求）；`.test-d.ts` 可选加固已落地（增广键全仓唯一） |
| C5（§12.6） | 钉死值与 §4 基线一致；`generate --check` 按契约明示「或留给 CI」归门禁；本审查哈希复算一致 |
| C6（§12.7 + 设计 D7 六处） | 六处编辑全部落地且与设计目标文本逐句一致（§5 复核）；`docs/`（排除 `docs/adr/`）扩充模式组 grep 本审查独立重跑：**矛盾陈述残余为零**（命中仅剩新口径一致面 + `instance-replication-v1.md:506` wire 凭据已登记排除项） |
| C7（§12.8 门禁） | SA3 红灯→绿灯序列证据（stash 移除实现 43 红 → 恢复 58/58 绿）+ SA4 静态计数复核一致；SA10 边界内不复跑（§7-5 披露） |
| §12.9 突变敏感性 | 弱实现矩阵逐条有杀手用例：宽松正则（`.5`/`1.`/`-.5`/`1..5`）、吞 trivia（`- 1`/`-/*c*/1`）、文本判定 -0（下溢对）、`===0` 代 `Object.is`（正例 `-1e-323`）、负值跳有限闸门（`-'9'×309`）、epsilon（C3 后继双精度失配）、生成器记法特判（C4 tsc + 值钉）、IR 键序/前缀升版/生成物再生成（C5 三钉） |

## 5. 规范文档与 ADR 符合性

| 规范条款 | 落实 | 判定 |
| --- | --- | --- |
| ADR 0020 决策 3（文法/紧邻/负例/f64/超域/`-0` E100 修订句/指数不做/全局生效） | 逐条对应 §2 各行；`-0` 修订句按值判定落地，消息引仓内权威「ADR 0020 决策 3」 | ✅ |
| ADR 0020 决策 5（既有语义指纹全部不变） | IR 类型族零新增（`ir.ts` 不在 diff）；C5 双指纹全值钉死 | ✅ |
| ADR 0020 决策 7（codegen `number`，字节稳定基线） | `packages/vfsl-codegen/src/**` 零改动；`generated.ts` 哈希复算不变 | ✅ |
| ADR 0020 决策 10（spec 与实现同 PR；指数记号不做） | 六处文档编辑与源码同变更集（同 commit）；`1e3`/`1.5E-2` 仍 E100 | ✅ |
| v1-spec §2 EBNF（`:63`） | `NumberLiteral = [ "-" ], digit, { digit }, [ ".", digit, { digit } ] ;` 与 D7-1 候选逐字符一致（候选已经 EbnfValidator 0 错误验证） | ✅ |
| v1-spec 注记 7（`:83-90`） | 紧邻/负例/指数不做/f64 归一举例/前导零合法/严格相等/超域/`-0` 引导写 `0` 全要素齐备 | ✅ |
| v1-spec 微示例（合法块增 `type Level = -1 \| 0.5 \| 2;`、非法块删 C 行） | 一删一增落地 | ✅ |
| v1-spec E100 码表行（`:364`） | 「负数 / 小数字面量」移除，代以 `-0 字面量`/`.5` `1.` 形态/指数记号 | ✅（措辞精度见 §7-1 MINOR） |
| 指南 `:146` + `:217` 护栏句 | `:217`「字符串或数字文字（可选负号与十进制小数；负号须紧邻数字）」与 D7-6 目标文本逐字一致（SA2 F1 增补第六处兑现）；§6 示例可选增补已落地 | ✅ |

## 6. Scope 审查与 PR 披露项

**Scope creep：无。** 交付 commit 变更路径 = 设计 ALLOW LIST 全 11 项 + wiki 流水线产物，逐项核对
无未申报文件；DENY LIST 零触碰（`ir.ts`/`evaluate.ts`/`validate.ts`/`fingerprint.ts`/codegen src/
`domains/vfs3-assets/**`/`docs/adr/**`/`tests/acceptance/**`/CI 均不在 diff）。非目标全守：
无 `Int`/`Range` 语法、无指数记号、无运行期 number 基线收窄（`validate.ts` 零改动）、无
`FINGERPRINT_PREFIX` 升版（D2 触发器裁定经 SA8 两轮复查 `clear`）、无其他进制/epsilon/十进制语义。

**PR 必须披露的已登记行为变化（非未达成项）**：

1. `-1e3` 错误锚 (1,23) → **(1,25)**（`-1` 成合法记号后与 `1e3` 同构；唯一登记的锚位变化，契约钉死）。
2. `-0` 字面量（含下溢形态）新增解析期 E100 条件——复用既有 E100 码，非新码（ADR 0020 决策 3 修订句 + v1-spec §8 首次发布前豁免，SA8 两轮裁决背书）。
3. 自然 fallout（码 + 锚不变、消息正文变，无既有断言、非验收义务）：`1 -1`/`1-1` 消息由「未知记号: -」变为「期望 ';'…数字字面量 '-1'」；`Record<-0, string>` parse 期先命中 -0 闸门。
4. 既有测试载体替换 4 处（T1 `type A = -1;`→`type A = -0;` 保 (1,10)；T2 三例 `-1`→`- 1` 保 (1,16)/(1,17)/(1,16)），断言数值零改动、R-1 回归意图完整。
5. `-0` 错误消息与注记 7 引「ADR 0020 决策 3」（仓内权威）替代设计草案的「ADR 0021 决策 2」（该 ADR 正文不在仓）——授权链内已申报偏离（设计 §5-D4 落地建议 + SA2 观察 2 + SA8 §8.5），码 + 锚不变。

**未达成/部分实现项：无。**

## 7. Non-blocking observations（MINOR，不阻断 approve）

1. v1-spec `:364` 把「`-0` 字面量」列于「不可从 §2 文法推导的任何构造」示例清单，而 `-0` 文法可推导、
   拒绝依据是值域闸门（注记 7 `:89-90` 承载精确口径，两处对码的指派一致）——纯文档措辞精度项，
   SA8 实现后复查 §8.1 / SA4 观察 1 已登记，留 owner 后续微调（如「文法/值域外的构造」）。
2. C2 未钉 `1.5e3`/`-1.`/`1.5.5`/`1 -1` 等 fallout 形态——SA6 §12.3 契约矩阵本就不含，设计 §8 已
   登记为自然 fallout；可后续可选加固，非弱化。
3. `.test-d.ts` 为手写发射参照系而非生成器输出的编译期导入——仓内既有先例同构
   （`generate-discriminated-narrow.test-d.ts`），运行时 C4 已钉实际发射文本，配对闭合 AC4；
   设计本就标注「可选，建议」。
4. 交付 commit message 仅一行（`feat(vfsl): support signed decimal literals`），未含 #314 引用与
   SA3 建议的披露正文——建议 finalize/PR 时采用 SA3 建议稿或并入 §6 披露清单。
5. 动态门禁证据依赖：全量 `pnpm test` / `pnpm typecheck` / `pnpm generate --check` / 验收机检的
   exit 0 取自 SA3 自报 + SA4 静态交叉复核（计数精确吻合）；SA10 按角色边界未复跑测试——
   SA4 §11 已将实跑路由 SA7/CI（6 分片首纳 5 个新文件、`--typecheck.only`、`codegen-freshness`），
   本审查附议该路由。

## 8. 本审查独立执行的只读校验

| 校验 | 结果 |
| --- | --- |
| `git diff --name-only 29ff10f..HEAD` + `git status` 全量核对 | 变更 = ALLOW 11 项 + wiki 产物；untracked 仅 Host 简报；DENY 零触碰 |
| `git diff --check 29ff10f..HEAD` | CLEAN（AC5 卫生面独立复跑） |
| `sha256sum domains/vfs3-assets/generated.ts` 复算 | `342d8c1f…6707` = C5 钉死值 = SA6 §4 基线（三方一致） |
| docs/ 扩充模式组 grep（`无符号\|整数文字\|数字字面量\|NumberLiteral`，排除 adr/） | 矛盾陈述残余为零（命中均为新口径一致面 + 已登记 wire 凭据排除项） |
| 新测试 skip/only/todo/failing grep | 零命中（§12.0 纪律） |
| 锚位列算术独立复算（码点计列） | T1 (1,10)、T2 (1,16)/(1,17)/(1,16)、`-1e3` (1,25)、`-1 & string` (1,26)、`Record<-1,string>` (1,30)、`.5` (1,23)、`1.` (1,24)、`1e3` (1,24)、E311 (1,13) 全部一致 |
| fixture `-` 用法审计 | 唯一命中在 Pattern 字符串字面量内（`schema.vfsl:8`），记号化不变 ⇒ 指纹零漂移构造性成立 |
| tokenizer/parser diff 逐 hunk 精读 | 扫描算法 = 设计 §7 伪代码逐行；-0 闸门 = D4 目标形态（并列、值判定、锚 `tok`）；有限性闸门原文零改动 |
| ADR 0020 决策 3 原文比对 | 修订句（`-0` 解析期 E100、消息引导写 `0`）与实现语义一致 |

---

— SA10 Spec Review · dispatch `sa-5eff6cdd-21d4-4aa8-bbf2-8239b1a3103d` · iteration 0
