# Issue #238 SA3 实现记录（implementation，MABF round 1 iteration 0）— 2026-09-06

- Worktree：`/home/wangjian/nomicore-fix-issue-238`（branch `mabf/issue-238`）
- 输入：已批准设计 `wiki/raw/task_238_design_2026-09-06.md`（Verdict approve）+ SA2 攻击复核
  `wiki/raw/task_238_design_attack_review_2026-09-06.md`（approve，随附 F3/F4/F5/F6 处置）+ SA8
  冲突复核 `wiki/raw/task_238_design_conflict_recheck_2026-09-06.md`（clear）+ SA6 红灯契约
  `wiki/raw/task_238_red_contract_validation_2026-09-06.md` + Owner 基线 repro
  `packages/ws-replication/test/ws-replication-issue238-repro.test.ts`（**字节不动**）。
- 未提交、未推送（按任务要求）；全部业务变更仅限设计 §9 受影响文件清单（lease.ts/persistence/
  plugin.ts/CONTEXT.md/docs/adr 决策正文零改动——ADR-0010 仅追加 issue #238 修订节登记）。

## 1. 实现内容（按设计 §15 顺序）

### namespace-runtime（槽内捕获层，全加性）
- `src/sequencer.ts`：`SequencerSlotKind`（P0/S/E/R/schema/bump/close-barrier）、
  `SequencerSlotSample`、`SequencerSlotMetricsSink`、`WriteSequencerMetrics`；`WriteSequencer`
  构造可选 metrics（stageClock+slotMetrics 双在场才记账），`enqueue(run, slotKind?)` 加性 label
  + 私有 depth + 有界样本缓冲（1024 防御上限）+ 槽释放后续体 flush（sink throw 自捕获）。
  FIFO 链形 `tail.then(run, run)`/`tail.then(noop,noop)` 逐字节不变（G1）；记账关闭路径与既有
  实现逐字节同形（D7 零开销）。
- `src/replication-session.ts`：新导出 `RuntimeReplicationApplyStages`（四段差值）；
  `RuntimeReplicationSessionApplyResult` ok 分支加性可选 `stages?`；`RuntimeReplicationHost`
  可选 `stageClock`；A4 接纳戳（入队前采样，'R' label）；`runSessionApplySlot` 槽内四戳
  （slotStart=thunk 入口 / applyStart=R4 通过后 / dirtyStart=R5.5 后 / dirtyDone=R6 resolve 后）
  全部纯同步时钟读、零新增 await；全 present 才组 stages（含 Object.freeze），refusal/fatal
  路径逐字节不动；close barrier label 'close-barrier'。
- `src/runtime.ts`：seam 输入加 `replicationObservability?`（形状 loud 校验）；sequencer 构造
  接 metrics（obs 双在场时）；host 透传 stageClock；P0/S/schema/E/bump 各入队点加 label；
  `createNamespaceRuntime` 第三可选参。
- `src/internal.ts`：`createNamespaceRuntimeForRegistry` 第三可选参（值导出仍恰两键）。

### namespace-registry（装配/命名层，全加性）
- `src/types.ts`：镜像 `ReplicationApplyStages` + ok 分支 `stages?`（lease.ts `Equal<>` 锁保持
  绿）；公共 `ReplicationObservabilitySlotSample`/`NamespaceReplicationObservabilityOptions`；
  `CreateNamespaceRegistryOptions.replicationObservability?`。
- `src/registry.ts`：内部 options/testing seam + `RuntimeReplicationObservabilitySeam`（本地结构
  镜像）+ `runtimeReplicationObservabilityFor(namespaceId)` 闭包盖 namespaceId 戳；open/create/
  import 三处 runtime 构造点透传；公共工厂转发。lease.ts 零改动（2 键输入校验冻结保持）。
- `src/testing.ts`：overrides + 内部选项透传（runtimeFactory 第三可选参）。
- `src/index.ts`：新公共类型导出。

### ws-replication（事件发射层，全加性；零 wire 变更）
- `src/types.ts`：事件 union 20→21 型（append-only）：`update-sent`+`sequence`+`sendQueueMs?`；
  `update-applied`/`sync-diff-applied`+`sequence`+四段；`update-acked`+`sequence`；
  `degraded-bypass-applied`+`sequence`（不加时延字段——F6）；第 21 型 `event-loop-delay-sampled`
  {side, connectionId?, delayMs}。
- `src/update-channel.ts`：`queuedAt?` 入队记账（safeNow 折叠）；出站帧 `sendQueueMs` =
  出队 − 帧内最旧项入队（direct 与合并帧同口径）；新 host 回调 `noteUpdateSent`（seq>0 每帧
  恰一，发射方做 observer 门——F5 形状落定）；`onUpdateAcked` info + `sequence`。
- `src/peer-namespace.ts`/`src/hub-namespace.ts`：update-sent 发射移至 onUpdateSent（含
  sequence/sendQueueMs）；update-acked + sequence；apply 结算续体 update-applied/
  sync-diff-applied 并入触发帧 sequence + result.stages（全 present/全缺席）；degraded 事件
  + sequence。发射点仍在 apply 结算续体（§23.4）。
- `src/liveness.ts` + `src/peer-connection.ts`/`src/hub-connection.ts`：`LivenessDelayProbe`
  （now/sample）；arm ping 记 scheduledFireAt（probe 在场且有读数），fire 采样
  delayMs=fire−计划；武装门 = observer+clock+ping/onPong 齐备（连接层提供 delayProbe），
  任一缺席 → 零状态零调度；经既有 observer dispatch 发 event-loop-delay-sampled。

### docs（F4 必含项）
- `docs/adr/0010-...md`：追加「issue #238 修订」节——ok 分支加性可选 stages 消歧登记（SA8
  复核随附要求）＋观测参数不进 session open 输入＋槽级记账落点＋发射点纪律。
- `docs/protocols/instance-replication-v1.md` §23：§23.1 20→21 型 + 连接域新行 + updates 表行
  字段增补（sequence/四段/sendQueueMs，标 issue #238 append-only）；§23.3 允许清单（差值/有限
  数值扩列）；§23.4 四段捕获纪律 + 跨侧减法边界（triage 近似声明）；§23.7 conformance 增补。

### 测试（契约 + 同步扩面）
- **新文件** `packages/ws-replication/test/ws-replication-issue238-segmented-observation.test.ts`
  （8 用例，设计 §11.2 全项）：① 四段分解逐笔精确值（saveGate 构型，u1 dirty=5000、u2–u5
  queueWait=[4000..1000]）；② 逐笔 sum(stages)===applyLatencyMs 守恒；③ sequence 三事件面 +
  wire 关联（非合并 + 合并构型：一 sequence 覆盖合并帧、sendQueueMs=0/4000 精确差值、事件
  bytes===wire 载荷长度）；④ dormant（无 stageClock → 事件零四段字段）；⑤ clock-throw 折叠
  （throw 时源 → 字段缺席、协议正常、零 unhandledRejection）；⑥ wire 不变（两构型帧 kind#seq
  语义序列全等 + 控制面字节长度一致——Yjs 载荷含随机 doc client id，按本仓库 conformance
  惯例判定）；⑦/⑧ event-loop 探针正向控制（已知漂移 Δ → delayMs===Δ）与 dormant（无时源读数
  → 零采样）；⑨ 槽级记账（慢 S 槽 runMs、R 槽 waitMs/queueDepthAtStart、namespaceId 盖戳、
  事件面交叉一致）。零 real sleep；共享同一手动单调时钟注入 ws clock 与 registry stageClock。
- `test/harness.ts` + `test/issue137-driver.ts`：`makeNode`/`bootMulti` 加性可选
  `replicationObservability` 透传（缺省 dormant——既有调用方零变化）。
- `test/ws-replication-observer-red.test.ts`：key-set 冻结白名单同步扩（21 型 + 新字段 +
  数值有限检查列）。
- `test/ws-replication-api.test-d.ts`：21 型精确 union + 新字段类型精确断言。
- `packages/namespace-runtime/test/runtime-registry-internal-type-guard.test-d.ts`：internal 工厂
  输入形态守卫同步扩为「两参 / 三参（第三参可选=复制观测）/ 单对象形」+ AC2 负向保持
  （位置形第 0 参仍须 DocHandle；无 p0Gate/compile 泄漏；对象形负向不变）。
- Owner repro `ws-replication-issue238-repro.test.ts`：**字节未动**（基线锚保持）。

## 2. 精确改动路径（worktree-relative；22 modified + 1 new）

```
docs/adr/0010-hub-peer-websocket-ydoc-replication.md
docs/protocols/instance-replication-v1.md
packages/namespace-registry/src/index.ts
packages/namespace-registry/src/registry.ts
packages/namespace-registry/src/testing.ts
packages/namespace-registry/src/types.ts
packages/namespace-runtime/src/internal.ts
packages/namespace-runtime/src/replication-session.ts
packages/namespace-runtime/src/runtime.ts
packages/namespace-runtime/src/sequencer.ts
packages/namespace-runtime/test/runtime-registry-internal-type-guard.test-d.ts
packages/ws-replication/src/hub-connection.ts
packages/ws-replication/src/hub-namespace.ts
packages/ws-replication/src/liveness.ts
packages/ws-replication/src/peer-connection.ts
packages/ws-replication/src/peer-namespace.ts
packages/ws-replication/src/types.ts
packages/ws-replication/src/update-channel.ts
packages/ws-replication/test/harness.ts
packages/ws-replication/test/issue137-driver.ts
packages/ws-replication/test/ws-replication-api.test-d.ts
packages/ws-replication/test/ws-replication-observer-red.test.ts
packages/ws-replication/test/ws-replication-issue238-segmented-observation.test.ts   ← 新增
```
（`issue137-driver.ts` 含 SA5 既有加性 clock seam——本实现仅追加 obs 透传；wiki 六件 +
repro 为先前 SA 产出，非本实现改动。）

## 3. 验证摘要（全部本 worktree 实跑）

| 命令 | 结果 |
|---|---|
| 聚焦：repro + segmented + observer-red | 38 tests passed（2 修复后重跑全绿） |
| segmented-observation.test.ts（8 用例逐项） | 8/8 passed（首轮 2 项断言勘误后全绿：sequence 连接局部起始值 → 相对关联断言；wire 跨运行字节比受 Yjs 随机 doc client id 限制 → 仓库惯例 kind#seq 语义序列 + 控制面字节长度） |
| ws-replication 全套件 | 50 files / 356 tests passed（348 基线 + 8 新契约），Type Errors no errors |
| namespace-runtime + namespace-registry 测试目录 | 62 files / 578 tests passed（含 12 键/O-11/2 键 lease/export 审计/internal 守卫负向全部绿） |
| `pnpm typecheck`（13 项目全链） | exit 0 |
| `pnpm test`（根门全工作区 vitest --typecheck） | **226 files / 2351 tests passed**，Type Errors no errors，exit 0 |
| 10× 稳定性循环（repro + segmented） | 10/10 exit 0（手动时钟/微任务泵/门闩，零 real sleep） |
| `git diff --check` | 零 whitespace 错误 |

一次全量根门首轮曾见 `apps/yjs-server` 真进程集成单测 spawn EAGAIN（环境进程上限，
非代码回归）；该文件在隔离与根门重跑均全绿（6/6；根门重跑 2351/2351）。

边界遵守：基线 repro 的 clock/gate 性质与登记值 [5000,4000,3000,2000,1000] 保留；无
env-override/fallback（生产代码单控制路径）；观测/探针全部 dormant 缺省（无注入 = 零时钟读、
零样本、零调度，wire 零扰动）；未把复现表述为生产 11 秒阶段证明；未做 wire/槽序/判据/冻结
注册表变更（G1–G4 未触发，全部由既有负向守卫与新增契约实证）。

Verdict: approve（实现按已批准设计完成，红灯契约转绿，验证全绿）
