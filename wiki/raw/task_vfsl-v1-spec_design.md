# SA1 设计文档 — VFSL v1 方言规格（issue #4，R3 修订版）

- **任务类型**：功能开发（交付物是文档，不是代码）
- **交付物**：`docs/vfsl/v1-spec.md`（SA6 已锚定）
- **验收命令**：`python3 tests/acceptance/vfsl_spec_acceptance.py`（0=绿 / 1=红）
- **上游**：PRD 归档 `wiki/raw/20260818-prd-vfsl-v1.md`（issue #3 逐字）
- **设计期实测**：R1 / R2 / R3 三轮基线均通过验收脚本 **21/21 GREEN**（R3 为现行版，证据见 §5）

## R2 修订记录（2026-08-18，响应 SA2 reject）

- **评审输入**：`wiki/raw/task_vfsl-v1-spec_sa2_review.md`（verdict: reject；F1 CRITICAL
  / F2~F4 HIGH / F5~F9 MEDIUM / F10~F12 LOW）
- **修订范围**：F1~F12 **全部 12 条逐条落实**；F1~F4（必修）已同步进 §4.1 基线——
  基线是 SA3 的逐字成文依据，改设计不改基线 = 没改
- **错误码空间变化**：18 → **19**（新增 **VFSL-E309 混合联合**，F1-③ 立场的载体）。
  按 SA2 裁决：本轮属发布前修订，不受 D13「只增不改」约束；**错误码编号随 R2
  发布冻结**——该边界已写入基线 §8
- **基线章节结构变化**：语法注记 8 → 9 条（F4 trivia 条款）；新增基线 §9「实现
  自由度与未冻结项声明」（F12），附录由 §9 顺移至 §10；§4 新增「错误判定顺序
  （规范性）」小节（F3/F6）
- **回归验证**：R2 基线副本重跑
  `python3 tests/acceptance/vfsl_spec_acceptance.py --spec <R2 基线副本>` →
  **GREEN 21/21，exit=0**（15962 字符；R1 基线 11345 字符同为 GREEN，回归无损）。
  证据见 §5
- **逐条回应**：见文末「§9. SA2 反馈逐条回应」

## R3 修订记录（2026-08-18，并入 SA2 R2 复审非阻塞建议 N1~N5）

- **评审输入**：SA2 R2 复审（`task_vfsl-v1-spec_sa2_review.md` 文末「R2 复审」节）
  ——终裁 **pass**，另列 2 MEDIUM + 3 LOW 非阻塞建议（N1~N5），授权 SA1 并入基线后
  **不再送 SA2 复审**（增补不改变已核验条款的语义，SA4 复核时按其 R2-2 节核对落点）
- **修订范围**：N1/N2（MEDIUM，必并入）+ N3~N5（LOW，一句话级，同轮顺手收口）
  全部 5 条落实；均为纯增补条款 / 措辞精确化，**不新增错误码（19 码不变）**、
  不改既有条款语义、不新增标题与表格、EBNF 围栏块零改动
- **基线变化（§4.1）**：① §4 判定顺序新增**第 7 条**（保留名记号统一归属：裸
  六标记 / `Record` / `Pattern` 引用、保留名后随 `<`、`type` 在类型位 → E100 锚
  该记号；声明名位保留名 → E303 即时判定，`type type = …` 唯一答案；**E301 仅
  适用于非保留名标识符**；tokenizer 双设计收敛声明——N1 根因收口），第 2 / 6 条
  加「保留名不适用」旁注，E100 / E301 / E303 行与保留名段同步交叉引用；② §3
  三分类后新增**标记归类段**（YMap/YArray/YXmlFragment→容器形，YLeaf/Pattern→
  标量形，**YPlainArray 同步上下文按标量形**——N2）；③ §4 单错误策略改**分相位**
  措辞（词法 / 语法相位即时即报，语义相位取位置最前；顺带收口 E106 相位归属
  ——N3）；④ §2 新增**语法注记 10**（`char` 双语义 + EOF 行注释终结——N4）；
  ⑤ §3 YMap 键空间澄清句改「未被任何成员声明的键」（N5）
- **回归验证**：R3 基线副本重跑
  `python3 tests/acceptance/vfsl_spec_acceptance.py --spec <R3 基线副本>` →
  **GREEN 21/21，exit=0**（17527 字符；R1 11345 / R2 15962 字符同为 GREEN，三轮
  回归无损）。默认路径仍 RED 0/16（exit=1，交付物未建，红灯锚定不变）。证据见 §5 A″
- **逐条回应**：见文末「§9. SA2 反馈逐条回应」R3 小节

## R3 修复轮修订记录（2026-08-18，SA7 F-1 fail-needs-fix）

- **评审输入**：`wiki/raw/task_vfsl-v1-spec_sa7_report.md`（verdict: fail-needs-fix，
  唯一阻塞项 F-1）——`type D = true | false;` 的错误码归属矛盾：交付物 L117 微示例
  与 L336 E100 总表标注 E100，但按 §2 文法 true/false 匹配 Ident→TypeRef 可推导，
  判定顺序 1~7 条不命中、保留名集合不含 true/false，与 E301 条款及 §6 yleaf 先例
  同构应归 E301；两种成文 tokenizer 设计对该输入产出不同错误码
- **修订范围**：按 SA7 §5.5 **方案 A（统一 Ident 路线，与 §6 yleaf 先例对齐）**
  落实（两案均经 SA7 核对不触碰 G1~G16 扫描面）——基线 §2 注记 8 补「true/false
  为普通 Ident（未声明 → E301、可声明为普通别名）」；微示例 D 注释改 E301；E100
  总表行删「布尔字面量联合」；§4 判定顺序第 7 条尾注补「keyword 分类以保留名集合
  为完备边界」条款（两种 tokenizer 设计收敛）；设计 §3 D6 同步修订（撤销 true/
  false 的 E100 归属）
- **回归验证**：R3 修复轮基线副本重跑 `--spec` → **GREEN 21/21，exit=0**（17816
  字符）；交付物默认路径同跑 GREEN 21/21 exit=0；基线提取副本与交付物逐字一致
  （cmp 零差异）。证据见 §5 A‴

---

## §0. 任务定位与需求推演（Feature）

### 0.1 切入点：文档即产品

本任务的"产品"就是规格文档本身。SA1 的设计输出因此不是"改动哪些代码"，而是
**规格文档的全部规范内容 + 成文结构 + 与机器契约的对齐方式**。设计粒度必须细到：
SA3 拿到本文档即可机械成文，SA2 可以攻击具体条款而非空泛意图。

### 0.2 推演链

```
PRD #3（冻结子集/六标记/禁止清单/注释/信封/演进）
  └→ 简报（环境事实：空仓库、上游文档全缺位 → 规格必须自含 fixture）
      └→ SA6 机器契约（G1~G16，可执行验收）
          └→ SA1 规格内容决定（D1~D18，本文档 §3）
              └→ 成文基线（§4，已实测 GREEN）
                  └→ SA3 成文 docs/vfsl/v1-spec.md → 验收转绿
```

### 0.3 关键语义推演：裸类型的默认物化（本设计最重要的一步推演）

PRD 只说"标记类型标记 Yjs 物化语义"，**没有说**不带标记的类型物化成什么。这不是
可以回避的空白——附录 fixture 里就有三种裸用法（`AssetEntity` 的裸联合成员、
`keywords: YLeaf<string>[]` 的裸数组、`Record<AssetId, AssetEntity>`）。推演：

1. `AssetEntity` 的联合成员是裸对象 `{ kind: …; audit: Audit }`，而 `Audit =
   YMap<{…}>`、字段是 `YLeaf<…>`——**同步容器（YMap）嵌在裸对象内部**。若裸对象
   按非同步纯数据解释，"同步 Y.Map 嵌在非同步 JSON 里"自相矛盾（Yjs 中 Y 类型只能
   活在 Y 容器或根上）。⇒ **裸对象必须默认物化为 Y.Map（同步）**。
2. `tags: YArray<YLeaf<string>>`（显式）与 `keywords: YLeaf<string>[]`（裸）在
   fixture 中并存且语义家族相同 ⇒ **裸 `T[]` 默认物化为 Y.Array**；显式标记的
   意义是文本自描述 + 切换默认（`YPlainArray` 是唯一的显式退出同步通道）。
3. `YPlainArray<T>` "不进入 Yjs 文档" ⇒ 其实参子树整体按纯 JSON 值解释 ⇒ 子树内
   再出现同步标记（`YMap`/`YArray`/`YXmlFragment`）是语义矛盾，应报错（E307）；
   而 fixture 自身写了 `YPlainArray<YLeaf<string>>`，证明 **YLeaf/Pattern（值
   语义标记）必须放行**。⇒ "纯值上下文"两模式规则是必要细化，不是新增语法。

以上推演固化为本设计 **D1/D2**（§3）。R2 补注：SA2 F1 指出 R1 的默认物化表
「容器内的联合 → 原生叶子值」行按字面读会把 `AssetEntity`（对象联合）整体物化
为 JSON 叶子，与推演 1 的排除论证自相矛盾——R2 以「联合成员三分类」（D1）修正：
全标量 → 叶子；全容器 → 按成员形状多态物化（fixture 的 `AssetEntity` 归此类）；
混合 → E309 拒绝。推演链本身不变，表格与推演不再互噬。

### 0.4 抓出的范本缺陷（SA2 预防）

SA6 的契约示例 `tests/acceptance/exemplar/spec-exemplar-v1.md` 本身存在**文法与
fixture 互不自洽**：其 EBNF 写 `FieldList = Field, { ",", Field }`（逗号分隔），
但其 fixture 的对象字段全部用 `;` 分隔且带尾分隔符——按其文法 fixture 不可推导。
本设计修正为**双分隔符（`;` 或 `,`）+ 允许尾分隔符**（D4），并保留 TS 兼容性依据
（user story 1"零学习成本"：TS 对象类型成员本就 `;`/`,` 皆可、尾分隔符可选）。
验收脚本只做两侧配对词法检查，不查文法↔fixture 推导，所以范本的这个缺陷在
GREEN 路径上不可见——但 SA2 攻击"你的文法推不出你的 fixture"时它是致命的，
必须在设计层修掉。（SA2 评审 §0 已独立核验此缺陷属实。）

---

## §1. 交付物与验收契约

### 1.1 锚定（沿用 SA6，环境事实 #4 的设计决定）

| 项 | 决定 |
| --- | --- |
| 规格路径 | `docs/vfsl/v1-spec.md`（与 PRD 所述 ADR 目录同源的 `docs/` 树） |
| 验收形态 | 可执行检查脚本（不做清单评审）——文档无运行时，结构化解析即行为验证 |
| 验证命令 | `python3 tests/acceptance/vfsl_spec_acceptance.py`（纯 stdlib） |
| 版本 bump 门禁 | 不适用（仓库无 package.json，简报已声明；SA4 复核确认即可） |

### 1.2 G1~G16 → 规格章节映射（成文时的自查表）

| 检查 | 规格落点（§4 基线章节号） |
| --- | --- |
| G1 交付物存在 | 文件本身 |
| G2 章节关键字 EBNF/禁止/注释/大小写/信封/附录 + 六标记标题精确相等 | §2/§4/§5/§6/§7/§10 标题；§3 六个 `### <标记名>` 小节（**标题不得加任何后缀**） |
| G3~G6 EBNF 块/结构/生产式/终元 | §2 唯一 ```` ```ebnf ````块（首个块即被校验，全文只放这一个） |
| G7 × 6 标记语义表 | §3 六小节，表头严格 `Yjs 物化含义 / 写入粒度 / PATCH 可否下钻`，PATCH 列 ∈ {可下钻, 部分下钻, 不可下钻} |
| G8~G9 禁止清单 | §4 六行表（首列命中关键字：any/泛型/条件类型/mapped|映射/interface|继承/递归|循环），错误类型列 `VFSL-` 前缀，行列列含 行/列/line/column |
| G10 注释规则 | §5（必须出现：`//`、`/* */`、`/** */`、忽略、原文、捕获、挂载、类型别名、属性、标记、`@tag`、机器） |
| G11 大小写契约 | §6（六标记 + 「未知名」） |
| G12 信封形状 | §7 代码块含 `"lang": "vfsl"`/`"version": 1`/`"id"`/`"text"` + 只消费/方言路由/出范围 |
| G13 方言演进 | §8（只增不改、自述；全文任意位置均可） |
| G14 附录溯源 | §10（issue #9 / 还原 / 缺位） |
| G15 fixture 块 | §10 内唯一的 ```` ```vfsl ````围栏块 |
| G16 fixture 构造覆盖 | §10 fixture（六标记/AssetId/Audit/AssetEntity/AssetsDoc/vfs3+assets/`?:`/`|`/`[]`/`&`/`Pattern<`/`Record<`/`/** */`） |

### 1.3 成文纪律（D15，机器契约驱动，SA3 必须遵守）

1. **```` ```vfsl ````围栏全文只出现一次**（附录 fixture）。G16 会词法扫描全文所有
   `vfsl` 块的合并内容，其中任何 `Y[A-Z]` 开头的非标准拼写标识符（如演示用的
   `YLEaf`）都会直接判失败。⇒ 合法微示例用 ```` ```ts ````围栏；**非法示例（含
   大小写变体演示）一律用 ```` ```ts ````或行内反引号，绝不进 `vfsl` 块**。
2. 六个标记小节标题**逐字等于标记名**（`### YMap`，不加编号/后缀）——G2/G7 按
   标题全文相等匹配。
3. **表格单元格内不出现未转义 `|`**——限定到**脚本实际解析的表**：G7 六标记表
   与 G8 禁止清单表按 `|` 切列，单元格内未转义 `|` 会错列（G8 的错误类型 / 行列
   列错位直接 FAIL）。其余表格（§5 挂载示例表、§7 信封字段表等）不被脚本解析，
   但同样遵守（注释原文含 `|` 时以 `…` 省略），避免渲染错位与 SA3 误抄。
4. 全文**只放一个 ```` ```ebnf ````块**（G3~G6 校验首个）；`<`、`>`、`&` 在 EBNF
   中一律写成引号终元 `"<"`、`">"`、`"&"`（脚本 tokenizer 的符号集不含裸尖括号，
   裸写会判"意外的符号"）。
5. 含「禁止」「注释」「大小写」「信封」「附录」「EBNF」关键字的标题——脚本行为
   是**「存在性 + 首个命中章节定位」**（G2 查存在；G8/G10 取首个含关键字的章节
   扫描其表格 / 正文），**不查唯一**。因此约束是：**首个命中的关键字标题必须落在
   正确章节**（基线现状：§4 章标题「禁止清单与错误语义」与其小节「禁止清单」均
   含「禁止」，首个命中为 §4 章级标题，其范围涵盖小节表格，G8/G9 正常通过——
   已实测）。无须强求唯一，但**不得新增早于目标章节的关键字标题**。

---

## §2. 规格文档章节骨架（docs/vfsl/v1-spec.md）

| # | 章节 | 内容要点 |
| --- | --- | --- |
| — | 标题 + 状态引言 | 冻结状态、章节地图、上游引用（PRD #3、issue #9 还原） |
| 1 | 概述与范围 | VFSL 定义、范围内条款清单、出范围清单（parser/求值器/信封路由/JSDoc 标签/服务端/迁移）、术语（自含，不引 CONTEXT.md——已核实缺位） |
| 2 | 语法子集（EBNF） | 允许语法清单 + EBNF 块 + 10 条语法注记（注记 9 词法 trivia 定位、注记 10 `char` 双语义）+ 正/反微示例 |
| 3 | 标记类型语义 | 正交总则、默认物化规则表 + 联合成员三分类（含标记归类）、纯值上下文（判定时机与别名传递）、标记实参形状约束表（别名链传递）+ 六个标记小节（三列表 + 约束说明） |
| 4 | 禁止清单与错误语义 | 错误模型（身份/行列基准/19 码分野/**错误码传递通道**/单错误策略——分相位报告）、六项禁止表、**错误判定顺序（规范性，7 条）**、递归检测细则、错误码总表（19 码）、保留名集合（含 any/extends/interface，ASCII 冻结） |
| 5 | 注释规则 | 三态表、忽略/捕获边界（含 `/**/`、`/***/` 特例）、挂载规则（三类目标节点、连续注释、悬空 E305）、`@tag` 规则、挂载示例表（**全部 7 条注释**） |
| 6 | 大小写契约 | 六标准拼写、变体按未知名报错（E301）、**v1 不保留变体拼写**（声明后即普通别名）、大小写敏感声明 |
| 7 | 信封形状 | JSON 形状块、字段表、只消费 text、方言路由出范围、version 自述语义 |
| 8 | 方言演进 | 只增不改三则（语法/语义/错误码）、文本自述版本、**发布前修订边界** |
| 9 | 实现自由度与未冻结项声明 | 正则合法性 / BOM / 多错误恢复 / @tag 前瞻——四项收口，禁止 issue #5~#9 静默决定 |
| 10 | 附录：vfs3.assets 参考 fixture | 溯源引文（issue #9 / 还原 / 缺位）+ 唯一 `vfsl` 块 + 构造覆盖说明（trivia 限定措辞） |

---

## §3. 核心设计决定（D1~D18，防弹点）

每条：**决定 → 依据 → 预防的攻击**。（D16~D18 为 R2 新增；D1/D2/D3/D7/D9/D13/D14
为 R2 修订，修订处以「R2」标注。）

### D1 默认物化规则（裸类型的物化基准）【R2 修订】

- **决定**：裸对象 ≡ `YMap` 物化；裸 `T[]` ≡ `YArray` 物化；`Record<K,V>` →
  Y.Map（键空间受 K 约束）；容器内的原始类型 / 字面量**及其联合（成员全部为
  标量形）** ≡ 原生叶子值（`YLeaf` 值语义）；**含对象 / 数组成员的联合（成员
  全部为容器形）→ 按命中成员的形状物化（多态值形状）**，适用于字段类型、数组
  元素、Record 值位与标记实参。联合成员形状**三分类**（判定在别名解析后，沿
  别名链取最终形状）：全标量 → 叶子；全容器 → 多态物化；**混合（标量与容器
  成员并存）→ 拒绝，新错误码 VFSL-E309**——仅同步物化上下文适用，纯值上下文
  （YPlainArray 子树）内混合联合合法（普通 JSON 值形状）。`YMap<对象形联合>`
  的键空间 = 各成员字段键集**之并的封闭**（值-成员匹配校验属语义层）。**R3 补
  （N2/N5）**：三分类对**标记成员**闭合——YMap/YArray/YXmlFragment → 容器形，
  YLeaf/Pattern → 标量形，**YPlainArray 同步物化上下文按标量形**（父容器中单一
  JSON 值承载、不可下钻，采 SA2 建议立场并写死）；YMap 对象形联合键空间澄清句
  改「未被任何成员声明的键」（消除并集 / 交集歧读）。
- **依据**：§0.3 推演（`audit: Audit` 嵌套矛盾排除"裸=纯数据"读法；
  tags/keywords 并存证明默认数组=同步）。R2：SA2 F1 证实 R1 表行「容器内的
  联合」字面读法与推演 1 互噬；混合联合拒绝而非物化，是因为"一个位置同时是
  Y.Map 又是原生叶子"没有可实现的物化语义，且 Yjs 无判别式下钻——静默选择
  任一侧都是 D3 要预防的"无规格依据决定"。
- **预防**：SA2"六个标记之外，裸类型物化是什么？"（R1 已防）+"`{a} | number`
  物化成什么？`Record<AssetId, AssetEntity>` 按你的表是 JSON 叶子——和你自己
  的推演矛盾"（R2 修复）。

### D2 纯值上下文（YPlainArray 子树的两模式规则）【R2 修订】

- **决定**：`YPlainArray<T>` 实参子树按纯 JSON 值解释；子树内禁止
  `YMap`/`YArray`/`YXmlFragment`（→ E307）；`YLeaf`/`Pattern`（值语义标记）放行。
  **R2 补**：E307 判定在**模块全量解析、别名解析后**进行——同步标记可经别名
  **间接**进入纯值上下文（`type A = YMap<{…}>; type B = YPlainArray<A>;` →
  E307）；锚点 = 标记记号直接出现时为标记记号，经别名引入时为**引入该别名的
  引用记号**。纯值上下文内联合按普通 JSON 值解释，混合联合合法（E309 不适用）。
- **依据**：`YPlainArray` "不进入 Yjs 文档"的直接推论；fixture
  `YPlainArray<YLeaf<string>>` 实证 YLeaf 必须允许。R2：SA2 F5——同步标记
  经别名的间接进入若无条款，issue #5 只能静默决定报不报、锚哪。
- **预防**：SA2"`YPlainArray<YMap<…>>` 合法吗？"（R1 已防）+"经别名绕进去算
  不算？锚点锚哪？"（R2 修复）。

### D3 标记实参与 Record 键的形状约束【R2 修订】

- **决定**：`YMap`/`YXmlFragment` 实参须对象形（内联 ObjectType 或解析到对象/
  对象形联合的别名）；`YLeaf` 实参须标量形（primitive/字面量/其联合/含 Pattern；
  `unknown` 允许）→ 违反 E304。`Record` 键须 string 形（`string` /
  `string & Pattern` / 其别名）→ 违反 E306（Y.Map 键空间是字符串）。
  `YArray`/`YPlainArray` 实参任意。裸 `Pattern<"…">`（脱离 `string &`）→ E100。
  **R2 补**：形状解析沿**别名链传递**（`type A = B; type B = {…}` 中 A 的形状
  即 B 链式展开后的最终形状），判定在模块全量解析后；标记实参位置先判 E304，
  非标记位置的联合归类判 E309。**R2 删除「附着结构」未定义术语**：YXmlFragment
  实参字段 ↔ XML 节点（元素 / 属性 / 子节点）的语义映射**属语义层（求值器）
  职责**，方言层仅冻结对象形约束（E304）与字段上的注释挂载规则（§5）。
- **依据**：Yjs 数据模型（Y.Map 键为 string；标量叶子非 Y 类型）；标记在文法层
  统一取 TypeExpr（parser 实现统一），形状校验放语义层（别名解析后）——与环检测
  同层，PRD 已把引用图解析划给 parser。R2：SA2 F9——未定义术语与未声明边界
  同属"逼实现做静默决定"。
- **预防**：SA2"parser 拿到规格仍要做大量静默决定"（R1 已防）+"YXmlFragment
  的字段怎么对应 XML？附着结构是什么？"（R2 修复）。

### D4 EBNF 精确化（文法工程决定）

- **决定**：
  - 优先级显式：`TypeExpr = UnionType`；`UnionType = [ "|" ], ArrayType,
    { "|", ArrayType }`；`ArrayType = PrimaryType, { "[", "]" }`——`|` 最低、
    `[]` 紧贴 Primary，无左递归、无歧义；
  - 字段分隔符 `;` 或 `,` 等价 + 允许尾分隔符 + 空对象合法；
  - 联合允许 TS 风格前导 `|`（fixture 实际使用）；
  - 类型别名强制 `;` 终止；模块可为空（空文本合法）；
  - 无括号分组类型（`(A|B)[]` → E100）；
  - `<`/`>`/`&` 在 EBNF 文本中写成引号终元（对齐 SA6 校验器记号集，D15-4）；
  - **R3 补（N4）**：词法元符号 `char` 双语义成文（语法注记 10）——StringLiteral
    的 char 不含行终止（E201 / 注记 6 的实现依据），注释产生式的 char 含行终止
    （多行注释与 §5「逐字保留含缩进」的依据）；EOF 无换行视同 eol 终结行注释。
- **依据**：§0.4 范本缺陷修正；TS 兼容（story 1）；SA6 校验器实测通过（§5）。
- **预防**：SA2"文法推不出 fixture 自己的构造"“文法有左递归/歧义，parser 无法
  直接实现”。

### D5 字符串字面量与转义

- **决定**：`"` 界定、不得跨行（未闭合 E201）；仅 `\"`、`\\` 两个转义，其余
  `\x` 一律 E202；正则反斜杠双写（正则 `\d` 写 `\\d`）。
- **依据**：fixture 实文 `Pattern<"^[A-Za-z0-9_\\-]{1,64}$">`（`\\-` 即正则 `\-`）
  是双写规则的既有实证；严格转义集杜绝实现各自发挥。
- **预防**：SA2"fixture 里 `\\-` 什么语义？规格没定义转义"。

### D6 字面量联合成员冻结为 string/number 字面量

- **决定**：`LiteralType = StringLiteral | NumberLiteral`；`true`/`false`/`null`
  字面量不进 v1（**R3 修复轮（SA7 F-1）修订**：`true`/`false` 词法上是普通
  Ident、不在保留名集合——未声明引用按 E301 报未知名、亦可被声明为普通别名
  （基线注记 8，与 §6 `yleaf` 同构），原「→ E100」标注撤销；`null` 由原始
  类型表达，不涉越界）；数字字面量仅无符号十进制整数。
- **依据**：PRD"字面量联合"的用途是枚举与判别字段（story 4，fixture 的
  `"image"|"text"|"file"`）；布尔/空值语义已由原始类型表达，无枚举用例。
  留白符合"只增不改"（未来可只增引入）。
- **预防**：SA2"`true | false` 联合合法吗？`-1 | 1` 呢？规格语焉不详"。

### D7 标识符、字符集与保留名【R2 修订】

- **决定**：`Ident = letter, { letter | digit | "_" }`；**letter ∈ [A-Za-z]、
  digit ∈ [0-9]（ASCII 冻结）**——`$` 与非 ASCII 字母（`type 資産 = …`）均不在
  v1 字符集，出现即不可推导 → E100；Unicode 标识符留给未来版本只增引入。
  大小写敏感；保留名集合 = `type` + `Record` + `Pattern` + 五个原始类型 +
  **`any` + `extends` + `interface`（R2 新增）** + 六个标记，别名声明占用保留名
  → E303（字段名不受限——TS 中 `type:` 作属性名常见）。**R2 立场（F7）**：
  标记的**大小写变体不进保留名**——`type yleaf = string;` 是合法别名声明，
  此后 `yleaf` 即已知名（普通别名）；未声明时引用变体（`type T = yleaf;` 或
  `YLEaf<string>`）自然落入 E301。标记拼写、内建名与别名同居同一标识符名空间。
- **依据**：`type Record = …` 会与内建产生不可判定歧义；最小冻结。R2：SA2
  F3/F7/F8——`any` 不入保留名则 `type any = …` 合法而 `type T = any` 恒 E101
  （合法声明永不可用的自相矛盾）；`extends`/`interface` 同理需与判定顺序条款
  （D16）闭环；变体保留与否两条款必须一致；D8 连码点都冻结了，Ident 字符集
  不冻结则 issue #5 测试无法书写。
- **预防**：SA2"别名能叫 `Record` 吗？"（R1 已防）+"`type any = …` 合法吗？"
  "`type 資産 = …` 呢？"`yleaf` 声明了还算未知名吗？"（R2 修复）。

### D8 行列定位基准

- **决定**：line/column 均 1 起；column 按 Unicode 码点自行首累加；行分隔 `\n`，
  `\r\n` 中 `\r` 不占列（文本按 UTF-8 解码后计）。**R2 补（F12-②）**：解码后
  首字符为 U+FEFF（BOM）时剥离、不报错、不占行列（见 D18）。
- **依据**：issue #5 的测试要断言精确行列（PRD Testing：错误断言含行列信息），
  不冻结基准则测试无法书写、实现间不可比。
- **预防**：SA2"行列是字节还是码点？0 起还是 1 起？"（R1 已防）+"带 BOM 的
  text 第一列锚哪？"（R2 修复）。

### D9 错误码空间、传递通道与"错误身份"边界【R2 修订】

- **决定**：**19 个错误码**三层分野——语法 E100~E106（E100 为越界语法
  catch-all）、词法 E201~E203、引用/语义 E301~**E309**（E301 未知名=大小写
  变体的报错归宿、E302 重复声明、E305 悬空文档注释、E307 纯值上下文违例、
  E308 字段重名、**E309 混合联合——R2 新增**）。**错误身份 = 码 + 消息 + 行列**；
  issues 数组的字段形状不冻结（PRD #3 `parseVfsl` 接缝所有权，规格只冻结身份
  与定位锚）。**R2 新增传递通道契约（F2）**：错误码以 message 的**冻结前缀
  格式**传递——每条 issue 的 message 必须形如 `VFSL-E<编号>: <人类可读消息>`
  （如 `VFSL-E101: any 类型不在 v1 冻结子集`）；前缀（`VFSL-E` + 三位编号 +
  冒号 + 单空格）冻结，消息正文措辞不冻结；测试以前缀为断言锚。未来若需独立
  code 字段，属公共接缝变更，**须回 PRD #3 走变更流程**，不在 §8「只增不改」
  范畴。**R2 新增错误数量策略（F12-③）**：v1 冻结「首个错误即失败」，issues
  恰含 1 条（数组形状为未来放宽预留）。**R3 补（N3）**：单错误策略改**分相位**
  措辞——词法 / 语法相位（含判定顺序映射码）遇到处即时失败即报；全量解析成功后
  才进引用 / 语义相位（E106 与 E301~E309）取位置最前；「位置最前」不再跨相位
  要求，顺带收口 E106（引用图成环需全量解析）的相位归属。
- **依据**：PRD 接缝 `{ ok, issues: [{ message, line, column }] }` 不得被规格
  覆写；G8/G9 要求结构化错误码。R2：SA2 F2——前缀方案是唯一同时满足「接缝
  零改动 + 码可断言 + 实现间可比」的最小通道；单错误是 story 9 确定性的最简
  可冻结形态（多报的顺序 / 恢复契约成本远超 v1 需要）。
- **预防**：SA2"其余失败怎么报？规格越权改公共接缝"（R1 已防）+"18 个码怎么
  到达调用方？断言不了码的错误语义是文档装饰"（R2 修复）。

### D10 注释边界与挂载的完备规则

- **决定**：三态表（`//`、`/* */` 忽略；`/** */` 原文捕获）；边界特例 `/**/` 与
  `/***/` 是块注释而非文档注释；文档注释不嵌套（首个 `*/` 终结）；未闭合 → E203。
  挂载 = 前导相邻（中间只许空白与忽略型注释）到三类声明性节点（类型别名/属性/
  标记）；连续多个文档注释按序同挂一个节点；**悬空（到模块末尾无可挂载）→ E305
  拒绝**（loud assert，不静默丢弃——作者写下的语义是单一真相源的一部分）。
  `@tag` 零处理：不解析、不校验、不告警（标签机器语义属语义层任务，PRD Out of
  Scope 明示；story 12 的 warn 语义随之延后），原文随注释保留。
- **依据**：PRD 注释条款 + ADR-0001 主题（无机器标签）；SKILL"拒绝虚假降级"立法
  ——悬空注释静默丢弃 = 正常路径的缺陷被降级处理，必须 loud。
- **预防**：SA2"`/**/` 是 doc 还是 block？""文件末尾孤悬的 `/** */` 去哪了？"
  "@tag 要不要 warn？story 12 说了 warn"。

### D11 Pattern 的正则语义

- **决定**：实参解码后的字符串按 ECMAScript RegExp、无标志解释；锚定不隐含
  （fixture 显式 `^…$`）。校验执行属语义层（validateSnapshot），本规格只冻结解释。
  **R2 补（F12-①）**：实参解码后**是否为合法正则不在方言层校验**——
  `Pattern<"[">` 词法 / 语法 / 形状全过，`ok: true`（见 D18）。
- **依据**：宿主引擎是 TS（nomicore 全新重写）；旧体系 name 禁 `.`/`|` 的检查迁移
  场景（story 3）要求正则语义可预期。方言层无 RegExp 合法性判定职责——混入会
  把语义层校验提前到 parse，违反 D3 的分层。
- **预防**：SA2"Pattern 的正则按什么方言？PCRE 还是 JS？锚定隐含吗？"（R1 已防）
  +"`Pattern<"[">` 报不报错？"（R2 修复）。

### D12 信封字段语义与 version 自述

- **决定**：字段表成文（lang/version/id/text 的类型与语义）；id 对 parser 不透明
  （唯一性校验属引擎层，出范围）；`version` 是"文本自述版本"的来源；parser 只
  消费 text 且**隐式按 v1 解释**——`version ≠ 1` 的路由决策属引擎层（方言路由
  出范围）。
- **依据**：PRD"parser 只消费 text"+演进条款"以文本自述版本为准"的一致性闭环：
  自述在信封上，而 parser 不读信封，二者职责必须在规格里说清边界。
- **预防**：SA2"自述版本在哪？parser 只看 text 怎么知道版本？两条条款互相打架"。

### D13 方言演进条款具体化【R2 修订】

- **决定**：只增不改三则——语法只增（不得收窄/废除既有推导）、语义不改（物化/
  挂载/错误语义不得重解释）、错误码稳定（已发布码的条件与含义不变，新条件用新
  码）；本规格自身的修订同样只增不改。**R2 补**：**首次发布前**的规格评审修订
  轮次不受本条约束（本轮 R2 即属此类：新增 E309、扩充保留名、增补 §9 均为发布
  前成文，不是对已发布语义的改写）；**错误码编号随首次发布冻结**，发布后新增
  条件一律用新码、不得复用既有编号——该边界已写入基线 §8。
- **依据**：PRD v1 冻结语义；错误码一旦被下游（测试/工具）依赖也是契约。R2：
  SA2 裁决明确「本轮属发布前修订，不受该条约束，但错误码编号一经 R2 发布即
  冻结」——须成文以免后续轮次争议。
- **预防**：SA2"只增不改是一句口号，落到条款是什么？"（R1 已防）+"你自己 R2 就
  改了 18→19，违反你自己的条款"（R2 修复：发布前边界成文）。

### D14 fixture 基线（附录自含正例）【R2 修订】

- **决定**：附录采用 §4.1 基线内的 fixture 全文（与 SA6 契约示例同源，已修正到
  与本设计文法自洽——**剥离词法 trivia 后完全可推导**，见 D17），溯源标注
  "依据 issue #9 描述还原，原设计文档缺位"；构造覆盖说明成文。挂载示例表
  （规格 §5）覆盖 fixture 的**全部 7 条**文档注释 → 挂载点（R1 仅列 5 条，漏
  「附件→Attachments」「AssetsDoc→AssetsDoc」，R2 补齐——F10）。
- **依据**：简报环境事实 #3（设计文档缺位 → 规格自含 fixture，标注还原）。
- **预防**：SA2"fixture 凭什么是这段文本？"（R1 已防，有溯源/可推导/验收实证）
  +"挂载示例表说好每条注释都有映射，怎么只有 5 行？"（R2 修复）。

### D15 成文纪律

见 §1.3（五条，全部由机器契约实测反推而来；**R2 按 SA2 F11 精确化第 3 / 5 条
措辞**：第 3 条限定到脚本实际解析的表，第 5 条改为「首个命中被检查，不查唯一」
并记录基线双「禁止」标题现状——避免 SA3 误修 / 漏修）。**预防**：SA3 成文时
意外踩中 G2/G7 标题精确匹配、G16 全文 `vfsl` 块合并扫描、表格切列等脚本级陷阱。

### D16 错误判定顺序（规范性）【R2 新增 — F3/F6】

- **决定**：E101~E105 对应构造在冻结文法下不可推导，形式上会全部落入 E100
  catch-all。为使专属码可达，规格成文**规范性判定顺序**：tokenizer/parser 在
  落入 E100 之前先按特征映射——模块层或类型位置遇 `interface` → E105；别名
  声明名后遇 `<` → E102；类型位置遇 `extends` → E103；字段名位置遇 `[` →
  E104；类型位置遇 `any` → E101；`<` 前标识符非 Record / 六标记时，未声明名
  或标记变体 → E301（锚引用记号）、已声明别名 → E100（锚 `<`）。多特征命中
  取文本位置最前，并列取次序靠前。**别名解析时机（F6）**：模块全量解析后、
  与声明顺序无关（前向引用 `type A = B; type B = string;` 合法），「未声明」=
  模块内无该名 TypeAlias 声明；该时机条款适用全部 E3xx。配套：`any` /
  `extends` / `interface` 入保留名（D7），`type any = …` → E303。**R3 补（N1）**：
  增补**第 7 条**——保留名记号出现在其对应产生式不适用的位置（裸六标记 /
  `Record` / `Pattern` 引用、保留名后随 `<`、`type` 在类型位）→ E100 锚该记号；
  **声明名位保留名 → E303**（`type type = …` 与 `type any = …` 同归，解析到
  声明名时即时判定）；**E301 仅适用于非保留名标识符**（裸 `YMap` 与裸 `Pattern`
  同族同判，不再异判）；第 2 / 6 条加「保留名不适用」旁注（消解 `type Record<…>`
  的条款次序歧义）；并成文声明本族条款使 keyword 记号与统一 Ident + 后置查表
  两种 tokenizer 设计产出相同错误码与锚点——N1 根因（tokenizer 政策未成文）收口。
- **依据**：SA2 F3 逐项复现——`interface A {}` 不以 `type` 开头、`type Box<T>`
  在 Ident 后遇 `<` 非 `=`、TypeRef 后遇 `extends`、字段位遇 `[`，四码在文法下
 形式不可达；`YLEaf<string>` 文法下是「TypeRef 后遇 `<`」的 E100 而非 §6 承诺
  的 E301；`any` 不在保留名则「合法声明永不可用」。锚点列（如 E102 锚 `<`
  起点）本就暗示特征检测，缺的只是顺序条款。
- **预防**：SA2"禁止清单四码形式不可达，错误码总表与文法冲突""错误判定顺序无
  条款，四个实现四种报法"。

### D17 词法 trivia 定位（注释与空白的文法地位）【R2 新增 — F4】

- **决定**：语法注记第 9 条成文：**空白与注释是词法级 trivia，可出现于任意记号
  边界，不参与语法推导**——全部产生式按「剥离 trivia 后的记号序列」解释；记号
  内部不容 trivia（`str/**/ing` → E100）。`Comment` 系列产生式（含 LineComment /
  BlockComment / DocComment）**不出现在任何语法产生式右部**，仅规范注释自身的
  词法形状（起止界定、未闭合判定），是 E203 判定与 §5 原文捕获的依据。附录
  「完全可推导」声明全部改为「**除词法 trivia 外**完全可推导」。
- **依据**：SA2 F4——R1 的 `Comment` 四条产生式无任何引用点、词法元符号注记只
  豁免 char/digit/letter/eol：按严格文法视角，fixture 的 5 处别名级注释 + 1 处
  属性级注释使文本不可推导，AC #1 的「EBNF 能推出 fixture 每个构造」字面失败；
  而 `type/*c*/A = string;` 合法与否也无从回答。trivia 是 TypeScript / 主流
  parser（TS scanner 产出 trivia 通道）的既有惯例，issue #5 可直接照此实现。
- **预防**：SA2"注释是语法成分还是词法 trivia？你的 fixture 按你自己的文法推
  不出来"。

### D18 实现自由度与未冻结项声明【R2 新增 — F12】

- **决定**：规格增设 §9「实现自由度与未冻结项声明」，四项收口：① Pattern 实参
  正则合法性不校验（`Pattern<"[">` → ok:true，非法正则的暴露属语义层）；②
  UTF-8 BOM：解码后首字符 U+FEFF 剥离、不报错、不占行列；③ 多错误恢复：v1
  单错误（issues 恰含 1 条，首个错误即失败，恢复与多报留给未来只增）；④ @tag
  前瞻：story 12 的 warn 属语义层任务，未来引入不构成 v1 语义改写。
- **依据**：SA2 F12 四项均会迫使 issue #5/#9 做静默决定。逐项立场依据：①是
  D3 分层（方言层不做 RegExp 合法性判定）的直接推论；②与验收脚本自身的
  `utf-8-sig` 读取惯例同向（剥离是项目既有行为，D8 行列基准的补全）；③是
  story 9 确定性的最简形态，issues 的数组形状为未来放宽预留；④是 PRD Out of
  Scope + D10 的时间维延伸。
- **预防**：SA2"非法正则报不报？BOM 锚哪？多报还是首错即停？@tag 以后会不会
  改语义？——四个位置 issue #5 全要掷硬币"。

---

## §4. 规范内容基线（SA3 成文依据 —— 逐字采用，实测 GREEN）

> 以下全文即 `docs/vfsl/v1-spec.md` 的成文基线。SA3 的成文自由度仅限：错别字
> 修缮、编号微调（不违反 §1.3 纪律）；**任何规范条款的实质变更必须回 SA1**。
> R1 基线（11345 字符）、R2 基线（15962 字符）、R3 基线（17527 字符）与
> **R3 修复轮基线（现行版，17816 字符）** 均于 2026-08-18 通过
> `python3 tests/acceptance/vfsl_spec_acceptance.py --spec <基线副本>`：
> **GREEN 21/21，exit=0**（证据 §5）。R2 变更（F1~F4 必修 + F5~F12 同轮收口）
> 见文首「R2 修订记录」；R3 变更（N1/N2 必并 + N3~N5 收口）见文首「R3 修订记录」；
> R3 修复轮（SA7 F-1，方案 A）变更见文首「R3 修复轮修订记录」。

### 4.1 基线全文

````markdown
# VFSL v1 方言规格（VFSL Dialect — version 1）

> 状态：已冻结（v1 frozen）｜ 演进规则：只增不改（§8）
> 本规格是 VFSL v1 方言的唯一规范来源：语法子集（§2）、六个标记类型语义（§3）、
> 禁止清单与结构化错误（§4）、注释规则（§5）、大小写契约（§6）、信封形状（§7）、
> 方言演进（§8）、实现自由度与未冻结项（§9）、参考 fixture（附录 §10）。
> 上游需求：PRD issue #3。附录 fixture 依据 issue #9 描述还原。

## 1. 概述与范围

VFSL（VF Schema Language）是 Namespace Schema 的单一真相源文本方言：以
TypeScript 的一个冻结子集书写，用标记类型表达 Yjs 物化语义。parser（issue #5~#9）
按本规格实现；文本之外的一切以本规格为输入另行发展。

范围内：v1 语法与语义的全部规范条款（§2~§9）与参考 fixture（附录 §10）。

出范围（out of scope）：

- parser 实现与 IR 具体形状——公共接缝 `parseVfsl(text)` → `{ ok: true, module }` 或 `{ ok: false, issues }`（PRD #3 冻结）；
- 求值器（结构树 / 值 schema 派生）、路径索引、`validateSnapshot`、编译缓存；
- 信封解析与方言路由（未知方言 loud-fail 只读）；
- JSDoc 标签的结构化解析（语义层任务；本方言无机器标签——ADR-0001 主题）；
- yjs-server 服务端（WS/REST、存储、同步协议、namespace 生命周期与创建事务）；
- schema 升级 / 迁移（Phase 3）。

术语：本规格自含全部术语定义；「物化」指某类型按其标记对应的 Yjs 运行时结构
（Y.Map / Y.Array / Y.XmlFragment / 原生值）承载的方式。

## 2. 语法子集（EBNF）

v1 冻结的允许语法：类型别名；封闭对象字面量类型（未声明字段拒绝的语义基础）；
`?:` 可选属性；原始类型 `string` / `number` / `boolean` / `null` / `unknown`；
字面量联合；`T[]`；`Record<K, V>`；`string & Pattern<"正则">`（唯一允许的交叉
类型）；注释。

```ebnf
(* VFSL v1 冻结语法子集；char/digit/letter/eol 为词法元符号，由 tokenizer 直接实现 *)
Module        = { TypeAlias } ;
TypeAlias     = "type", Ident, "=", TypeExpr, ";" ;
TypeExpr      = UnionType ;
UnionType     = [ "|" ], ArrayType, { "|", ArrayType } ;
ArrayType     = PrimaryType, { "[", "]" } ;
PrimaryType   = RecordType | ObjectType | Marker
              | PatternType | LiteralType | PrimitiveType | TypeRef ;
RecordType    = "Record", "<", TypeExpr, ",", TypeExpr, ">" ;
ObjectType    = "{", [ FieldList ], "}" ;
FieldList     = Field, { ( ";" | "," ), Field }, [ ";" | "," ] ;
Field         = Ident, [ "?" ], ":", TypeExpr ;
Marker        = "YMap", "<", TypeExpr, ">"
              | "YArray", "<", TypeExpr, ">"
              | "YPlainArray", "<", TypeExpr, ">"
              | "YLeaf", "<", TypeExpr, ">"
              | "YXmlFragment", "<", TypeExpr, ">" ;
PatternType   = "string", "&", "Pattern", "<", StringLiteral, ">" ;
LiteralType   = StringLiteral | NumberLiteral ;
PrimitiveType = "string" | "number" | "boolean" | "null" | "unknown" ;
TypeRef       = Ident ;
Comment       = LineComment | BlockComment | DocComment ;
LineComment   = "//", { char }, eol ;
BlockComment  = "/*", { char }, "*/" ;
DocComment    = "/**", { char }, "*/" ;
StringLiteral = '"', { char }, '"' ;
NumberLiteral = digit, { digit } ;
Ident         = letter, { letter | digit | "_" } ;
```

语法注记（与文法同等效力）：

1. **优先级与结合**：`|`（联合）最低；`[ ]` 后缀紧贴其前置 PrimaryType——
   `string & Pattern<"a">[]` 是「约束字符串的数组」，`A | B[]` 是 `A | (B[])`。
   同层联合记号自左向右，无结合性歧义。
2. **前导 `|`**：联合允许 TS 风格前导分隔符（`type T = | A | B;`），文法以
   `[ "|" ]` 冻结。
3. **字段分隔符**：对象字段以 `;` 或 `,` 分隔，二者等价；允许尾分隔符
   （`{ a: string; }` 与 `{ a: string }` 均合法）；空对象 `{}` 合法。
4. **别名终止与模块**：每个类型别名必须以 `;` 终止；模块为别名的任意序列，
   可为空（空文本合法，产出空模块）。
5. **无括号分组**：括号分组类型（如 `( A | B )[]`）不在 v1 子集，出现即
   VFSL-E100。`<` 与 `>` 仅作为 Marker 与 Record 的实参括号。
6. **字符串字面量**：以 `"` 界定，不得跨行（未闭合 → VFSL-E201）；仅认 `\"` 与
   `\\` 两个转义（`\"`→`"`、`\\`→`\`），其余任何 `\x` 序列非法（→ VFSL-E202）。
   正则实参中的反斜杠须按此规则双写（正则 `\d` 写作 `\\d`）。
7. **数字字面量**：仅无符号十进制整数。负数、小数、其他进制不在 v1 子集
   （→ VFSL-E100）。
8. **字面量联合成员种类**：v1 冻结为字符串字面量与数字字面量两类；`true` /
   `false` / `null` 字面量不进入 LiteralType（布尔与空值语义由原始类型
   `boolean` / `null` 表达）。`true` / `false` 词法上是普通 Ident（不在保留名
   集合，§4）：未声明引用按 **VFSL-E301** 报未知名（与 §6 `yleaf` 同构），
   亦可被声明为普通别名。
9. **空白与注释是词法级 trivia**：空白（空格 / 制表 / 行结束）与注释可出现于
   **任意记号边界**，不参与语法推导——本节全部产生式均按「剥离 trivia 后的
   记号序列」解释；记号**内部**不容 trivia（`str/**/ing` 是两个记号，不可推导，
   → VFSL-E100）。`Comment` 系列产生式（Comment / LineComment / BlockComment /
   DocComment）因此不出现在任何语法产生式的右部：它们仅规范注释**自身的词法
   形状**（起止界定与未闭合判定），是 VFSL-E203 判定与 §5 原文捕获的依据。
10. **词法元符号 `char` 的双语义**：StringLiteral 的 char **不含行终止**（字符串
   跨行即未闭合 → VFSL-E201，注记 6「不得跨行」由此实现）；注释产生式
   （LineComment / BlockComment / DocComment）的 char **含行终止**——多行块
   注释 / 文档注释由此可能，§5 原文捕获「逐字保留，含内部 `*` 与缩进」即依赖
   此项；行注释由 eol 终结，文本在 EOF 结束而无换行时**视同 eol** 终结行注释
   （合法，不报错）。

微示例（合法）：

```ts
type Port = 80 | 443;
type Name = string & Pattern<"^[a-z]+$">;
type Pair = { first: string; second?: number };
type Names = Name[];
type Index = Record<string, Port>;
```

微示例（非法，错误码见 §4）：

```ts
type A = ( string | number )[];    // VFSL-E100：括号分组不在子集
type B = string & Pattern<"a\d">;  // VFSL-E202：非法转义（须写 \\d）
type C = -1 | 1;                   // VFSL-E100：负数字面量不在子集
type D = true | false;             // VFSL-E301：true/false 未声明（布尔字面量不进入 LiteralType，注记 8；按未知名报错）
```

## 3. 标记类型语义

六个标记类型，大小写是契约（§6）。标记表达**结构语义**（Yjs 物化），与**值语义**
（原始类型、字面量、约束）正交：标记不改写值约束，值类型不隐含结构。

### 默认物化规则（无标记类型的物化基准）

| 类型写法 | 默认物化 | 说明 |
| --- | --- | --- |
| 裸对象类型 `{ ... }` | 等价 `YMap<...>` 物化 | 封闭键空间的 Y.Map |
| 裸数组 `T[]` | 等价 `YArray<T>` 物化 | 同步元素序列 |
| `Record<K, V>` | Y.Map（键空间受 K 约束） | 键须为 string 形（E306） |
| 容器内的原始类型 / 字面量**及其联合**（成员全部为标量形） | 原生叶子值 | 等价 `YLeaf<...>` 的值语义 |
| 含对象 / 数组成员的联合（成员全部为容器形） | 按命中成员的形状物化（多态值形状） | 成员各自适用默认 / 标记规则；适用于字段类型、数组元素、Record 值位与标记实参（实参位置先判形状约束 E304） |

联合成员的形状归类（判定在**别名解析后**进行，沿别名链取最终形状；上表末两行
由此三分类确定）：

- **全部标量形**（原始类型 / 字面量 / 含 Pattern 约束）→ 原生叶子值（上表
  第 4 行），如 `keywords: YLeaf<string>[]` 的元素；
- **全部容器形**（对象 / 数组 / Record）→ 按命中成员的形状物化（上表第 5 行）：
  对象成员 → Y.Map、数组成员 → Y.Array、Record 成员 → Y.Map。附录 fixture 的
  `Record<AssetId, AssetEntity>` 即此——`AssetEntity` 的每个联合成员按其对象
  形状物化为 Y.Map，成员内部的标记字段（`audit: Audit` 等）各自适用标记规则；
- **标量形与容器形并存（混合联合）**，如 `{ a: string } | number` → 在同步物化
  上下文中**拒绝**：VFSL-E309（定位锚为首个与首成员形状类别不同的成员起点
  记号）。纯值上下文（YPlainArray 子树，见下）不适用本条——普通 JSON 值允许
  混合形状联合。

**标记成员的形状归类**（联合成员是标记、或经别名解析到标记时，按该标记的物化归类，
判定时机同上——别名解析后）：`YMap` / `YArray` / `YXmlFragment` → **容器形**；
`YLeaf` / `Pattern`（`string & Pattern<…>` 的约束侧）→ **标量形**；`YPlainArray`
在同步物化上下文按**标量形**（父容器中以单一 JSON 值承载，不可下钻）。三分类由此
对标记成员闭合，无两可读法：`type M = YMap<{ x: string }>;` 与 `type T = M | { y: number };`
（别名解析后 M 为标记 → 全容器形 → 多态物化，合法）；
`YPlainArray<{ a: string }> | string`（全标量形 → 原生叶子值，非 E309）。

显式标记的意义：物化在文本上自描述（不依赖读者记忆默认规则），以及切换默认
（`YPlainArray` 显式退出同步）。

### 纯值上下文（YPlainArray 子树）

`YPlainArray<T>` 的实参 T 处于**纯值上下文**：子树内一切按普通 JSON 值解释
（裸对象 = 普通对象，裸数组 = 普通数组，Record = 普通对象），**禁止**出现同步标记
`YMap` / `YArray` / `YXmlFragment`（→ VFSL-E307）；`YLeaf` 与 `Pattern` 是值
语义标记，允许出现。纯值上下文内联合按普通 JSON 值形状解释，混合联合合法
（见「默认物化规则」三分类）。

**判定时机与别名传递**：本条在模块全量解析、别名解析后判定——同步标记可经
别名**间接**进入纯值上下文：`type A = YMap<{ x: string }>; type B = YPlainArray<A>;`
中 B 的实参 A 解析后含同步标记 → VFSL-E307。锚点：标记记号直接出现时为该标记
记号；经别名引入时为实参中引入该别名的**引用记号**（上例锚点为 `A`）。

### 标记实参的形状约束

| 标记 | 实参形状约束 | 违反错误 |
| --- | --- | --- |
| `YMap` | 对象形：内联 ObjectType，或解析到 ObjectType / 对象形联合的别名 | VFSL-E304 |
| `YXmlFragment` | 同 `YMap` | VFSL-E304 |
| `YArray` | 任意 TypeExpr | — |
| `YPlainArray` | 任意 TypeExpr（子树进纯值上下文） | — |
| `YLeaf` | 标量形：原始类型 / 字面量 / 其联合 / 含 Pattern 约束（`unknown` 允许） | VFSL-E304 |
| `Pattern` | 仅 StringLiteral，且仅在 `string & Pattern<...>` 中出现 | 裸 Pattern → VFSL-E100 |

形状解析沿**别名链传递**：`type A = B;` 的形状即 B 解析后的最终形状（链式展开
到非别名目标为止），判定同样在模块全量解析后进行。标记实参位置先适用本表形状
约束（E304）；非标记位置（字段 / 元素 / Record 值）的联合形状归类见「默认物化
规则」三分类（E309）。

### YMap

| Yjs 物化含义 | 写入粒度 | PATCH 可否下钻 |
| --- | --- | --- |
| 物化为 `Y.Map`：封闭键空间逐键同步，未声明字段按对象封闭语义拒绝 | 属性级（单键 set / delete） | 可下钻 |

`YMap<T>` 的 T 为对象形（形状约束表）；T 为**对象形联合**时，键空间为各成员
字段键集之**并集**（封闭）——未被任何成员声明的键不属于该联合的键空间；写入值
与命中成员的字段匹配校验属语义层（validateSnapshot），方言层仅冻结键空间的
并集封闭。裸对象类型的默认物化即 YMap；根别名的标记决定文档根的物化。

### YArray

| Yjs 物化含义 | 写入粒度 | PATCH 可否下钻 |
| --- | --- | --- |
| 物化为 `Y.Array`：有序元素序列，逐元素同步 | 元素级（按下标 insert / delete） | 可下钻 |

裸数组 `T[]` 的默认物化即 YArray。元素为联合时按「默认物化规则」三分类处理
（全部容器形 → 元素级多态物化；全部标量形 → 原生叶子元素；混合 → E309）。

### YPlainArray

| Yjs 物化含义 | 写入粒度 | PATCH 可否下钻 |
| --- | --- | --- |
| 不物化为 Yjs 类型：在父容器中以普通 JSON 数组这一单一值承载，Yjs 不感知其内部变化 | 整体级（整体替换） | 不可下钻 |

实参子树为纯值上下文（见上），同步标记进入（含经别名间接进入）即 VFSL-E307。

### YLeaf

| Yjs 物化含义 | 写入粒度 | PATCH 可否下钻 |
| --- | --- | --- |
| 标量叶子：在父容器中以 Yjs 原生值（非 Y 类型）存储，内部结构不被 Yjs 感知 | 值级（整体赋值） | 不可下钻 |

实参须为标量形（形状约束表）；容器形实参 → VFSL-E304。

### YXmlFragment

| Yjs 物化含义 | 写入粒度 | PATCH 可否下钻 |
| --- | --- | --- |
| 物化为 `Y.XmlFragment`：子节点（文本 / 元素）可区分的结构容器 | 子节点级 | 可下钻 |

实参为对象形（形状约束表，违反 → VFSL-E304）。实参字段与 XML 结构（元素 /
属性 / 子节点）的**语义映射属语义层（求值器）职责**，方言层不冻结；方言层仅
冻结对象形约束（E304）与字段上的注释挂载规则（§5）。

### Pattern

| Yjs 物化含义 | 写入粒度 | PATCH 可否下钻 |
| --- | --- | --- |
| 无独立物化：静态字符串约束（`string & Pattern<"正则">` 的约束侧），校验属语义层 | 类型级（不可写入） | 不可下钻 |

正则语义：Pattern 实参解码后的字符串按 ECMAScript RegExp（无标志）解释；锚定
不由方言隐含（须如附录 fixture 显式书写 `^` / `$`）。实参解码后是否为合法正则
**不在方言层校验**（见 §9）。

## 4. 禁止清单与错误语义

### 结构化错误模型

违反任何规范条款即产生结构化错误：**错误身份 = 错误码 + 人类可读消息 + 行列**。
行列基准：line 与 column 均 1 起；column 按 Unicode 码点计（自行首累加）；行分隔
为 `\n`（`\r\n` 中的 `\r` 不占列）。issues 数组的精确字段形状由 parser 公共接缝
（PRD #3）冻结；本规格冻结错误身份与定位锚。

错误码共 **19 个**，三层分野：语法 E100~E106（E100 为越界语法 catch-all）、
词法 E201~E203、引用 / 语义 E301~E309。

**错误码传递通道**：issues 数组的字段形状由 PRD #3 冻结为
`{ message, line, column }`（无独立 code 字段，本规格不增改该公共接缝）。错误码
以 message 的**冻结前缀格式**传递：每条 issue 的 message 必须形如
`VFSL-E<编号>: <人类可读消息>`（如 `VFSL-E101: any 类型不在 v1 冻结子集`）——
前缀（`VFSL-E` + 三位编号 + 冒号 + 单空格）是本规格冻结项，消息正文措辞不冻结。
issue #5 起的测试应以前缀为断言锚（断言前缀格式而非消息全文，给实现留措辞
自由度）。未来若需独立 code 字段，属公共接缝变更，须回 PRD #3 走变更流程，
不在本规格 §8 演进条款的「只增不改」范畴内。

**错误数量与恢复策略**：v1 冻结「首个错误即失败」，issues 数组恰含 1 条。错误
报告**分相位**：词法 / 语法相位（E100~E105、E201~E203，含按「错误判定顺序」映射
出的专属码，以及第 7 条在声明名位映射出的 E303）在**遇到处即时失败并即报**；
仅当模块**全量解析成功**，才进入引用 / 语义相位（E106 引用图成环与
E301 / E302 / E304~E309），相位内取**文本位置最前**的一处。同一记号命中多个条件
时按「错误判定顺序」的优先级取一（该特征分派亦在词法 / 语法相位内）。错误恢复
与多错误报告留给未来版本只增引入（issues 的数组形状已为此预留）。

### 禁止清单（越界即错误）

| 禁止构造 | 违反示例 | 错误类型 | 行列信息 |
| --- | --- | --- | --- |
| any 类型 | `type T = any;` | VFSL-E101 | `any` 记号起点的 line/column |
| 自定义泛型 | `type Box<T> = { value: T };` | VFSL-E102 | 泛型参数表 `<` 起点的 line/column |
| 条件类型 | `type T = A extends B ? C : D;` | VFSL-E103 | `extends` 记号起点的 line/column |
| mapped type | `type T = { [K in Keys]: V };` | VFSL-E104 | `[` 起点的 line/column |
| interface 继承（interface 声明族） | `interface A extends B {}` | VFSL-E105 | `interface` 记号起点的 line/column |
| 递归 / 循环引用 | `type A = { b: B }; type B = { a: A };` | VFSL-E106 | 再入引用记号的 line/column（消息含环路径） |

注：PRD 禁止条目为「interface 继承」，v1 按「interface 声明族」整族冻结——任何
`interface` 声明（含无 `extends` 形态）一律 E105。

### 错误判定顺序（规范性）

E101~E105 对应的构造在 §2 文法下**不可推导**（形式上会落入 E100 catch-all：
`interface` 不以 `type` 开头、别名名后遇 `<` 非 `=`、TypeRef 后遇 `extends`、
字段名位遇 `[`）。为使专属错误码可达，tokenizer / parser 在落入 E100 之前，必须
先按下列特征把禁止构造映射到专属码（规范性判定顺序，逐条自上而下）：

1. 模块层（TypeAlias 期望位）遇前导 `interface` 记号，或类型位置（TypeExpr
   期望位）遇 `interface` 记号 → **E105**（锚 `interface` 记号）；
2. 别名声明名（Ident；声明名位为保留名时不适用本条，见第 7 条）之后遇 `<` 而非
   `=` → **E102**（锚 `<` 记号）；
3. 类型位置遇 `extends` 记号 → **E103**（锚 `extends` 记号）；
4. 字段名位置（Field 的 Ident 期望位）遇 `[` → **E104**（锚 `[` 记号）；
5. 类型位置遇 `any` 记号 → **E101**（锚 `any` 记号）；
6. `<` 前的标识符不是 `Record`、不是六个标记的标准拼写、**且不是保留名**（保留
   名见第 7 条）时：
   - 该标识符为**未声明名或标记的大小写变体**（如 `YLEaf`）→ **E301**（锚该
     引用记号）；
   - 该标识符为**已声明别名** → **E100**（锚 `<` 记号；v1 无自定义泛型的声明
     与调用，别名带实参使用即越界语法）。
7. **保留名记号出现在其对应产生式不适用的位置**：裸引用六个标记 / `Record` /
   `Pattern`（如 `type T = YMap;`，或 `Pattern` 脱离 `string &` 语境——§3「裸
   Pattern → E100」即本条实例）、保留名（`Record` 与六标记之外）后随 `<`（如
   `type T = string<number>;`）、`type` 出现在类型位置（如 `type T = type;`）→
   **E100**（锚该保留名记号）；**声明名位出现保留名 → E303**（锚声明名，解析到
   声明名时即时判定——`type type = string;` 与 `type any = string;` 同归）；
   **E301 仅适用于非保留名的标识符记号**。本条与第 1~6 条共同使「keyword 记号」
   与「统一 Ident + 后置查表」两种 tokenizer 设计产出相同的错误码与锚点。
   keyword 记号的分类以保留名集合（§4）为完备边界——集合之外不存在被分类为
   keyword 的标识符（`true` / `false` 即属此类，词法上为普通 Ident，见注记 8）：
   两种 tokenizer 设计对非保留名标识符一律按普通 Ident 读法处理，错误码与锚点
   一致。

一处构造同时命中多个特征时，取**文本位置最前**的特征；位置并列时按本条次序
靠前者（本段的特征分派在词法 / 语法相位内进行；与引用 / 语义相位的先后见
「错误数量与恢复策略」的分相位规则）。第 6 条中「已声明 / 未声明」的终判在
模块全量解析后进行——**别名解析与声明顺序无关**（前向引用
`type A = B; type B = string;` 合法），「未声明」=
模块内不存在该名的 TypeAlias 声明。该解析时机条款同样适用于
E301 / E304 / E306 / E307 / E309（全部引用 / 语义层错误）。

### 递归与循环引用检测

类型别名引用图（引用边来自字段类型、Marker 实参、Record 键 / 值、数组元素、联合
成员、Pattern 之外的一切别名引用）成环即拒绝：自引用（`type A = { x: A };`，含经
容器包裹的 `type A = { x: A[] };`）与互引用（A→B→A）同样 → E106。消息携带环路径
（如 `A → B → A`），line/column 为检测到再入的引用记号。

### 错误码总表

| 错误码 | 条件 | 定位锚 |
| --- | --- | --- |
| VFSL-E100 | 越界语法：不可从 §2 文法推导的任何构造（括号分组、负数 / 小数字面量、裸 Pattern、裸标记 / 保留名误用（判定顺序第 7 条）、未知记号等；判定顺序见 §4） | 构造起点记号 |
| VFSL-E101 | `any` 类型 | `any` 记号 |
| VFSL-E102 | 自定义泛型参数 | 泛型参数表 `<` |
| VFSL-E103 | 条件类型 | `extends` 记号 |
| VFSL-E104 | mapped type | `[` 记号 |
| VFSL-E105 | interface 声明族 | `interface` 记号 |
| VFSL-E106 | 别名引用图成环 | 再入引用记号（消息含环路径） |
| VFSL-E201 | 字符串字面量未闭合（含跨行） | 起始 `"` |
| VFSL-E202 | 非法转义（`\"` `\\` 之外） | 反斜杠记号 |
| VFSL-E203 | 块注释 / 文档注释未闭合 | 起始 `/*` |
| VFSL-E301 | 未知名引用：引用未声明别名（模块全量解析后判定，声明顺序无关），或六个标记的大小写变体（按未知名报错）；仅适用于非保留名的标识符（保留名见判定顺序第 7 条） | 引用记号 |
| VFSL-E302 | 类型别名重复声明 | 重复的声明名 |
| VFSL-E303 | 别名名占用保留名（含声明名位出现保留名 / 文法关键字，如 `type type = …`——判定顺序第 7 条） | 声明名 |
| VFSL-E304 | 标记实参形状不合法（YMap / YXmlFragment 非对象形；YLeaf 非标量形；形状沿别名链解析后判定） | 标记记号 |
| VFSL-E305 | 悬空文档注释：`/** */` 后无任何可挂载的后续声明性节点 | 注释起始 |
| VFSL-E306 | Record 键类型非 string 形（string / string & Pattern / 其别名） | 键类型起点 |
| VFSL-E307 | 同步标记位于纯值上下文（YPlainArray 子树内，含经别名间接引入） | 标记记号；经别名引入时为引入别名的引用记号 |
| VFSL-E308 | 对象字段重名 | 重复字段名 |
| VFSL-E309 | 混合联合：同步物化上下文中联合成员标量形与容器形并存（别名解析后判定；纯值上下文不适用） | 首个与首成员形状类别不同的成员起点记号 |

保留名集合：`type`、`Record`、`Pattern`、`string`、`number`、`boolean`、`null`、
`unknown`、`any`、`extends`、`interface`、`YMap`、`YArray`、`YPlainArray`、
`YLeaf`、`YXmlFragment`。别名声明占用任一保留名 → VFSL-E303（如
`type any = string;` → E303，锚声明名——保留名含 `any` 后，`any` 不再可能成为
合法别名，消解「合法声明却永不可用」的矛盾；声明名位出现文法关键字亦同，
`type type = string;` → E303，判定顺序第 7 条给唯一答案）。标识符大小写敏感，由 ASCII 字母
开头，可含 ASCII 字母 / 数字 / 下划线：letter ∈ [A-Za-z]、digit ∈ [0-9]
（**ASCII 冻结**；`$` 与非 ASCII 字母均不在 v1 标识符字符集——`type A$B = …`、
`type 資産 = …` 均不可推导 → VFSL-E100）；Unicode 标识符留给未来版本只增引入。

## 5. 注释规则

三态处理：

| 注释形态 | 处理 | IR 影响 |
| --- | --- | --- |
| `//` 行注释 | 忽略 | 无节点 |
| `/* */` 块注释 | 忽略 | 无节点 |
| `/** */` 文档注释 | 原文捕获（逐字保留，含内部 `*` 与缩进） | 挂载到相邻 IR 节点 |

**忽略与捕获的边界**：以 `/*` 开头的注释，若开标签后紧接 `*`（即 `/**` 开头）
则为文档注释；特例 `/**/` 与 `/***/` 是（空 / 单星内容的）块注释，不是文档注释。
文档注释不嵌套：自开标签后首个 `*/` 即终结。未闭合的块注释 / 文档注释 →
VFSL-E203。

**挂载规则（捕获的目标节点）**：文档注释是前导注释——挂载到紧随其后（中间仅
允许空白与忽略型注释）的**声明性节点**，三类：**类型别名**（声明处）、**属性**
（对象字段处）、**标记类型**（Marker 记号处）。连续多个文档注释按出现顺序全部
挂载到同一后续节点。若直到模块末尾都没有可挂载节点 → VFSL-E305（拒绝静默丢弃
作者语义——单一真相源不容丢失）。

**`@tag`**：不做机器解析（本方言无机器标签，全部文档性质——ADR-0001 主题）；
标签随文档注释**原文保留**，不校验、不告警（标签的机器语义属语义层任务，出范围）。

**挂载示例**（附录 fixture 的全部 7 条文档注释 → 挂载点）：

| fixture 中的文档注释 | 挂载目标 |
| --- | --- |
| `/** vfs3.assets — … */`（文件首个） | 类型别名 `AssetId`（相邻的下一个声明） |
| `/** 资产 ID：… */` | 类型别名 `AssetId`（与上一条连续，同挂一节点） |
| `/** 审计信息：… */` | 类型别名 `Audit` |
| `/** 资产实体：… */` | 类型别名 `AssetEntity` |
| `/** 附件：… */` | 类型别名 `Attachments` |
| `/** AssetsDoc：… */` | 类型别名 `AssetsDoc` |
| `/** @semantic 可选说明字段 */` | 属性 `notes?`（`AssetsDoc` 对象内） |

## 6. 大小写契约

标记类型六个，大小写是契约（精确拼写）：

`YMap`、`YArray`、`YPlainArray`、`YLeaf`、`YXmlFragment`、`Pattern`

变体拼写（`YLEaf`、`yleaf`、`ymap`、`yarray`、`yplainarray`、`YXMLFRAGMENT`、
小写 `pattern` 等）**不是已知名**：作为类型引用出现且未被声明为别名时，按
**未知名**报错（VFSL-E301，消息含原文记号与行列）。v1 **不把任何变体拼写纳入
保留名**——`type yleaf = string;` 是合法的别名声明，此后 `yleaf` 即已知名（普通
别名，与标记语义无关）；未声明时引用 `type T = yleaf;` 同样落 E301。标记拼写、
内建名与别名同居同一标识符名空间，一律大小写敏感（§4 保留名集合）。

## 7. 信封形状

```json
{
  "lang": "vfsl",
  "version": 1,
  "id": "<文档标识>",
  "text": "<VFSL 文本>"
}
```

| 字段 | 类型 | 语义 |
| --- | --- | --- |
| `lang` | 字符串，`"vfsl"` | 方言名 |
| `version` | 整数，`1` | 方言版本（文本自述版本的来源） |
| `id` | 字符串 | 文档标识；对 parser 不透明（不解析、不校验唯一性——引擎层职责，出范围） |
| `text` | 字符串 | VFSL 文本本体（UTF-8） |

parser **只消费 `text`**：信封解析与方言路由（未知方言 loud-fail 只读）是后续
引擎任务，**出范围**（out of scope）。parser 隐式按 v1 解释 text；`version` 与
text 的匹配、以及 `version ≠ 1` 的路由决策由引擎层完成。

## 8. 方言演进

v1 冻结后的演进规则：**只增不改**。

1. 语法只增：后续版本可新增生产式 / 记号，不得收窄或废除 v1 已有推导；
2. 语义不改：既有构造的物化 / 挂载 / 错误语义不得重新解释；
3. 错误码稳定：已发布错误码的条件与含义不变（新条件用新码）。

对历史文本的解释，永远以**文本自述**的方言版本为准（即信封 `version` 字段）；
本规格自身的修订同样只增不改。**首次发布前**的规格评审修订轮次不受本条约束
（错误码编号随首次发布冻结；发布后新增条件一律用新码，不得复用既有编号）。

## 9. 实现自由度与未冻结项声明

以下各项是 v1 明确**不冻结**或已给出唯一立场的边界。issue #5~#9 在这些位置
**不得做静默决定**——按本节（及所引条款）实现：

1. **Pattern 实参的正则合法性**：方言层不校验实参解码后是否为合法 ECMAScript
   正则——`type T = string & Pattern<"[">;` 通过词法 / 语法 / 形状全部检查，
   `ok: true`；非法正则的暴露时点属语义层（validateSnapshot）。
2. **UTF-8 BOM**：text 经 UTF-8 解码后的首个字符若为 U+FEFF（BOM），parser 须
   剥离且不报错；行列基准自 BOM 之后的首个字符起算（BOM 不占 line 1 的任何
   列，不计入 column）。
3. **多错误恢复**：v1 冻结单错误——issues 恰含 1 条（见 §4「错误数量与恢复
   策略」），错误恢复与多报留给未来版本只增引入。
4. **@tag 的未来语义**：story 12 的 warn 语义属语义层任务；未来版本若引入标签
   机器语义，按 §8 只增不改演进——新增告警不构成对 v1 文本「不解析、不校验、
   不告警」行为的改写。

## 10. 附录：vfs3.assets 参考 fixture

> 溯源：本 fixture 依据 **issue #9** 描述**还原**构造——原设计文档《yjs-server
> Namespace Schema 自描述体系》**缺位**（仓库与上游均无），fixture 以 issue #9
> 描述 + PRD #3 语法子集为约束自洽构造，作为规格的自含正例，供 issue #9 直接引用。

```vfsl
/** vfs3.assets — 依据 issue #9 描述还原（原设计文档缺位） */

/** 资产 ID：键约束由 Pattern 定义，禁 "." 与 "|" */
type AssetId = string & Pattern<"^[A-Za-z0-9_\\-]{1,64}$">;

/** 审计信息：所有写入留痕 */
type Audit = YMap<{
  createdBy: YLeaf<string>;
  createdAt: YLeaf<number>;
}>;

/** 资产实体：按 kind 判别的封闭联合 */
type AssetEntity =
  | { kind: "image"; url: YLeaf<string>; width: YLeaf<number>; height: YLeaf<number>; audit: Audit }
  | { kind: "text"; body: YLeaf<string>; audit: Audit }
  | { kind: "file"; name: YLeaf<string>; size: YLeaf<number>; tags: YArray<YLeaf<string>>; audit: Audit };

/** 附件：与 Yjs 同步无关的纯值数组 */
type Attachments = YPlainArray<YLeaf<string>>;

/** AssetsDoc：命名空间根文档，assets 键集受 AssetId 的 Pattern 约束 */
type AssetsDoc = YXmlFragment<{
  assets: Record<AssetId, AssetEntity>;
  attachments: Attachments;
  audit: Audit;
  /** @semantic 可选说明字段 */
  notes?: YLeaf<string>;
  keywords: YLeaf<string>[];
}>;
```

fixture 构造覆盖：六标记全部出现；`AssetId`（Pattern 键约束）、`Audit`、判别联合
`AssetEntity`（字面量联合成员 `"image"` / `"text"` / `"file"`，全部容器形成员——
按 §3 三分类物化为多态 Y.Map，非混合联合）、`AssetsDoc`；
`?:` 可选属性（`notes?`）、`T[]`（`keywords`）、`Record<`（`assets`）、
`string & Pattern<`（`AssetId`）、文档注释原文（含 `@tag`）。该文本**除词法
trivia（空白与注释，注记 9）外**按 §2 文法完全可推导，语义检查（§3~§5）全部
通过。
````

### 4.2 基线对 PRD 子集的覆盖自证（AC #1）

| PRD #3 允许语法 | 基线落点 |
| --- | --- |
| 类型别名 | `TypeAlias` 生产式 + `;` 终止（注记 4） |
| 封闭对象字面量 | `ObjectType`/`FieldList`/`Field`；封闭语义在 §3 YMap 行成文 |
| `?:` 可选属性 | `Field = Ident, [ "?" ], ":", TypeExpr` |
| 五个原始类型 | `PrimitiveType` 五选一 |
| 字面量联合 | `LiteralType`（注记 8 冻结成员种类） |
| `T[]` | `ArrayType = PrimaryType, { "[", "]" }` |
| `Record<K, V>` | `RecordType` + E306 键约束 |
| `string & Pattern<"正则">`（唯一交叉） | `PatternType`；裸 `&` 他用不可推导（→ E100） |
| 注释 | `Comment` 三态（词法形状，注记 9）+ §5 全部规则 |

fixture 每个构造的推导核对：`AssetId`→PatternType；`Audit`→Marker(YMap,
ObjectType)；`AssetEntity`→前导 `|` 联合 + 尾无/有分隔符两种对象形态；
`Attachments`→Marker(YPlainArray)；`AssetsDoc`→Marker(YXmlFragment) 内含
RecordType / TypeRef / 可选 Field / ArrayType / 属性处文档注释——全部可由文法
推出（注释与空白按注记 9 为词法 trivia，剥离后推导；词法实证见 §5 G4/G16
双绿）。

### 4.3 对下游（issue #5~#9）的契约面声明

- issue #5（parser）：本规格冻结文法、错误身份（**19 码** + 定位锚 + 行列
  基准）、**错误码传递通道（message 前缀格式 `VFSL-E<编号>: `——PRD 接缝零
  改动）与单错误策略（issues 恰含 1 条；分相位报告：词法 / 语法相位即时即报，
  语义相位取位置最前）**、注释挂载、默认物化语义检查
  （E304/E306/E307/E308/**E309** 属 parse 时可查的语义层，均在别名解析后判定；
  E309 不适用于纯值上下文）。IR 形状与 issues 字段形状不在本规格内（PRD 接缝
  所有权；如需独立 code 字段，须回 PRD #3 走变更流程，不得私改接缝）。
- issue #9（fixture/环检测）：直接引用附录 fixture 为正例（其联合成员全部
  容器形，按 §3 三分类物化，非 E309）；§4 递归检测细则是其负例设计的规范依据
  （自引用、经容器包裹自引用、互引用）。

---

## §5. 设计期实测验证（证据，2026-08-18）

按 SKILL「协议假设依据」与修订质量纪律（命令先原样跑过再收进设计），SA1 在
设计期实际运行的命令与结果如下。R1 与 R2 两轮基线各自留档，回归可比。

**A（R1，历史留档）. R1 基线全稿 vs 机器契约**（基线副本与 R1 版 §4.1 逐字
一致，11345 字符）：

```
$ cd /home/wangjian/nomicore-refactor-vfsl-v1-
$ python3 tests/acceptance/vfsl_spec_acceptance.py --spec /tmp/vfsl-spec-check/v1-spec.md
[PASS] G1 交付物存在 — 存在（11345 字符）
[PASS] G2 章节齐全 — 必需章节齐备
[PASS] G3 EBNF 块存在 — 1 个 ```ebnf 围栏块
[PASS] G4 EBNF 结构合法 — 文法块通过 tokenize + 递归下降校验
[PASS] G5 EBNF 生产式覆盖 — 必需生产式齐备
[PASS] G6 EBNF 语法要素覆盖 — PRD 语法子集要素齐备
[PASS] G7 标记语义定义·YMap / ·YArray / ·YPlainArray / ·YLeaf / ·YXmlFragment / ·Pattern — 各 1 行符合契约（6 项全 PASS）
[PASS] G8 禁止清单六项 — 六项禁止构造逐行列出
[PASS] G9 禁止清单错误语义 — 六项均含结构化错误码与行列信息
[PASS] G10 注释规则 — 注释三态/捕获挂载/@tag 规则齐备
[PASS] G11 大小写契约 — 六标记标准拼写 + 变体未知名报错
[PASS] G12 信封形状 — 信封形状 + 只消费 text + 出范围声明
[PASS] G13 方言演进规则 — 只增不改 + 文本自述版本
[PASS] G14 附录溯源 — fixture 溯源标注（issue #9 / 还原 / 缺位）
[PASS] G15 fixture 块 — 1 个 ```vfsl 围栏块，位于附录内
[PASS] G16 fixture 构造覆盖 — 六标记/AssetId/Audit/AssetEntity/AssetsDoc/联合/可选/数组/Pattern/Record/JSDoc 构造齐备
GREEN（验收通过）: 21/21 项全部通过   exit=0
```

**A′（R2，历史留档）. R2 基线全稿 vs 机器契约**——从本设计文档 §4.1 逐字提取
（提取命令见下），15962 字符：

```
$ cd /home/wangjian/nomicore-refactor-vfsl-v1-
$ awk '/^````markdown$/{flag=1;next} /^````$/{flag=0} flag' \
    wiki/raw/task_vfsl-v1-spec_design.md > /tmp/vfsl-spec-check-r2/v1-spec-r2.md
$ python3 tests/acceptance/vfsl_spec_acceptance.py --spec /tmp/vfsl-spec-check-r2/v1-spec-r2.md
[PASS] G1 交付物存在 — ../../../tmp/vfsl-spec-check-r2/v1-spec-r2.md 存在（15962 字符）
[PASS] G2 章节齐全 — 必需章节齐备
[PASS] G3 EBNF 块存在 — 1 个 ```ebnf 围栏块
[PASS] G4 EBNF 结构合法 — 文法块通过 tokenize + 递归下降校验
[PASS] G5 EBNF 生产式覆盖 — 必需生产式齐备
[PASS] G6 EBNF 语法要素覆盖 — PRD 语法子集要素齐备
[PASS] G7 标记语义定义·YMap — 1 行符合契约
[PASS] G7 标记语义定义·YArray — 1 行符合契约
[PASS] G7 标记语义定义·YPlainArray — 1 行符合契约
[PASS] G7 标记语义定义·YLeaf — 1 行符合契约
[PASS] G7 标记语义定义·YXmlFragment — 1 行符合契约
[PASS] G7 标记语义定义·Pattern — 1 行符合契约
[PASS] G8 禁止清单六项 — 六项禁止构造逐行列出
[PASS] G9 禁止清单错误语义 — 六项均含结构化错误码与行列信息
[PASS] G10 注释规则 — 注释三态/捕获挂载/@tag 规则齐备
[PASS] G11 大小写契约 — 六标记标准拼写 + 变体未知名报错
[PASS] G12 信封形状 — 信封形状 + 只消费 text + 出范围声明
[PASS] G13 方言演进规则 — 只增不改 + 文本自述版本
[PASS] G14 附录溯源 — fixture 溯源标注（issue #9 / 还原 / 缺位）
[PASS] G15 fixture 块 — 1 个 ```vfsl 围栏块，位于附录内
[PASS] G16 fixture 构造覆盖 — 六标记/AssetId/Audit/AssetEntity/AssetsDoc/联合/可选/数组/Pattern/Record/JSDoc 构造齐备
GREEN（验收通过）: 21/21 项全部通过   exit=0
```

R2 相对 R1 的条款增补（注记 9、判定顺序、E309、传递通道、保留名扩充、§9 新章、
挂载表补行、附录顺移 §10）**未破坏任何机器契约**——印证 SA2 放行条件的判断：
条款级增补不触碰 G1~G16 的解析面。

**A″（R3，现行版）. R3 基线全稿 vs 机器契约**——从本设计文档 §4.1 逐字提取
（提取命令见下），17527 字符：

```
$ cd /home/wangjian/nomicore-refactor-vfsl-v1-
$ awk '/^````markdown$/{flag=1;next} /^````$/{flag=0} flag' \
    wiki/raw/task_vfsl-v1-spec_design.md > /tmp/vfsl-spec-check-r3/v1-spec-r3.md
$ python3 tests/acceptance/vfsl_spec_acceptance.py --spec /tmp/vfsl-spec-check-r3/v1-spec-r3.md
[PASS] G1 交付物存在 — ../../../tmp/vfsl-spec-check-r3/v1-spec-r3.md 存在（17527 字符）
[PASS] G2 章节齐全 — 必需章节齐备
[PASS] G3 EBNF 块存在 — 1 个 ```ebnf 围栏块
[PASS] G4 EBNF 结构合法 — 文法块通过 tokenize + 递归下降校验
[PASS] G5 EBNF 生产式覆盖 — 必需生产式齐备
[PASS] G6 EBNF 语法要素覆盖 — PRD 语法子集要素齐备
[PASS] G7 标记语义定义·YMap — 1 行符合契约
[PASS] G7 标记语义定义·YArray — 1 行符合契约
[PASS] G7 标记语义定义·YPlainArray — 1 行符合契约
[PASS] G7 标记语义定义·YLeaf — 1 行符合契约
[PASS] G7 标记语义定义·YXmlFragment — 1 行符合契约
[PASS] G7 标记语义定义·Pattern — 1 行符合契约
[PASS] G8 禁止清单六项 — 六项禁止构造逐行列出
[PASS] G9 禁止清单错误语义 — 六项均含结构化错误码与行列信息
[PASS] G10 注释规则 — 注释三态/捕获挂载/@tag 规则齐备
[PASS] G11 大小写契约 — 六标记标准拼写 + 变体未知名报错
[PASS] G12 信封形状 — 信封形状 + 只消费 text + 出范围声明
[PASS] G13 方言演进规则 — 只增不改 + 文本自述版本
[PASS] G14 附录溯源 — fixture 溯源标注（issue #9 / 还原 / 缺位）
[PASS] G15 fixture 块 — 1 个 ```vfsl 围栏块，位于附录内
[PASS] G16 fixture 构造覆盖 — 六标记/AssetId/Audit/AssetEntity/AssetsDoc/联合/可选/数组/Pattern/Record/JSDoc 构造齐备
GREEN（验收通过）: 21/21 项全部通过   exit=0
```

R3 增补（判定顺序第 7 条与第 2 / 6 条旁注、三分类标记归类段、分相位措辞、语法
注记 10、键空间措辞替换、E100 / E301 / E303 行内交叉引用）同样**未破坏任何机器
契约**——全部为列表项 / 段落级增补与措辞替换，无新增标题、无新增表格、EBNF
围栏块零改动（G3~G6 解析面不动），GREEN 保持。

**A‴（R3 修复轮，现行版）. R3 修复轮基线全稿 vs 机器契约**——从本设计文档 §4.1
逐字提取（提取命令见下），17816 字符；与交付物 `docs/vfsl/v1-spec.md` 逐字一致
（`cmp` 零差异）：

```
$ cd /home/wangjian/nomicore-refactor-vfsl-v1-
$ awk '/^````markdown$/{flag=1;next} /^````$/{flag=0} flag' \
    wiki/raw/task_vfsl-v1-spec_design.md > /tmp/vfsl-spec-check-r3fix/v1-spec-r3fix.md
$ python3 tests/acceptance/vfsl_spec_acceptance.py --spec /tmp/vfsl-spec-check-r3fix/v1-spec-r3fix.md
[PASS] G1 交付物存在 — ../../../tmp/vfsl-spec-check-r3fix/v1-spec-r3fix.md 存在（17816 字符）
[PASS] G2 章节齐全 — 必需章节齐备
[PASS] G3 EBNF 块存在 — 1 个 ```ebnf 围栏块
[PASS] G4 EBNF 结构合法 — 文法块通过 tokenize + 递归下降校验
[PASS] G5 EBNF 生产式覆盖 — 必需生产式齐备
[PASS] G6 EBNF 语法要素覆盖 — PRD 语法子集要素齐备
[PASS] G7 标记语义定义·YMap — 1 行符合契约
[PASS] G7 标记语义定义·YArray — 1 行符合契约
[PASS] G7 标记语义定义·YPlainArray — 1 行符合契约
[PASS] G7 标记语义定义·YLeaf — 1 行符合契约
[PASS] G7 标记语义定义·YXmlFragment — 1 行符合契约
[PASS] G7 标记语义定义·Pattern — 1 行符合契约
[PASS] G8 禁止清单六项 — 六项禁止构造逐行列出
[PASS] G9 禁止清单错误语义 — 六项均含结构化错误码与行列信息
[PASS] G10 注释规则 — 注释三态/捕获挂载/@tag 规则齐备
[PASS] G11 大小写契约 — 六标记标准拼写 + 变体未知名报错
[PASS] G12 信封形状 — 信封形状 + 只消费 text + 出范围声明
[PASS] G13 方言演进规则 — 只增不改 + 文本自述版本
[PASS] G14 附录溯源 — fixture 溯源标注（issue #9 / 还原 / 缺位）
[PASS] G15 fixture 块 — 1 个 ```vfsl 围栏块，位于附录内
[PASS] G16 fixture 构造覆盖 — 六标记/AssetId/Audit/AssetEntity/AssetsDoc/联合/可选/数组/Pattern/Record/JSDoc 构造齐备
GREEN（验收通过）: 21/21 项全部通过   exit=0
```

R3 修复轮变更（SA7 F-1 方案 A：注记 8 增补、微示例 D 注释改 E301、E100 行删
「布尔字面量联合」、判定顺序第 7 条尾注增补 keyword 完备边界条款）同样**未破坏
任何机器契约**——全部为段落 / 列表项级增补与措辞替换，无新增标题、无新增表格、
EBNF 围栏块零改动（G3~G6 解析面不动），GREEN 保持；交付物默认路径同日同跑
GREEN 21/21 exit=0。

**B. 默认路径复核**（交付物未写时的红灯仍在）：

```
$ python3 tests/acceptance/vfsl_spec_acceptance.py
[FAIL] G1 交付物存在 — 规格文档缺失: docs/vfsl/v1-spec.md
（…16 项全 FAIL…）
RED（验收未通过）: 0/16 项通过，16 项失败   exit=1
```

与 SA6 红灯锚定记录一致（真实红：交付物未建前不可能转绿）。R3 修订日复核：
默认路径仍 RED 0/16，exit=1（红灯锚定不变）。

---

## §6. 文件清单（File Scope）

### ALLOW LIST

- `docs/vfsl/v1-spec.md` — **新建，唯一交付物**。按本设计 §4.1 基线（R3 现行版）成文（约 360 行 markdown）。
- `tests/acceptance/vfsl_spec_acceptance.py` — `[SA6 owned]` 验收检查脚本（已存在，本任务不改）。SA3/SA4 不改其断言逻辑。
- `tests/acceptance/exemplar/spec-exemplar-v1.md` — `[SA6 owned]` 契约示例 fixture（已存在，非交付物，不改）。
- `wiki/raw/task_vfsl-v1-spec_design.md` — 本设计文档（SA1 产出与后续 R 修订；R2/R3 修订于 2026-08-18）。
- `wiki/raw/task_vfsl-v1-spec.md`、`wiki/raw/20260818-prd-vfsl-v1.md`、`wiki/raw/task_vfsl-v1-spec_dispatch.md`、`wiki/raw/task_vfsl-v1-spec_sa2_review.md` — 任务档案（总控/SA6/SA2 已写入；如随 PR 提交属档案范围，内容不改）。
- `TASK.md` — 总控任务卡（已在工作区，不改）。

### DENY LIST

- `docs/adr/**` — ADR-0001/0002 缺位**不补写**：规格按"主题引用"成文（如
  「ADR-0001 主题：无机器标签」），补 ADR 属独立决策任务。
- `packages/**`、`src/**`、`apps/**`、`node_modules/**` — 本任务不引入任何运行时
  代码（parser 是 issue #5~#9 的事；PRD：不引入 yjs/网络/存储依赖）。
- `scripts/test-lock.sh` — 简报明示本机制无端口、无测试包，无需新增（SA4 复核确认）。
- `tests/**`（SA6 两文件之外的任何新增/改动）— 验收机制已由 SA6 锚定；parser
  测试（vitest 基线）属 issue #5~#9，本任务不建。
- `LICENSE`、`.gitignore`、`.mabf-bg/**` — 不动；`.mabf-bg` 是流程运行时产物，
  不属任务产出，不提交。

---

## §7. 协议假设依据 (Protocol Assumption Evidence)

本设计为**纯文档交付**：无 HTTP/WS 端点、无端口/进程/时序、无第三方库行为假设。
唯一的外部行为依赖是 SA6 验收命令，已用**设计期实测**闭环：

| 假设 | 依据类型 | 依据内容（具体引用） | 风险等级 |
| --- | --- | --- | --- |
| `python3 tests/acceptance/vfsl_spec_acceptance.py` 以退出码 0/1 判绿红，`--spec` 可覆写检查目标 | 设计期实测验证 | SA1 于 2026-08-18 实跑：默认路径 exit=1（RED 0/16）；`--spec <基线副本>` exit=0（GREEN 21/21）。输出全文见 §5 | 低 |
| §4.1 基线内容满足 G1~G16 全部机器契约（SA3 照抄即绿） | 设计期实测验证 | R1 / R2 / R3 三轮基线副本均实跑 GREEN 21/21（R1 11345 字符 / R2 15962 字符 / R3 17527 字符——R3 为现行版，命令与输出见 §5 A′ / A″） | 低 |
| R2 条款增补不破坏机器契约（SA2 放行条件的回归要求） | 设计期实测验证 | R2 基线副本重跑 `--spec` → GREEN 21/21 exit=0（§5 A′）；新增小节标题不含 G2/G8/G10 定位关键字、新增表格行不进入 G7/G8 解析面 | 低 |
| R3 条款增补不破坏机器契约（SA2 R2 授权「并入后不再复审」的前提） | 设计期实测验证 | R3 基线副本重跑 `--spec` → GREEN 21/21 exit=0（§5 A″，17527 字符）；增补全部为列表项 / 段落 / 措辞替换，无新增标题与表格、EBNF 围栏块零改动；默认路径同日复核仍 RED 0/16 exit=1 | 低 |
| 验收脚本判别力（非恒绿） | 现有验证记录引用 | 简报「绿路径与判别力验证」：删 `### Pattern` 章节与 `UnionType` 生产式的变异体 → exit=1 且精确定位 G2/G5/G7（2026-08-18 SA6 实测） | 低 |

---

## §8. 契约改动连锁审计 (Contract Change Caller Audit)

**无契约改动**：本设计仅产出 markdown 规格文档，不触碰任何函数签名、返回类型、
throw 行为或模块边界（仓库当前无任何源码，`LICENSE`/`.gitignore` 之外为空）。

对下游的影响是**新增文档化契约**而非改动既有契约：issue #5 的 parser 实现将消费
本规格的文法、**19 个错误码**与语义条款。错误码经 **message 前缀格式**
（`VFSL-E<编号>: `）传递——`parseVfsl` 接缝本身由 PRD #3 冻结，本规格不改写其
字段形状、也不要求新增字段（见 D9 传递通道契约与 F2 回应：独立 code 字段须回
PRD 变更流程，issue #5 不得私改接缝）。

---

## §9. SA2 反馈逐条回应

对 `task_vfsl-v1-spec_sa2_review.md`（reject，2026-08-18）全部 12 条攻击点
（F1~F4 必修 + F5~F9 MEDIUM + F10~F12 LOW）的逐条落实。修订位置中「基线 §N」
指 §4.1 基线全文内的章节（= 交付物 `docs/vfsl/v1-spec.md` 的章节）。

| 要求 | 是否落实 | 修订位置 | 修订内容摘要 |
|------|:--:|------|------|
| F1（CRITICAL）默认物化表「联合」行与 §0.3 推演自相矛盾；混合联合 / YMap 对象形联合键空间 / YArray 联合元素未定义 | ✅ | 基线 §3 默认物化表 + 三分类注 + YMap/YArray 小节；D1 | 表行改「原始类型 / 字面量**及其联合**（成员全部为标量形）」；新增「含对象 / 数组成员的联合 → 按命中成员的形状物化（多态值形状）」行，适用于字段 / 元素 / Record 值 / 标记实参；三分类注把 `Record<AssetId, AssetEntity>` 归入全容器形多态物化（表格与推演 1 不再互噬）；混合联合 v1 立场 = **拒绝，新码 VFSL-E309**（锚：首个异类成员起点；纯值上下文不适用）；`YMap<对象形联合>` 键空间 = 成员键集**之并的封闭**（值-成员匹配校验属语义层）；YArray 联合元素按同三分类 |
| F2（HIGH）18 个错误码无传递通道（PRD 接缝无 code 字段，D9 又声明不冻结 issues 形状） | ✅ | 基线 §4 结构化错误模型「错误码传递通道」；D9；§4.3；§8 | 采纳 SA2 前缀方案：每条 issue 的 message 必须形如 `VFSL-E<编号>: <人类可读消息>`——前缀（含冒号+单空格）冻结、正文措辞不冻结、**PRD 接缝零改动**、测试以前缀为断言锚；同时显式成文「独立 code 字段属 issue #5 接缝决策点，须回 PRD #3 走变更流程，不在 §8 只增不改范畴」——两头都收口 |
| F3（HIGH）禁止构造检测机制未定义，E102~E105 文法下不可达；YLEaf<string> 归属冲突；any 不在保留名致「合法声明永不可用」 | ✅ | 基线 §4「错误判定顺序（规范性）」+ 保留名集合段 + E301/E100 行；D16、D7 | 六条判定顺序成文（模块层或类型位 `interface`→E105；别名名后 `<`→E102；类型位 `extends`→E103；字段名位 `[`→E104；类型位 `any`→E101；`<` 前非 Record/六标记：未声明名或变体→E301 锚引用记号、已声明别名→E100 锚 `<`）+ 多特征命中取位置最前/次序靠前；`any`/`extends`/`interface` 加入保留名 → `type any = …` 归 E303，矛盾消解 |
| F4（HIGH）Comment 四产生式悬空，「完全可推导」声明为假 | ✅ | 基线 §2 语法注记 9 + §10 构造覆盖段末句；D17；设计 §4.2 | 注记 9 成文：空白与注释是**词法级 trivia**、任意记号边界、不参与语法推导、记号内部不容 trivia（`str/**/ing`→E100）；`Comment` 系列仅规范注释自身词法形状（E203 判定与 §5 原文捕获依据），不入语法右部；「完全可推导」全部改写为「**除词法 trivia 外**完全可推导」（基线 §10 + 设计 §4.2 + D14） |
| F5（MEDIUM）E307 经别名间接进入纯值上下文未定义；E304 别名链传递性未成文 | ✅ | 基线 §3 纯值上下文「判定时机与别名传递」+ 形状约束表后注 + E304/E307 行；D2、D3 | E307 判定在模块全量解析、别名解析后；锚点：直接出现=标记记号，经别名引入=引入该别名的**引用记号**（示例 `type A = YMap<…>; type B = YPlainArray<A>;` 锚 `A`）；E304 形状沿别名链传递（`type A = B;` 形状即 B 链式最终形状）成文 |
| F6（MEDIUM）E301 未声明名的解析时机二义（前向引用合法与否） | ✅ | 基线 §4 判定顺序末段 + E301 行；D16 | 成文「别名解析在模块全量解析后进行，与声明顺序无关；前向引用 `type A = B; type B = string;` 合法；未声明 = 模块内无该名 TypeAlias 声明」；时机条款显式覆盖全部 E3xx（E301/E304/E306/E307/E309） |
| F7（MEDIUM）§6「变体按未知名报错」与保留名不含小写变体打架（`type yleaf = …` 两条款矛盾） | ✅ | 基线 §6 重写；D7 | 立场成文：**v1 不把变体拼写纳入保留名**——未声明的变体引用自然落 E301（`type T2 = YLEaf;` / `type T = yleaf;` 未声明时均 E301）；声明后即已知名普通别名（`type yleaf = string; type T = yleaf;` → ok）——与 §4 保留名集合交叉核对一致，矛盾消除 |
| F8（MEDIUM）Ident 的 letter/digit 未定义 ASCII 还是 Unicode | ✅ | 基线 §4 保留名集合段；D7 | 成文 letter ∈ [A-Za-z]、digit ∈ [0-9]（**ASCII 冻结**）；`type 資産 = …`、`type A$B = …` 均不可推导 → E100；Unicode 标识符留给未来版本只增引入 |
| F9（MEDIUM）YXmlFragment「附着结构」未定义术语；字段↔XML 映射无边界声明 | ✅ | 基线 §3 YXmlFragment 小节；D3 | 删除「附着结构」术语；成文边界：实参字段 ↔ XML 结构（元素/属性/子节点）的语义映射**属语义层（求值器）职责**，方言层仅冻结对象形约束（E304）与字段上的注释挂载规则（§5） |
| F10（LOW）挂载示例表漏「附件→Attachments」「AssetsDoc→AssetsDoc」两行 | ✅ | 基线 §5 挂载示例表 | 补齐 2 行，表头改「**全部 7 条**文档注释」；注释原文含 `|` 处仍以 `…` 省略（D15-3） |
| F11（LOW）D15-3/D15-5 措辞与基线自洽瑕疵（§5 表含 `\|`；双「禁止」标题 vs「各自唯一」） | ✅ | 设计 §1.3 D15-3 / D15-5 | D15-3 限定到**脚本实际解析的表**（G7 六标记表、G8 禁止清单表按 `\|` 切列会 FAIL；其余表不被解析但同守纪律）；D15-5 改为「脚本行为 = 存在性 + **首个命中章节**定位，不查唯一」，并记录基线双「禁止」标题（§4 章级 + 小节级）在首个命中=§4 下实测通过——SA3 无须误修 |
| F12（LOW）边界杂项四项（非法正则 / BOM / 多错误恢复 / @tag 前瞻）无条款 | ✅ | 基线 §9「实现自由度与未冻结项声明」（新章，附录顺移 §10）；D18；D8、D11 | 逐项唯一立场：① `Pattern<"[">` → ok:true（方言层不校验正则合法性，暴露属语义层）；② BOM（U+FEFF）剥离、不报错、不占行列；③ v1 单错误——issues 恰含 1 条、首个错误即失败（恢复与多报留给未来只增）；④ @tag 未来语义按 §8 只增演进，不改写 v1「不解析不告警」 |

### R3 微修订（SA2 R2 复审非阻塞建议 N1~N5）逐条回应

R2 复审终裁 pass 时另列 2 MEDIUM + 3 LOW 非阻塞建议（评审文末「R2-2 新一轮攻击
扫描」节），授权并入后不再送 SA2。5 条全部落实（修订位置中「基线 §N」同上，
= §4.1 基线章节）：

| 建议 | 是否落实 | 修订位置 | 修订内容摘要 |
|------|:--:|------|------|
| N1（MEDIUM）裸标记引用 / 内置名后随 `<` / 声明名位 keyword 在判定顺序无归属；裸 Pattern 与 E301 对同族异判；tokenizer keyword-vs-Ident 政策未成文 | ✅ | 基线 §4 判定顺序第 7 条（新增）+ 第 2/6 条旁注 + E100/E301/E303 行 + 保留名段；D16 | 第 7 条：保留名记号在其产生式不适用的位置（裸六标记 / `Record` / `Pattern` 引用、保留名后随 `<` 如 `string<number>`、`type` 在类型位）→ **E100 锚该保留名记号**；**声明名位保留名 → E303**（`type type = …` 与 `type any = …` 同归，解析到声明名即时判定——唯一答案）；**E301 仅适用于非保留名标识符**（裸 `YMap` 与裸 `Pattern` 同族同判 E100，异判消除）；第 2/6 条加「保留名不适用」旁注（`type Record<…> =` 不再落第 2 条 E102，统一落第 7 条 E303）；明文两种 tokenizer 设计（keyword 记号 vs 统一 Ident + 后置查表）产出相同码与锚点——根因收口 |
| N2（MEDIUM）三分类未给标记归类；`YPlainArray<X> \| string` 容器形 / 标量形两可（一个读法 E309 一个放行） | ✅ | 基线 §3 三分类后新增「标记成员的形状归类」段；D1 | 标记按物化归类（别名解析后）：YMap/YArray/YXmlFragment → 容器形；YLeaf/Pattern → 标量形；**YPlainArray 同步物化上下文按标量形**（父容器中单一 JSON 值承载、不可下钻——采 SA2 建议立场并写死）；两个触发例各给唯一答案：`M \| { y: number }`（M=YMap<…>）全容器形多态物化合法、`YPlainArray<{ a: string }> \| string` 全标量形非 E309——三分类对标记成员闭合，E309 锚点规则依赖的「形状类别」随之闭合 |
| N3（LOW）单错误「位置最前」跨相位措辞不可实现（语义错误在前、语法错误在后时两实现可分报 E301/E100） | ✅ | 基线 §4「错误数量与恢复策略」重写为分相位；判定顺序末段加相位限定旁注；D9；§4.3 | 词法 / 语法相位（E100~E105、E201~E203 + 判定顺序映射码 + 第 7 条即时 E303）**遇到处即时失败即报**；全量解析成功才进引用 / 语义相位（**E106** 与 E301/E302/E304~E309）取位置最前——`type A = B; type C = (` 现有唯一答案（报 E100）；顺带收口 E106（引用图成环需全量解析）的相位归属；判定顺序「多特征取最前」注明限词法 / 语法相位内 |
| N4（LOW）词法元符号 `char` 未定义且按产生式两种语义（字符串排除行终止 / 注释包含行终止）；行注释 EOF 无换行未声明 | ✅ | 基线 §2 语法注记 10（新增，EBNF 围栏块不动）；D4 | StringLiteral 的 char **不含行终止**（E201 / 注记 6「不得跨行」的实现依据）；注释产生式的 char **含行终止**（多行块 / 文档注释由此可能，§5「逐字保留含缩进」的依据）；EOF 无换行**视同 eol** 终结行注释（合法，不报错） |
| N5（LOW）YMap 联合键空间澄清句「任一成员未声明的键」中文歧义（可读作交集，与「并集」前行矛盾） | ✅ | 基线 §3 YMap 小节一句措辞替换；D1 | 「任一成员未声明的键」→「**未被任何成员声明的键**」——与前行「并集（封闭）」单一读法，SA3 逐字照抄无歧义 |

**一致性自检**（SKILL 修订协议要求）已完成：全文搜「18 个」（错误码计数）已全量
更新为 19；「附着结构」「§4.6」等死引用已清除；「完全可推导」均已加 trivia
限定；§6 与 §4 保留名条款交叉核对一致；R2 基线副本重跑 GREEN 21/21（§5 A′）。
R3 追加自检：计数表述同步（§2 骨架 10 条注记 / 7 条判定顺序，无残留「六条」
现行态表述——R2 回应表内「六条判定顺序成文」为历史记录）；判定顺序第 2 / 6 / 7
条与错误码总表 E100 / E301 / E303 行、保留名段、§6 变体条款交叉核对一致（保留
名一律不落 E301；变体非保留名、未声明落 E301 不变）；N2 立场与 §3 形状约束表 /
纯值上下文条款无冲突（YPlainArray 实参子树纯值语义不变，归类句只管其在联合
成员位时的形状类别）；R3 基线副本重跑 GREEN 21/21（§5 A″）。
