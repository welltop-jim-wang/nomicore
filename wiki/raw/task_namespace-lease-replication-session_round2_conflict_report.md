# 冲突门禁报告 — issue #134 Round 2（合同缺陷修复修订轮，PR #146 评审 12 项）

- 被审对象：`wiki/raw/task_namespace-lease-replication-session_round2.md`（round=2 任务简报：评审 12 项 → R2-1..R2-12 验收合同）
- 冲突基准：`docs/adr/` 全集 10 份（本轮逐个全读复核，状态与 round-1 盘点一致）+ `CONTEXT.md`；重点比对 ADR 0010（含 issue #134 修订节）、ADR 0008（含 #93/#132 修订节）、ADR 0009（含 #131/#134 修订节）
- 复用 round-1 档案：`task_namespace-lease-replication-session_relevant_decisions.md`（O-1..O-12）、`_conflict_report.md`（T-1..T-7）、`_design_conflict_report.md`（C-1/C-2、R-1..R-6）——round-1 结论仍有效，本报告只裁增量
- SA8 前置门禁（Round 2），2026-08-28

## Verdict

`clear`

12 项 R2 合同逐条对账：**无 hard-violation、无 override-declared、无未声明演进**。评审定位的「违背 ADR 0010 非阻塞要求」（阻断 3）经核对属实为**实现缺陷而非合同冲突**——R2-3 的修正是回归 ADR 条款而非偏离。5 组重点对账问题全部 no-conflict（逐条见下）；其中 4 组附**登记义务**（ADR 0010 修订节 append-only 增补——沿 #64/#79/#93/#131/#132/round-1 的评审驱动修订节先例，属既定正式机制，不构成演进上报项）；R2-4 与 R2-7 的替代路径已给出放行方向与约束。

## ADR 盘点（round-2 复核）

| 编号 | 标题 | 状态 | 相关 | 对照结论 |
|---|---|---|---|---|
| 0001 | VFSL 单一真相源 | accepted（含修订节） | 弱 | 与 round-2 五组问题无条款交集；无冲突 |
| 0002 | 重写范围、authority 出范围 | accepted | 无 | 无交集 |
| 0003 | 求值器与派生 schema | accepted | 无 | 无交集 |
| 0004 | 类型协议包 | accepted | 无 | 无交集 |
| 0005 | 投影生成管线 | accepted | 无 | 无交集 |
| 0006 | 持久化插件（含 #64/#79 修订、#131 对齐说明） | accepted | 是 | R2-6/R2-7 durability 诚实域：#79「saveDoc 在 degraded 下仍必须 resolve」（L192）为 no-op/重复 update 照常登记 dirty 的互证；四态词（L187）不变。无冲突 |
| 0007 | 逻辑验证与 Yjs bridge（Runtime 部分被 0008 取代） | accepted | 是 | L54 observer no-rollback / 保守 fatal 纪律（与 0010 L112 并行，T-2 和解继续成立）；R2-6 beforeTransaction/observer 抛错的保守 committed 同源。无冲突 |
| 0008 | Runtime 读写能力与单序列器（含 #93/#132 修订节） | accepted | 高 | R2-1（bump 槽序 #132 L134「同步投影」步）、R2-2（L93 close 停接纳+barrier+排空）、R2-4（L31 META 值域 plain value 域）、R2-6（L84 保守 committed 纪律）逐条核对，见冲突点表。无冲突（R2-4 路径 (b) 的值域收窄变体有一处登记红线，见 #3） |
| 0009 | Registry、Lease 与 Host 生命周期（含 #131/#134 修订节） | accepted | 高 | R2-2（L99 shutdown 主动 close Runtime + 排空）、R2-5（L42 release 同步失效/same-Promise）、#134 修订节 L150（release 同步段调 session.close()）。无冲突 |
| 0010 | Hub/Peer WebSocket Y.Doc 复制（含 issue #134 修订节） | accepted | 最高 | round-2 五组问题第一权威；正文 L53/L55/L81/L90/L96-113/L107/L139 与修订节 L236-263 逐条对账，见冲突点表。无冲突 |

无整卷 superseded ADR；0007 仅 Runtime/open/read 部分被 0008 取代（其底层条款按 round-1 T-2 和解继续适用）。

## 冲突点（5 组重点对账 + 7 项快速对账）

| # | R2 项 | 严重度 | ADR 条款（原文锚点） | 被审对象要求 | 裁决 | 依据 |
|---|---|---|---|---|---|---|
| 1 | R2-3 fanout 异步化 | 阻断修复 | ADR 0010 L109–113 observer 三条：「只交付复制需要的 owned bytes 和受控 origin」（L111）／「observer 失败不得回滚 transaction 或使 Runtime fatal」（L112）／「队列溢出只把 channel 标记为 `needs-resync`，不得阻塞 write sequencer」（L113）；六步之 4「Runtime observer 产出 owned update 与受控 origin」（L101）；phase-5 切片 3 L73「Observer failure 隔离和 `needs-resync` 通知」、验收场景 L175「慢消费者触发 `needs-resync`，不阻塞本地业务 write sequencer」 | fanout listener 不再于 Yjs transaction observer 内同步执行；owned bytes 复制后有界异步队列投递；溢出 → needs-resync；零阻塞 sequencer | **no-conflict**（附登记义务 D-1） | R2-3 是 L109–113 的**忠实落地**而非偏离：(i) owned bytes 复制满足 L111「只交付 owned bytes」；(ii) 异步投递使 L112「不回滚不 fatal」从 try/catch 隔离升级为时序隔离（慢/重入/不返回的 listener 均不再触碰 transaction 栈）；(iii) 有界队列+溢出 needs-resync 使 L113 从 round-1 的「无队列 ⇒ 条款不可达」（设计 L24）变为**字面实现**。六步之 4 的结构不受影响：该步要求的是「**产出** owned update 与受控 origin」——产出仍同步发生在 observer 内，异步的是**投递**（listener 消费），第 5/6 步（saveDoc、释放槽）照旧。phase-5 L175 验收场景本就明文要求慢消费者 needs-resync 且不阻塞 sequencer。**round-1 读法需要修订登记**：设计 L24「同步扇出……（同步扇出天然不阻塞 sequencer）」为事实性错误断言（慢 listener 阻塞 transaction 返回 → 阻塞槽释放 → 阻塞 sequencer；round-1 复审 R-3 已承认「慢 listener 会延长槽占用」，本次评审将其升格为阻断项）；设计 L370「fanout 在事务内同步扇出（六步之 4——结构性满足）」的读法不完备——六步之 4 不要求同步执行 listener，同步扇出只是 round-1 的实现选择。登记路径见 D-1 |
| 2 | R2-1 epoch bump 主动 fence | 阻断修复 | ADR 0010 L53「bumpReplicationEpoch()……使旧 epoch 的 peer 必须显式 reset/bootstrap」；L55「缺失或不同进入稳定 `conflicted` 状态，绝不自动覆盖或合并」；L81 session 冻结 epoch；修订节 L241（currentEpoch「fence 可观测」）、L245（「`conflicted`（epoch fence）皆终态并释放槽位」）、L247（终态共用 fanout.detach 摘除点「存量 listener 即刻停止投递」）；ADR 0008 #132 L134 bump 槽序「单 Yjs transaction → 同步投影 → await notifyDirty()」 | epoch bump 槽边界主动 fence/detach 旧 epoch session（或每次投递前核对冻结 identity/epoch）；旧 session 转 conflicted 可观测；新 epoch session 可重建 | **no-conflict**（附登记义务 D-2a） | 「在 bump 的 sequencer 边界主动 fence」正是 L53「使旧 epoch 的 peer 必须显式 reset/bootstrap」之「使」的字面兑现——效力由 bump 自身产生，不等下一次 apply 被动发现；落点 = #132 槽序的「同步投影」步（bump 槽边界），零新增 sequencer 机制。终态语义复用修订节 L245「conflicted（epoch fence）终态并释放槽位」；L247 已冻结「apply 槽 R2 conflicted 转换」触发点，bump 槽主动触发是**同一终态语义的新触发面**（append-only 增补，见 D-2a）；新 epoch session 重建 = L245「终态后同 Lease 可再 open（新 open 冻结新 epoch）」。备选机制「每次投递前核对冻结 identity/epoch」由修订节 L241 currentEpoch 投影直接支持（「与 replicationEpoch 不等即已过期」）。FIFO 补测（apply A → bump → apply B）锚 ADR 0008 L36 唯一严格 FIFO |
| 3 | R2-2 Runtime close 终止摘除 sessions | 阻断修复 | ADR 0008 L93「首次调用同步进入 `closing`，立即停止接纳公共 read 和 write，并在队尾加入 close barrier；此前已接纳任务无条件排空」；#93 修订节第 2 条第 (4) 类「close 后 lifecycle≠ready 的接纳拒绝」→ `RUNTIME_WRITE_DISABLED`；ADR 0010 L90「Lease release 同步停止 session 接纳」、L179 停止顺序「关闭 channels，等待已被 Runtime 接纳的 apply 槽完成但不无限等待网络 ACK」；ADR 0009 L99「主动 close 全部 active/idle Runtime……Runtime close 自己排空已接纳写」；修订节 L246（close barrier 语义）、L247（fanout.detach 摘除点） | Runtime close 同步停接纳时终止并 detach 现存 sessions；barrier/已接纳 apply 排空语义明确并测试 | **no-conflict**（附登记义务 D-2b） | 与 ADR 0008 L93 同构：close 同步段「停止接纳」对 session 面的等价物 = 终止/detach（sessions 的 apply 槽走同一 sequencer，close 后接纳被 #93 第 (4) 类拒绝）；「已接纳任务无条件排空」= 已接纳 apply 槽照常完成（L179「等待已被 Runtime 接纳的 apply 槽完成」明文）；close barrier 在队尾 ⇒ 在途 apply 槽先于 barrier 结算（FIFO 确定性顺序有 ADR 锚点）。L90 只明文 release 触发面，Runtime close 触发面是同款生命周期纪律的更强事件（Runtime 终结）；修订节 L247 的 detach 摘除点追加 Runtime close 为第三触发面（append-only，见 D-2b）。session 终态选 `closed`（非冲突性终态）还是 `conflicted` 由 SA1 冻结——ADR 不限定，不构成门禁问题 |
| 4 | R2-4 受保护字段结构值比较 | 阻断修复 | ADR 0010 L105「在 scratch clone 上确认 update 不改变 SCHEMA，也不改变 META 中的复制身份保留字段。Peer 接收 hub update 时允许同步 ROOT、SCHEMA 和允许的 META 字段」；修订节 L251（O-12 冻结）「非 primitive 形态（Yjs 容器/对象——契约外值域）保守判『已改变』→ 拒绝」；ADR 0008 L31「META 是开放键空间，但值只允许 JSON-compatible plain value，不允许嵌套 Yjs shared type」 | 两条路径： 对允许结构值做规范化深比较；或 在更早不变量明确禁止并锁定测试 | 路径 **no-conflict 对正文 / 演进登记对修订节 L251**；路径 **no-conflict（维持 L251 原文）**——放行方向：**优先 (a)**（见依据；两路均放行， 禁止无声收窄 META 值域） | **路径 (a)**：判据名即「内容投影相等」（L251 自名）——把该判据在 ADR 0008 L31 的全部合法值域（plain value 含 object/array）上执行完整；结构值（Y.Map/Y.Array 投影为 plain）规范化深比较 =「未变化正确放行、真变化仍拒」，修正 false positive 而非放宽保护（保护对象与判据不变），与 L105「不改变」字面**一致性更强**。但它修订 round-1 冻结词 L251「保守判已改变」——须经 ADR 0010 修订节 round-2 append-only 增补登记（判据细化条目），机制沿修订节先例，非 hard-violation。**路径**：即 L251 现状 + 不变量锁定测试，零改动；其可行性事实基础 = 现行合法受保护字段写路径只产生 primitive（create 仅 docId/createdAt——ADR 0009 L60「调用方不提供 META」/L66；管理写仅 replicationId(hex)/replicationEpoch(整数)——#132；SCHEMA 恒四 string 键——ADR 0008 L30）。**红线**：若 把「META 值域」整体收窄为 primitive-only，则与 ADR 0008 L31「值只允许 JSON-compatible plain value」（明文允许 object/array）字面冲突——不得无声收窄；只可登记「受保护字段**投影比对域** primitive 不变量」（不触碰整体值域）。**放行方向 = (a)**：误拒在复制链路是可用性缺陷（合法 update 全拒 → session 卡死），且 消除对 L121「未来其他非保留 META 字段可另行决定双向语义」与 peer bootstrap 继承完整 META（L54）两个未来演进面的隐性耦合；若 SA1 选 亦放行，但必须明文登记不变量 + 锁定测试 + 与 L31 值域关系的注记。L252「不得重开」条款只覆盖「删后同值重写」边界，本项不触碰 |
| 5 | R2-6 committed 诚实 | 明确并验证 | ADR 0010 L107「Yjs 没有通用 transaction rollback……也不得虚假声称 raw update 享有验证失败零写入」；修订节 L237「经 `RuntimeWriteFatalError` rejection（`committed` 诚实）」；ADR 0008 L84「`committed:true` 或未知异常保守视为可能已提交，在当前槽内 best-effort `notifyDirty()`，但始终 reject 原始 fatal」、L86「不补偿、不 fallback、不声称 rollback」；ADR 0007 L54「不虚假声称自动回滚」 | applyUpdate 异常的 committed 标记诚实区分（beforeTransaction/observer 抛错测试）；若只能保守过报，明确规范、不宣称精确 | **no-conflict** | 诚实 ≠ 精确：ADR 0008 L84 对业务写 fatal 已冻结「未知异常保守视为可能已提交 + 始终 reject」的保守诚实语义，R2-6 要求的是把同款纪律在 replication apply 域显式化并测试。两条子路径均一致：(i) 能区分（beforeTransaction 未开始 transaction ⇒ 零 mutation ⇒ `committed:false`）则精确；(ii) 不能区分则保守 `committed:true` + best-effort notifyDirty + 明文规范「不宣称精确」——正是 L107「不得虚假声称」的字面要求。beforeTransaction/observer 抛错测试面与 ADR 0007 L54 保守 fatal 纪律同源（fanout 自身自捕获义务不变——T-2 和解继续成立；测试注入的是 yjs 层/hostile seam 异常）。若采纳保守过报规范，随 R2 文档同步入 ADR 0010 修订节/README 明文 |
| 6 | R2-7 no-op update 置位语义 | 明确并验证 | 修订节 L241（O-11 冻结）「rootValidation……（raw apply 成功后置位、生命周期内永不清除）」「memoryCaughtUp 初值冻结 false……首次 apply 成功置 true 后不回落」；ADR 0010 L107「该 update 仍被接受并继续复制，复制状态标记 `replication-unvalidated`」；CONTEXT「复制未校验」词条（L129–131）「已在 sequencer 中提交并登记 dirty」；ADR 0010 L139「状态必须区分『内存已追上』与『磁盘未追上』，不得声称 peer 副本已经 durable」 | no-op/重复 update 的 rootValidation/memoryCaughtUp 置位语义二选一明文 + 测试 + ADR 文字 | 路径一「成功接纳即置位」**no-conflict（放行方向）**；路径二「实际状态推进才置位」= 修订节 L241 冻结词演进登记（不推荐） | **路径一**：no-op update 的 apply 同为「raw apply 成功」——L241 字面直接覆盖（「成功后置位」「首次 apply 成功置 true」，无「且推进」限定词）；L107「仍被**接受**……标记 replication-unvalidated」以接纳为置位条件；CONTEXT 词条「已提交并登记 dirty」——no-op update 走完六步（Y.applyUpdate 成功返回 + 第 5 步 saveDoc 无条件登记 dirty，ADR 0006 #79 L192「degraded 不构成拒绝理由」互证）即满足词条字面；memoryCaughtUp = 「本 session 曾成功 apply（内存收到）」，no-op 亦是收到。**路径二**需改写 L241 冻结词（加「且推进」限定）并重释 CONTEXT 词条「已提交」语义，且「推进」判定需引入 transaction-empty/state-vector 比对等新误差面——登记成本高、诚实性反而下降。放行方向：**路径一**，随 R2-7 要求补「成功接纳即计数」明文规范（ADR 文字 + 测试锚）——被审合同自身已预设该文档动作 |
| 7 | R2-8 plugin role 贯通 | 明确并验证 | ADR 0010 修订节 L260（O-4）「实例静态角色经 Registry 构造 `options.role`……生产 composition root（切片 9）必须显式传 role」；CONTEXT「实例角色」词条（L133–135）「生产 composition root（phase-5 切片 9）必须显式传入」；phase-5 切片 9 L132「必须显式向 Registry 构造传 role」；round-1 O-4/INV-S14（Registry 构造注入 + `NAMESPACE_REGISTRY_ROLE_INVALID` + `REPLICATION_ROLE_PERMISSION`/`REPLICATION_ROLE_MISMATCH`） | role 贯通 plugin config/校验/构造/README/hub-peer 装配测试；或收窄完成声明 | **no-conflict**（两路均放行） | 贯通路 = 让生产 composition 具备「显式传 role」的机制前置（提前履行切片 9 义务）：Registry 构造 `options.role` 已冻结（O-4），plugin config → Registry 构造的传参是加法接线；ADR 0009 §模块与 Cordis service 未冻结 plugin config 键集（`idleTimeoutMs` 为实现词汇非 ADR 面），新增 `role` 键无条款违反；校验错误形状沿 O-4 既有词汇（SA1 冻结细节）。收窄路 = 诚实登记「生产 peer 支持延后至切片 9」——与「切片 9 必须显式传」的本切片边界一致。两路均要求 README/完成声明诚实（R2-11 联动） |
| 8 | R2-5 lease release hostile seam | 阻断修复（快速对账） | ADR 0009 L42「首次 release() 在调用栈内同步将 lease 标记为 released……重复 release 返回 exact same Promise」；ADR 0009 #134 修订节 L150「release 同步段调用既有活跃 session 的 close()（停接纳 + 退订 + 释放 slot；零新增方法面）」；修订节 L246「close()：幂等 same-promise……**永不 reject**」 | release 不先查状态、直接幂等 close()；onReleased 在 guaranteed cleanup 路径执行/隔离；hostile seam 不造成半释放/首次 release 同步抛出 | **no-conflict** | 评审指出的「先 `getStatus()` 再 close」顺序使 first-release 同步栈暴露于 session seam 异常——与 L42「同步标记 released + same-Promise 稳定」及 L246「close 永不 reject」的稳定面纪律相悖（实现缺陷）；直接幂等 `close()` + guaranteed cleanup 是对该两条的字面回归。hostile seam 测试锚 L42/L150/L246 |
| 9 | R2-9 竞态矩阵七场景 | 测试收口（快速对账） | ADR 0010 L90/L53/L55/L179；ADR 0009 L42/L48/L99；ADR 0008 #132 L136（fatal committed facts）；round-1 AC-7 同源授权 | accepted apply → release/session close/Runtime close/idle expiry/shutdown/epoch bump/committed fatal 七场景确定性合同测试 | **no-conflict** | 测试面授权充分（round-1 AC-7 锚点全数沿用；R2-1/R2-2 补充的 fence/close 触发面见 #2/#3 登记义务） |
| 10 | R2-10 owned bytes 测试直存原始参数 | 测试收口（快速对账） | ADR 0010 L111「只交付……owned bytes」；round-1 设计「每 listener 每投递独立 `Uint8Array` 副本」 | 测试直接保存 callback 原始参数，断言数组与 buffer 均不共享 | **no-conflict** | 测试方法改进，owned bytes 隔离断言更严格化；与 R2-3 的 owned 复制语义联动（异步队列投递后每投递独立副本纪律不变） |
| 11 | R2-11 两包 README 登记 | 文档收口（快速对账） | ADR 0010 L79「API 文档必须明确 raw replication 会绕过 VFSL 业务校验」；修订节登记面（L232「本 ADR 为公共契约语义来源」） | README 登记 ReplicationSession/trusted raw 例外/degraded apply/静态 role/peer 权限与生命周期边界 | **no-conflict** | L79 本就要求 API 文档明示 raw 例外；README 是修订节登记面之外的包级文档义务，纯收口 |
| 12 | R2-12 空占位与 seam 类型收敛 | 文档收口（快速对账） | ADR 0010 修订节 L253「peer 允许的 META 白名单**首版 = 空集**（⟺ META 全键保护，两侧对称）」；L121「raw caller 不得逐次自定义受保护字段集合」 | 删除或实际使用 `PEER_ALLOWED_META_KEYS` 空占位；收敛 seam 类型手工重复 | **no-conflict** | 空集是**语义冻结**（L253）而非代码占位义务——删除空占位常量不触碰语义（受保护集合仍为冻结常量）；seam 类型收敛为代码卫生（round-1 Equal 锁纪律不变） |

## 登记义务（append-only，非冲突；SA1 设计增补必须内含，SA4/文档同步核验执行）

- **D-1（R2-3）**：ADR 0010 修订节 round-2 增补——fanout 投递本切片落地「owned bytes 复制 + 有界异步队列 + 溢出 → needs-resync（L113 字面）」；撤销 round-1「本切片无队列 ⇒ L113 不可达」读法（设计 L24/L370 两处断言修订）；修订节 L241「熔断/背压属切片 6 队列属主」收窄注记为「WS 发送队列/连接级背压（正文 L151 域）」，observerFailures「无界纯计数、不熔断不自动退订」语义不变；phase-5 L81 对账注记（C-1）同步更新为「needs-resync 于本切片落地」。needs-resync 标记后 channel 投递行为（继续投递/等同 detach）由 SA1 冻结并与 L241/L247 对账。
- **D-2a（R2-1）**：修订节 L245/L247 增补 bump 槽主动 fence 触发面（conflicted 终态 + fanout.detach 摘除点复用，零新增终态语义）。
- **D-2b（R2-2）**：修订节 L246/L247 增补 Runtime close 触发面（close 同步段终止/detach sessions；已接纳 apply 槽照常排空——ADR 0008 L93/L179 锚定）；session 终态词汇选择由 SA1 冻结。
- **D-3（R2-4，若选路径 (a)）**：修订节 L251 增补判据细化条目（结构值规范化深比较；规范化语义——键序/类型/Yjs 容器投影规则——由 SA1 冻结并锁定测试）。若选路径：不改 L251，但须登记「受保护字段投影比对域 primitive 不变量」+ 锁定测试 + 与 ADR 0008 L31 值域关系注记；**禁止无声收窄 META 整体值域**。
- **D-4（R2-6/R2-7）**：保守过报规范与「成功接纳即置位」明文随 R2 文档同步入 ADR 0010 修订节/README（评审项 6/7 自带「明确规范/ADR 文字」要求）。

## 结论

**verdict: clear，放行。**

- 12 项 R2 合同无一项与 ADR 全集/CONTEXT 冲突：阻断项 1–5 全部是**实现向 ADR 条款的回归**（round-1 设计的「同步扇出天然不阻塞 sequencer」「先查状态再 close」「非 primitive 恒判已改变」「无条件 committed:true」均为对既有条款的执行缺陷或过窄读法），修复方向与 ADR 0010 L53/L55/L90/L105/L107/L109–113、ADR 0008 L31/L84/L93、ADR 0009 L42 逐条同向。
- round-1 门禁的 T-1..T-7 和解与 O-1..O-12 裁决继续有效；本轮增量裁决（R2-4 放行方向、R2-7 置位语义、R2-8 双路）与登记义务 D-1..D-4 已写入 `task_namespace-lease-replication-session_round2_relevant_decisions.md` 供全链 SA 复用。
- SA1 R2 设计增补必须内含 D-1..D-4 对应的 ADR/文档登记条目；R2-4 二选一与 R2-7 二选一按本报告放行方向裁（(a) / 成功接纳即置位），SA6 锚定与 SA2 评审不得重开为「缺陷 vs 合同」之争——语义分歧回流 SA1 按本报告方向收敛。
- 需 Jim 裁决项：无（无 evolution 上报项、无 override 需求）。
