# SA2 攻击评审报告 — 验证型交付设计（Issue #9）

**Date**: 2026-08-19
**Verdict**: reject（3 项必修：MU-5 注入配方实证错误、spec 边源枚举两处未锚、替代证据标准内部不一致；全部修订均可经设计预授权路径完成，**无需任何产品代码改动**，验证型交付形态本身不被推翻）

> 评审人独立取证方式：worktree 只读复核 + `/tmp` 副本 mutation 实验（副本已删除，worktree 零触碰，`git diff -- packages/vfsl/src/` 为空自证）。所有「实测」标记均为本人实跑，非转述 SA1。

---

## 0. 独立复核记录（攻击前置事实基线）

| 事实 | 复核方式 | 结果 |
|---|---|---|
| 99/99 全绿 + typecheck EXIT=0 + src 零 diff | 本人实跑 `pnpm test` / `pnpm typecheck` / `git diff` | ✓ 与简报 §八/§九一致 |
| 测试文件 = 归档版本 | `git diff 6178994 -- <test file>` 为空 | ✓ 逐字一致 |
| P1 fixture 三源同一 | 独立抽取四处副本（spec §10 / SA6×2 / cm）求值模板转义后逐字节 diff | ✓ 四副本 IDENTICAL（注意：须按**求值后**字符串比对——SA1 未提及此转义层，比对方法本身是个隐性坑，见攻击点 8 备注） |
| P2 七锚点 | 独立重算（1 起行、`\n` 分隔、码点列） | ✓ 7/7 落位恰为再入记号 `A` |
| P5 七常量 / census | 脚本核验 | ✓ 7 常量逐字（含首尾空格）、`/**` 恰 7 条 |
| 前序基线行号引用 | 逐处读码（errors:170/177/142、e2e:128、jd:109/121、cm:320/326、r3:85/90/95、sa7s T-R4-1/2、T-l） | ✓ 全部落实、内容与引用语义相符 |
| P7 bump 先例 | `git log -G version` + 逐 commit 取值 | ✓（补注见攻击点 7） |
| MU-2 / MU-19 / M5 行为预言 | `/tmp` 副本注入实跑 | MU-2 ✓（4 红/10 绿恰如预言）；MU-19 行为 ✓（但行号错，攻击点 4）；**MU-5 配方 ✗（攻击点 1）** |

---

## 1. 攻击点清单

| # | 严重度 | 攻击面 | 具体漏洞 | 修订要求 |
|---|--------|--------|---------|---------|
| 1 | **CRITICAL** | SA7 动态协议可执行性（§6.2 MU-5） | **MU-5 注入配方实证错误**。设计写「`:166` `!black.has(ref.name)` 改 `true`（等价 gray∪black 皆环）」——该等价关系不成立：`:166` 条件改 `true` 的语义是「黑节点重入再遍历」而非「黑节点判环」。实测（/tmp 副本）：按原配方注入后**全量测试套件挂起**（sa7s T-l 的 20k 链触发 O(n²) 重遍历，180s+ 未完成被杀），预言的「AC3×4 + AC4#1/#2 红」**不可达**；改在 `:160` 注入意图突变（`gray.has(ref.name) \|\| black.has(ref.name)`）才得到：新文件 **AC3×4 + AC4×3 全红** + 基线 cm×5、sa7s T-l、e2e×1 红（7+7=14 红/85 绿）。后果链：SA7 按预登记配方执行 → 挂起或观测与 §4 推演不符 → 完成标准 3（「观测与 §4 推演一致」）不可满足 → SA7 被迫在 HG12（verdict 真实性）与流程完成之间二选一。且 §6 硬门禁无任何超时政策，挂起是无限等待 | ① MU-5 行改写为：注入 `:160` `if (gray.has(ref.name) \|\| black.has(ref.name))`，期望观测按本报告实测更新（AC3×4+AC4×3 红；基线 cm×5+sa7s T-l+e2e 红）；② §6.2 硬门禁补超时政策：每注入全量跑加外层 `timeout`（如 300s），超时记为「FAIL-挂起」如实入报告并还原，**不得**计为「全绿」或省略；③ M5 行的期望观测同步按实测修正（AC4#3 亦红，非仅 #1/#2） |
| 2 | **HIGH** | #9 子行为覆盖完整性（§3/§5） | **spec §4 边源枚举有两个位零锚定，§3「无未处置项」结论过载**。`docs/vfsl/v1-spec.md:333` 明文：引用边来自「字段类型、Marker 实参、Record 键 / 值、数组元素、联合成员」。§3 A 表锚定了字段（A1/A5）、Marker（A4/A6）、数组/容器（A2）、Record **值**（A9）——同一句话里的 **Record 键位**与**联合成员位**却无任何负例锚定（新 14 与 85 基线均无），仅以 §5 R2/R7「罕见/低风险」接受残留。设计自己的标准自相矛盾：值位（A9）入表为一等子行为，键位不入表。更关键的是两者突变存活模式不同且 R7 论断有错：record.key walk 分支删除仅使码归属漂移（E106→E306 同位，拒绝保持——R2 分析我已静态核实成立）；**union.members 分支删除则静默放行环**（walk 不下钻联合成员时 `type A = { x: B }; type B = A \| { y: string };` 的环不可达 → ok:true，无任何其他检查兜底）——R7 只辩解「形状语义不静默」，未意识到**环拒绝行为本身会静默丢失**。#9 正是环检测任务，一个 spec 明文边源在「边丢失」突变下全绿放行，是覆盖台账的真实缺口 | 经设计预授权路径（§1.3/§10 ALLOW「SA6 owned 文件最小增量」）补 2 用例（**零 diff 基线实测均绿，是已实现未锚定，不是功能缺口**，与验证型交付自洽）：① `type A = Record<A, string>;` → `expectIssueAt(issue,'106',1,17)` + `toContain('A → A')`（实测输出 `VFSL-E106: 循环引用: A → A`@(1,17)——同时锚住 E106/E306 同位码序 106<306 裁定、record.key 边源、并顺带关闭 R1 自环路径残留）；② `type A = { x: B };\ntype B = A \| { y: string };` → `expectIssueAt(issue,'106',2,10)` + `toContain('A → B → A')`（实测通过；两成员均容器形，避开 E309 干扰）。同步：§3 A 表补两行（A19/A20 或并入 A8/A9 族）、§5 R1/R2/R7 改判「已锚定」、映射结论重述 |
| 3 | **HIGH** | TDD 替代证据标准有效性（§1.2 × §6.2） | **「逐点」承诺与「抽样」交付不一致，且抽样对 #9 的核心增量链为零**。§1.2 替代证据表第一行承诺「mutation 矩阵动态核验：对实现**逐点**注入缺陷，**逐点**观测对应测试必红」；§6.2 实际只抽样 5/20（MU-1/2/3/5/19），且抽样集全部落在 E106 链与聚合链——fixture/JSDoc 链（M8~M16）**零动态核验**，而 AC3「JSDoc 原文逐节点挂载」恰是简报 §三点名的「#9 相对 #6/#7 唯一未被前序锚定的增量」，是本交付最有价值的新锚面。另外 M8~M16 多数无 file:line 注入点（如 M8「JSDoc 捕获丢弃（docs 恒 `[]`）」未指明注入哪一行），SA7 无法照单执行 | ① §1.2 表措辞改为与 §6.2 一致：「抽样实跑，覆盖全部结论类型 + 两条主链各至少一点」，消除「逐点」虚标；② §6.2 至少补 1 条 JSDoc/fixture 链 MU 并给出具体 file:line 注入点（推荐 M11「doc 非逐字 trim」或 M8「docs 恒 []」，注入于 tokenizer/parser 的 doc 捕获处——SA1 定位后写入），期望观测 AC3#3 红；③ 若坚持 5 条抽样，须在 §1.2 显式声明增量链仅静态推演 |
| 4 | MEDIUM | 证据链行号准确性（§4 M19 / §6.2 MU-19） | MU-19 引「`:236` 排序比较器反转」、M19 引「`semantic.ts:234-237` 排序/取值破坏」——**实际排序在 `:182-184`（比较器 `:183`、`candidates[0]` 在 `:185`）**；`:234-237` 是 toIRType 尾部/空行。SA7 按行号寻点会落空。行为预言本身我已实测成立且**强于设计声称**：比较器反转 → 新 14 全绿、r3×2 红（自环版/互环版）+ sa7s×5 红（T-R2-4/R2-5/R3-2/R4-1/R4-2）——联合锚定结论正确，「必须跑全量」的规程立法得到更强实证 | 行号改为 `:183`（比较器）/`:185`（取值）；M19 期望观测可补记 sa7s×5 红 |
| 5 | MEDIUM | 期望观测精度（§4 M5） | 即使按正确注入，M5 预言也计数不足：实测 AC4×3 全红（设计只写 #1/#2；#3 的合成文本含 `Root→Nested` 黑节点共享引用同样误报）、基线红为 cm×5 + sa7s T-l + e2e 迷你 fixture（设计只写「cm fixture 用例红」）。方向保守不至于伪绿，但 SA7 逐条比对时会遇「红多于预言」的无解释差异 | 按攻击点 1 的实测数据一并更新 M5/MU-5 期望观测 |
| 6 | LOW | MU-3 配方可执行性 | `:164` 处 `a` 不在作用域（`a` 属上方独立 for 循环），`ref.pos` 改 `a.namePos` 照抄会编译错。设计已自知并留白「SA7 自行选择」——但预登记协议的价值恰在消除现场自由度 | 直接写明可用等价注入，如 `root.namePos`（root 在 `:146` 循环作用域内） |
| 7 | LOW | P7 先例表述 | 「每任务一次 patch bump，无例外」成立，但未提 #6/#7 为平行分支各自 bump 至同值 0.1.2（merge 保留一处）——版本值只有 3 个（0.1.0/0.1.1/0.1.2）而任务数 4（含 scaffold）。不补注则 SA3/SA4 可能误读为「每任务必产生新版本值」 | §7 补一句并行碰撞说明。bump 值 `0.1.2→0.1.3` 裁定**正确**（简报 §五规则驱动 + 先例一致），此项无争议 |
| 8 | LOW | P1 比对方法注记 | 三源逐字比对的「IDENTICAL」须在**模板字面量求值后**成立（源码层 SA6 副本含 `\\\\` 四反斜杠、spec 含 `\\` 双反斜杠，直接 diff 必假阴性）。SA1 探针显然做了求值（结论正确），但 §2.2 未记载该方法，SA4 复核时可能踩坑 | §2.2 P1 行补注「比对前先求值模板字面量转义」 |

**test-only 交付是否足以关闭 #9（简报 §十指定攻击面）的裁定**：定性争议已由 supervisor 裁定，SA2 不越权重审；但「test-only **充分**」的前提是台账完整+锚定经验证——在攻击点 2/3 修复前，前提不成立（两个 spec 边源无锚 + 核心增量链无动态证据）；修复后，四条 AC 的全部可观测子行为均有红灯锚定、回归锁与 mutation 证据，且全部实测数据支持「已由 #6/#7 实现、本任务补证」的判断，届时 test-only 关闭成立。设计 §8 将终裁路由给 Jim 是正确的。

---

## 2. 协议假设依据审查（2026-06-13 立法）

- **章节存在性**：§11 存在，本任务无网络/端口/进程假设，登记的是工具链行为假设——合规。
- **依据可验证性**：逐行复核——root `package.json` scripts（本人实读 ✓）、单文件 vitest 运行形态（SA6 实跑记录可查 ✓）、`git checkout --` 还原前提（worktree git 管理且 src 零 diff，#7 有同类先例 ✓）、SA4 比对形态（流程引用 ✓）。无「应该/通常/预计」类无据推断。
- **是否虚标实测**：§4 矩阵明确标注为「静态推演」、动态核验显式交给 §6 SA7——无虚标。但推演本身有两处错误（MU-5 配方等价性、MU-19 行号），见攻击点 1/4；**推演错误不等于依据造假**，SA1 的 P1~P7 自测我抽验 6 项全部成立。
- **结论**：通过（附攻击点 1/4/6 的行级修正要求）。

## 3. 错误处理链路审查（2026-05-07 立法）

- **静默失败**：测试侧全部 helper（`expectOk`/`expectSingleIssue`/`aliasNode`/`objectFieldsOf`/`fieldNode`）对前提不满足均 loud-throw 且带诊断载荷（issues/module JSON 内联），无「跳过断言即绿」路径；`expectSingleIssue` 锁 `issues` 恰 1 条，防止吞错。✓
- **状态闭环**：`{ ok: true \| false }` 判别结果两条路径均有断言（ok:false 路径断言到 message/line/column 三字段形状与 1 起基准）。✓
- **降级路径 / 虚假降级**：无网络/外部依赖面；设计唯一的条件让步「若 CI/check.sh 另有要求，以总控裁决为准」（§7）是显式上报而非降级掩盖，不构成伪降级。✓
- **链路级备注**：攻击点 2 的 union.members 突变**静默放行环**（ok:true、无任何红灯）属「拒绝行为静默丢失」家族——不是设计中的降级，而是测试盲区上的静默失败，已并入攻击点 2 修订要求。

## 4. 红线测试思路（对应攻击点的红灯 IT 方向）

1. **攻击点 1**（MU-5）：修正后的 MU-5 实跑本身就是证据；另加「挂起即红」红线——SA7 每注入跑全量须带外层 timeout，超时=FAIL-挂起入报告（防「跑不完≈没跑≈全绿」的伪证据）。
2. **攻击点 2**（两个边源锚，实测已绿、作为回归锁入列）：
   - `it('Record 键位自引用环：type A = Record<A, string>; → E106@(1,17)，消息含 A → A（E106/E306 同位码序裁定）')` → `expectIssueAt(issue,'106',1,17)` + `expect(issue.message).toContain('A → A')`；
   - `it('联合成员位互引用环：type A = { x: B }; type B = A | { y: string }; → E106@(2,10)，消息含 A → B → A')` → `expectIssueAt(issue,'106',2,10)` + `toContain('A → B → A')`。
   两例入列后：walk 的 `record.key`（`:54`）与 `union.members`（`:48`）分支删除必红，M7 残余 R2/R7 关闭。
3. **攻击点 3**（JSDoc 链 MU，新增入 §6.2）：在 doc 捕获实现点注入 `.trim()`（或恒 `[]`）→ 期望 AC3#3 七常量逐字比对红（含首尾空格锚点，P5 已证常量带空格）；红灯证明「逐字保留」断言真实可失败。
4. 攻击点 4~8 为文档修正项，无新增测试；攻击点 4 修正后 MU-19 实跑预期（新 14 绿 + r3×2 + sa7s×5 红）可直接写入 SA7 预登记。

---

## 5. 对 SA1 修订的边界重申

- 所有修订 = 设计文档修正（§1.2/§3/§4/§5/§6/§7）+ §10 ALLOW 预授权的 SA6 文件最小增量（+2 用例）——**不触碰 `packages/vfsl/src/`、不改动既有 14 条断言、不破坏 85 基线**，验证型交付的定义性属性（零产品 diff）保持。
- 修订后 14+2=16 条新用例须重跑：单文件 + 全量 + typecheck 三绿，并更新简报/设计中的计数（「14 条」→「16 条」处需全文一致性检索，含 §10 ALLOW 注记、HG14 证据段、SA4 逐行复核范围）。
- 本报告实测数据（攻击点 1/5 的红名单、攻击点 2 的两例输出）可直接被 SA1 引用入设计的期望观测栏，SA7 实跑时逐条比对——这也是对 SA7 的预登记约束：**实测若与本报告不符，以 SA7 实测为准并如实报告**（HG12）。

---

**Verdict**: reject

（SA1 按攻击点 1/2/3 完成修订并回填「SA2 反馈逐条回应」表后，SA2 复审仅核对修订点与新增 2 用例的实跑三绿记录，不再全面重攻。）

---
---

# SA2 R2 复审（2026-08-19，修订 1 设计）

**Date**: 2026-08-19
**R2 Verdict**: pass

> **复审范围**：按 R1 报告尾部自诺的快速复审——核对三项必修（攻击点 1/2/3）与五项 M/L（攻击点 4~8）的落实情况，独立复跑 SA1 对 R1 的一处证据更正（P10），不再全面重攻。范围微调依总控 dispatch：+2 用例（新#15/新#16）落地改由 SA3 在编码阶段执行、落地↔规格一致性归 SA4——R2 评审对象为**修订后的设计文档本身**；「三绿记录」相应后移为 SA3/SA7 的验证义务（设计 §3.1 落地验收 + §9 已在册）。
>
> **取证方式**：与 R1 同法——worktree 只读复核 + `/tmp` 副本 mutation 探针（五变体：base / p10 删 record.key / p11 删 union.members / p13 MU-3 配方 / p14 MU-5 修订配方，`node --experimental-transform-types` 实跑，跑毕副本删除，`git diff -- packages/vfsl/src/` 为空自证——本次复审后复验为 0 文件）。

## R2.1 独立探针裁定 — SA1 对 R1 的证据更正（P10）成立，R1 一处断言自我证伪

SA1 §2.4 P10 称：R1 攻击点 2 中「record.key walk 分支删除仅使码归属漂移（E106→E306 同位，拒绝保持）」不成立，实际输出为 `ok:true` 静默放行。本人复跑裁定：

| 变体 | 新#15 输入 `type A = Record<A, string>;` | 对照（新#16/AC1-1/AC2-1/DAG） |
|---|---|---|
| base（pristine） | `VFSL-E106: 循环引用: A → A` @(1,17) | 全部与既有锚点逐字一致（(2,10)/(1,15)/(2,20)；DAG ok:true） |
| **p10（删 semantic.ts:54 `walk(t.key, visit)`）** | **`ok:true`（候选池空，静默放行）** | 三真环输出不变；DAG 仍 ok:true |

**裁定：SA1 更正确立，R1 该句为错**。机制经源码实读闭合：shapes.ts 有**自己的独立遍历**（`collectRefs` shapes.ts:86-88 自行下降 record.key，不经 semantic.ts 的 walk），故环图仍见 A→A 边 → `computeStrForm` 将 A 判入 cycNames → `shapes.ts:379` 预填 ⊥ → `checkE306`（shapes.ts:573）仅 `=== false` 入池，⊥ 不裁决 → 无 E306 候选；semantic 侧 E106 边一消，候选池为空 → ok:true。本人 R1 的「静态核实成立」系误核（漏查 shapes.ts 独立图构建这一层）。**该更正使新#15 的价值论证更强而非更弱**：失效模式与我 R1 对 union.members 的论断同族——「环拒绝行为静默丢失」（比「码漂但拒绝保持」更重），SA1 据此将 §5 R2 风险定性升级并实锚关闭，处置正确。新#15 标题弃用 R1 拟注的「（E106/E306 同位码序裁定）」有据（该裁定在本输入上不触发；semantic.ts:21-22 明示码序仅为文法不可构造的并列之兜底），断言本体与 R1 建议逐字一致，无断言偏离。

顺带复现的其余探针（与 SA1 §2.4 声称逐项吻合）：p11（删 union.members :48）→ 新#16 `ok:true`、其余不变（P11 ✓，R1 攻击点 2 论断维持）；p13（MU-3 定稿配方 `:164` 两处 `ref.pos` → `root.namePos`）→ 编译通过、四抽样用例锚全部漂至 (1,6)（root=A 声明名位 t1y2p3e4␣5A6），9 例 `expectIssueAt` 原锚无一为 (1,6) → 全红成立（P13 ✓，R1 攻击点 6 落实）；p14（MU-5 修订配方 `:160` gray∪black）→ 真环输出与 base 全同（新#15/新#16/AC1×3/AC2 族全绿，P14「7 红/9 绿」外推闭合 ✓），DAG 对照 `type B = { a: A, b: A };` 误报 `B → A` @(2,15)——黑节点共享引用误判环的机制亲证（M5/MU-5 期望观测的方向性成立）。

## R2.2 R1 三项必修核验

| 必修 | 落实判定 | 核验证据 |
|---|---|---|
| 攻击点 1（CRITICAL）：MU-5 配方 + 超时政策 + M5 计数 | ✅ 落实 | §4 M5 / §6.2 MU-5 配方改为 `:160` 单条件改写，原配方**作废并留理由**（黑重入再遍历 O(n²)）——非静默覆盖，符合证据纪律；期望观测按 R1 实测回填（新文件 7 红/9 绿 含 AC4#3；全量 14 红 = cm×5 + sa7s T-l + e2e×1）；§6.2 硬门禁 1 墙钟政策完整（`timeout 300`、FAIL-挂起如实入报、还原清零、不计全绿/不省略/不无限重试），`timeout` v9.4 可用性本人复验；§8 风险表新增对应行。p14 探针独立复现配方可执行且真环不变 |
| 攻击点 2（HIGH）：边源补锚 + §3.1 冻结规格 | ✅ 落实 | §3 A19/A20 入表、规格依据锚定 `v1-spec.md:333`（本人实读原文：边源枚举五+位明文）；§3.1 冻结规格断言与 R1 建议逐字一致，helper 签名（`expectSingleIssue`/`expectIssueAt`，测试文件 :47/:74）与落位指令（AC2 describe 第 4 个 it :154 之后）经实读测试文件全部对上；头注释「本文件新增：」清单存在（:18），追加二词的授权有落点；§5 R1/R2/R7 改判已关闭、R2 机制表述按 P10 更正；映射结论重述且如实承认修订前版本过载。两例 pristine 行列锚 (1,17)/(2,10) 本人 base 探针逐字复现（含列核算） |
| 攻击点 3（HIGH）：§1.2↔§6.2 自洽 | ✅ 落实 | §1.2 表行 1 改为「§4 全量 20 条静态推演 + §6.2 抽样实跑 7 条 + 未抽样条由静态推演承载」——「逐点」虚标消除；MU-11（tokenizer.ts:176 body `.trim()`）入抽样，注入点本人实读确证（`pending.push({ body: text.slice(open+3, close), … })` 恰在 :176）；MU-7 补入边源链；7 条抽样清点属实，四链（E106 主链/边源/fixture·JSDoc/聚合联合）各至少一点属实 |

## R2.3 R1 五项 M/L 核验

| # | 落实判定 | 核验证据 |
|---|---|---|
| 4（MEDIUM）MU-19/M19 行号 | ✅ | §4 M19 / §6.2 MU-19 改为排序 :182-184 / 比较器 :183 / 取首 :185——本人实读 semantic.ts 逐行对上；期望观测补 sa7s×5（T-R2-4/R2-5/R3-2/R4-1/R4-2），与 R1 实测一致 |
| 5（MEDIUM）M5 计数 | ✅ | AC4×3 全红（含 #3 合成文本 Root→Nested）+ 基线红名单细化（cm×5/T-l/e2e×1），与 R1 实测逐项一致 |
| 6（LOW）MU-3 配方 | ✅ | 定稿 `root.namePos` + 作用域论证（:146 循环覆盖 :164；原 `a` 出作用域留档）——p13 探针独立复现可编译、锚漂 (1,6) 如预言 |
| 7（LOW）P7 先例口径 | ✅ | §7 补注「bump 动作每任务一次 ≠ 每任务新版本值；#6/#7 平行分支同值 0.1.2、版本值 3 vs 任务数 4」；0.1.2→0.1.3 裁定无争议维持 |
| 8（LOW）P1 比对方法 | ✅ | §2.2 P1 补注转义层（SA6 源 `\\\\` ↔ spec 源 `\\`）与「求值模板字面量后逐字节 diff」方法，R1 §0 同判 |

**边界重申核验**：①计数纪律——§2.1 注记显式区分归档史实（14/99）与前瞻计数（16/101），全文检索无混用（基线头部 :5 的 99/99 属归档事实，合规）；②worktree 实测——`packages/vfsl/src/` 零 diff、测试文件 vs commit `6178994` 零 diff（「本轮不动测试文件」属实）；③期望栏以 SA7 实测为准已入 §6.2 硬门禁 5（HG12）；④M7 六边源行号（:45/:48/:51/:54/:55/:58）与 §3 A10「六处 toContain」（既有 4 + 新 2）均经实读/检索对上。

## R2.4 R2 新发现（两处 LOW，不阻塞——无下游执行路径）

| # | 发现 | 影响 | 处置建议 |
|---|---|---|---|
| E-A | §4 M8 注入点行号引 `parser.ts:157`，实际 `claimDocs` 的 return 语句在 **:155**（:157 为空白行）；描述文字本身准确无歧义 | 无执行后果：M8 **不在** §6.2 抽样矩阵（7 条为 MU-1/2/3/5/7/11/19），SA7 不按此行号寻点；SA4 静态读码时按描述可即位 | SA1 下次自然触碰设计文档时改 `:155`，或 SA4 review 顺手勘误登记；无需为此再开修订轮 |
| E-B | §1.2「抽样实跑 7 条（覆盖 §4 全部结论类型——必红/联合锚定/预期不红）」的枚举词与 §4 正式分类学（必红/联合锚定/**存活**）不对齐——M20（存活→R3）未抽样属设计裁定（R3 归 SA4 静态读码，§9 在册），但「全部结论类型」字面过载 | 无执行后果：R3/M20 的静态归属已在 §5/§9 显式在册；「预期不红」系抽样行内子现象（MU-2/MU-19 的预期内不红），非 §4 三分类之一 | 同上——措辞层面把「全部结论类型」收敛为「必红/联合锚定两类结论 + 预期内不红子现象；存活类（M20/R3）按 §5 裁定归 SA4 静态」即可 |

两项均为文档表面误差，无 SA3/SA4/SA7 会照单执行的路径（M8 不入矩阵、M20 不抽样），按「禁止为挑刺而挑刺」原则不构成 reject 事由；登记在案供 SA4 参照。

## R2.5 复审结论

- R1 三项必修全部实质落实（非表面措辞：CRITICAL 项的配方经本人独立注入复现、HIGH 项的两用例锚经 pristine 实测逐字复现且突变失效模式经 p10/p11 亲证）；
- SA1 对 R1 的证据更正（P10）经独立复跑**裁定成立**，R1 该句自我证伪如上登记——修订不仅修了设计，还纠正了评审侧的一处错误机制归因，且更正方向与 R1 自身攻击立论（防静默丢失）一致；
- 修订过程证据纪律良好：三处推演错误（MU-5 配方、MU-19 行号、R2 机制）全部留作废理由不静默覆盖（§4 结论 4 立法化）；
- 新增 2 用例为「已实现未锚定」的回归锁增量，与验证型交付自洽；§10 ALLOW 扩展有完整授权链（SA2 攻击点 2 + 总控 dispatch），零产品 diff 定义性属性未破；
- test-only 关闭 #9 的前提（R1 §1 裁定：台账完整 + 锚定经验证）**现已满足**：spec §4 五边源位全负例锚定、核心增量链（fixture·JSDoc）入抽样动态核验、残余台账（R3~R6）如实登记且各有归属。

放行 SA3 编码阶段（bump + §3.1 逐字落地 + 三绿）。SA4 静态请参照：①§3.1 落地↔规格逐字一致性（含「仅追加、既有 14 条断言零改动、头注释仅同步二词」）；②R2.4 两处 LOW 勘误的登记；③R3（IR 无 pos）读码核对。SA7 动态按 §6 全协议（7 条 MU + 墙钟政策 + 双跑法差异报告，MU-19 为关键证据点）。

**R2 Verdict**: pass
