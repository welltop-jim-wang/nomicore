# SA2 攻击评审报告 — Parser 禁止语法负例矩阵（Issue #8）

**Date**: 2026-08-19
**Verdict**: **reject**（窄域驳回：4 项修订要求，全部为测试文本/设计文本级，零生产代码改动；核心裁决方向经独立攻击后成立，详见「独立复核记录」）
**评审对象**: `wiki/raw/task_vfsl-forbidden-syntax-matrix_design.md`（SA1 裁决型设计）
**任务类型**: 功能开发

---

## 0. 独立复核记录（攻击未击穿的部分——总控可信赖）

SA2 以全新视角对 SA1 设计的全部可检验声明做了独立复核，**以下声明全部独立验证属实**，攻击未击穿：

| # | SA1 声明 | SA2 独立验证方式 | 结果 |
|---|---|---|---|
| V1 | §2.1 L1：161/161 全绿 + typecheck 0 | SA2 自行以后台独立进程复跑：`pnpm test` → exit 0，7 文件 161/161 passed（含矩阵文件 76 tests）；`pnpm typecheck` → exit 0 无输出。日志 `/tmp/sa2-review-test.log`、`/tmp/sa2-review-tsc.log` | ✅ 属实 |
| V2 | 基线 85 + 矩阵 76 = 161 | dispatch log 第 5 行「基线 85/85 绿」存在；6 个既有测试文件实测 33+7+19+7+11+8 = 85，无回归 | ✅ 属实 |
| V3 | §2.2 L2：38 个负例单元格锚点 100% 规范可推导 | SA2 不看 SA1 审计表、仅以 v1-spec §4 判定顺序 7 条 + §6 大小写契约 + 逐字符计数独立推导全部 38 个负例的（码， line, column），再与 `parse-vfsl-forbidden-matrix.test.ts` 断言比对 | ✅ **38/38 一致**（含 E104-06 的 E100 锚 `[`@1:21——SA2 认可其「可推导前缀在 `[` 处首次断裂」论证：`type T = { readonly` 是合法前缀，`readonly` 单独可作字段名（E104-06-pos 证明），构造首个不可推导记号即 `[`） |
| V4 | §2.3 L3：每单元格有「规范条款→实现语句」对应 | 逐条核对行号引用：parser.ts:213-215（E105 模块层）、232-237（E303 先于 E102）、348-350（E105 类型位）、351-353（E103 首记号位）、308-310（dispatchContinuation extends）、354-356（E101）、396-398+481-500（generic-diag 平衡扫描）、526-528（E104）、542-544（期望 `:` → E100）、77-82（16 保留名）、tokenizer.ts:196（一律 ident）、semantic.ts:104-111（已声明→E100 锚 ltPos / 未声明→E301 锚 namePos）、index.ts:31-39/42-52（编排 + 顶层兜底） | ✅ 全部引用属实，无虚构行号 |
| V5 | §3「矩阵全绿是 #5 架构的演绎必然」 | SA2 构造矩阵外对抗变体做静态推演（12+ 个：`type Record<T>=…`→E303@1:6、`Record<any,string>` 键位→E101@1:17、`type T = extends B;`→E103@1:10、`YPlainArray<interface B {}>`→E105、`Foo<Bar extends B>`（extends 被平衡扫描吞入，终判 E301/E100）、`Foo<string[]>`、`Record<string, Foo<Bar>>`、`{ any: string }` 字段名保留名→E100、`{ extends: string }`→E100、Record 键位 `[K in …]`→E100、`interface[]`→E105、`any extends B ?…`→E101 位置最前） | ✅ 全部与规范一致，**未找到任何规范/实现分歧**；「无缺陷、无需修 parser」的裁决经攻击成立 |
| V6 | §5 G1~G4 残余单元格「当前行为与规范一致，无缺陷」 | 同 V5（G1/G2/G3/G4 四格均在推演集内） | ✅ 属实（但覆盖价值评级见攻击点 #2） |
| V7 | §4.2「零生产改动 → 不 bump 版本」 | 简报 §五 bump 触发条件为「修改 `packages/vfsl/src/**`」；Path A 与 §5 增补均 test-only，`0.1.2` 维持正确 | ✅ 裁决正确 |
| V8 | 公共接缝冻结、ADR-0001/0002、issues 恰 1 条 | 矩阵仅消费 `parseVfsl`；`expectSingleIssue` 断言 `toHaveLength(1)`；errors.ts 前缀格式 `VFSL-E<编号>: ` 与 spec §4 冻结格式一致 | ✅ 无违反 |

**结论**：SA1 设计的主体——全绿声明属实、负例期望规范可推导、实现映射存在、零生产改动的裁决方向——经 SA2 独立攻击后**全部站得住**。驳回仅针对下述 4 个攻击点。

---

## 1. 攻击点清单

| # | 严重度 | 攻击面 | 具体漏洞 | 建议修订 |
|---|--------|--------|---------|---------|
| 1 | **HIGH** | AC-2 配对完整性 × SA1 审计真实性 | **`E102-06-pos` 不是正例——它断言的是 `ok: false`**。`parse-vfsl-forbidden-matrix.test.ts:185-188` 的 it 名以 `-pos` 结尾，函数体却是 `expectAnchored(expectSingleIssue(parseVfsl('type T = Foo;')), '301', 1, 10)`（对未声明裸引用断言失败）。触发条件：逐格核对 pos 槽位时命中。影响四层：① 违反 AC-2 字面要求（简报 §二 / TASK.md：「**每个负例**配套『最接近的合法写法』正例**通过（ok: true）**」）——负例 E102-06-neg（`Foo<Bar>`→E301）在全文件中没有任何 `ok:true` 配对测试；② 违反测试文件自身头部契约（:13-17「pos = ……断言 ok: true + 声明数」）；③ 毒化 AC-3 可指认性——vitest 报告中一行 `-pos` 实为负例，逐项指认矩阵单元格时误导读者；④ **SA1 设计 §4.1 AC-2 判定行「每单元格 neg/pos 成对；pos 断言 ok:true 且解析出全部声明数」成为事实虚假的审计陈述**——§2.2 审计只逐格核了负例锚点，pos 槽位从未被审计，这正是本缺陷漏网的原因。该测试自己的注释（:186「配对正例是声明后的裸引用」）已经写明了正确的正例是什么，却没有测它 | ① 将该格改写为真正例：`expectOk(parseVfsl('type Foo = string; type T = Foo;'), 2)`；② 对照负例（裸引用未声明→E301@1:10）有独立价值（证明 E301 拒的是「未声明」而非「实参形态」），保留但改名 `E102-09-neg`（或 `-contrast-neg`）；③ 设计 §4.1/§4.2/§4.3 同步修订：「原样采纳，不修改」与「SA3 工作单 = 空」不成立——存在一项 test-only 的 SA3/SA6 工作项（ALLOW LIST 文件内改 ≤1 格 + 增 1 it，仍零 src 改动、仍不 bump） |
| 2 | **MEDIUM** | 规则映射代码路径覆盖 ×「演绎必然」论证的隐藏缺口 | **判定顺序第 3 条（E103）在实现中映射在两个不同代码位**（parser.ts:351-353 首记号位分支 + parser.ts:308-310 dispatchContinuation 分支，SA1 自己在 §2.3 对应表列了两处），**矩阵 7 个 E103 单元格全部只走后者**（`extends` 均出现在已解析成员之后），首记号位分支（`type T = extends B;`）在矩阵与全部 7 个既有测试文件中**覆盖数为 0**。触发条件：删除/回归 parser.ts:351-353 分支 → 整个仓库测试套件全绿通过，矩阵失去防护力。SA1 在 §5 G3 已自认此缺口（「矩阵现有 7 个 E103 单元格的 `extends` 均出现在已有成员之后，未覆盖首记号位」）却裁为「非阻塞」——对同类维度（E105 的模块层/类型位两分支就各有单元格：E105-04 vs E105-05）而言标准不一致。E103 是五类中唯一「一条规范条款 × 两个实现位」且其一全仓无覆盖的类别 | 随攻击点 #1 的同一次文件触碰纳入 G3（+2 it：neg `type T = extends B;`→E103@1:10 + pos `type T = string;`→ok 1），test-only 零风险。G1/G2/G4 维持 SA1 的「非阻塞可选」裁决不变（SA2 认可：三者均为同一泛化路径的额外采样，与 G3 的「独立代码位」性质不同） |
| 3 | **LOW-MED** | §8 SA4 门禁的可操作性 | 「`git diff --name-only` 的实际改动集应恰为 ALLOW LIST 第一条（1 个文件）；**多出任何文件 = scope-creep-detected**」在当前 worktree 按字面执行必然误报：分支还合法携带 `wiki/raw/task_vfsl-forbidden-syntax-matrix*.md`（3 个，git 已暂存）、`TASK.md`、`.mabf-bg/`（流程产物）。SA4 若照字面执行会把流程产物判为 scope creep；若自行豁免则依据缺失 | 比对基线显式限定为 `packages/**` 命名空间（`git diff --name-only <base>..HEAD -- packages/` 输出恰为矩阵测试文件一行），并枚举豁免清单（wiki/raw/task_*.md、TASK.md、.mabf-bg/ 为流程产物，不计入） |
| 4 | **LOW-MED** | §2.2 审计方法论完备性 | 审计表只设「负例锚列」，无 pos 槽位断言形态核验列——攻击点 #1 正是从这个盲区漏过。若不修方法论，「38/38 一致」的结论在再评审时仍只覆盖半数槽位 | §2.2 审计表追加一列「pos 槽位断言形态」（逐格核验每个 `-pos` it 含 `expectOk` 且带声明数断言），随攻击点 #1 的修复重新出具 |

**无一发现涉及生产代码缺陷**（V5 攻击推演未找到任何规范/实现分歧）——Path A 的「零 src 改动、不 bump 0.1.2」内核成立；不成立的是「矩阵资产原样合格」与「SA3 工作单为空」两句。

---

## 2. 协议假设依据审查（2026-06-13 立法）

- **章节存在性**：设计含 `## §9. 协议假设依据 (Protocol Assumption Evidence)` ✅。
- **依据可验证性**：§9 声明「本设计无协议级假设」并附 4 行实测验证表（命令 + 结果）。SA2 判定该声明**正确**——任务是进程内纯函数行为断言，无 HTTP/WS 端点、端口/进程时序、第三方库行为假设。
- **「应该/通常/预计」类无据推断**：未发现。验证表两条命令 SA2 均已独立重跑复现（test 161/161 exit 0；typecheck exit 0），引用可定位（§2.3 行号全部核实属实）。
- **结论**：本维度 **pass**。

## 3. 错误处理链路审查（2026-05-07 立法）

任务无用户交互面（前端按钮/API/异步任务），按四项检查的对应类比逐项审查：

- **静默失败**：矩阵正例以 `expectOk`（断言 `aliases` 数组长度）封死「静默截断为 ok:true」伪绿路径——**除攻击点 #1 的 E102-06-pos 外**（该格断言失败，结构上不可能捕获截断伪绿，这正是它必须是真正例的原因）。负例 `expectSingleIssue` 断言 issues 恰 1 条，无静默吞错。
- **状态闭环**：被验证对象 `parseVfsl` 全部失败路径均经返回值闭环（语法相位 throw → index.ts:37-39 转 issues；未预期异常 → index.ts:42-52 结构化 E100 兜底），无无反馈路径。
- **降级路径**：纯函数、零外部依赖，无降级场景需要设计。
- **虚假降级识别**：`index.ts:42-52` 顶层兜底是 loud 的（`ok:false` + message 注明「内部错误」，不返回 `ok:true`）——是 #5 已评审的崩溃边界转化，**非伪降级**。SA1 §6 的自查陈述与代码一致。
- **结论**：本维度除攻击点 #1 外 **pass**。

---

## 4. 红线测试思路（逐攻击点）

1. **攻击点 #1**：
   - 改写格：`it('E102-06-pos 声明后裸引用（Foo 已声明的最接近合法写法）')` → `expectOk(parseVfsl('type Foo = string; type T = Foo;'), 2)`。红灯场景：若未来 parser 将已声明裸引用误判为越界，此格红。
   - 对照格：`it('E102-09-neg 裸引用未声明名 → E301 锚引用记号 1:10')`（即原断言原样保留、仅改名归位）。
   - 结构防线（可选，防未来同类错标）：仓库级 grep 检查——所有 `it('…-pos ` 块体必须含 `expectOk(`、所有 `it('…-neg ` 块体必须含 `expectSingleIssue(`，违反即 CI 红。
2. **攻击点 #2 / G3**：
   - `it('E103-08-neg 类型位置首记号即 extends → E103 锚 1:10')`：`expectAnchored(expectSingleIssue(parseVfsl('type T = extends B;')), '103', 1, 10)`（SA2 规范推导：t1-y2-p3-e4-␣5-T6-␣7-=8-␣9-e10）。
   - `it('E103-08-pos 最接近合法写法（原始类型）')`：`expectOk(parseVfsl('type T = string;'), 1)`。
   - 红灯场景：删除或回归 parser.ts:351-353 首记号位分支时，E103-08-neg 红（当前全套件对该分支零覆盖，删之全绿）。
3. **攻击点 #3**：SA4 门禁可执行化——`git diff --name-only <merge-base>..HEAD -- packages/` 输出恰为 `packages/vfsl/test/parse-vfsl-forbidden-matrix.test.ts` 一行即 pass；多出任何 `packages/**` 文件 = scope-creep-detected。wiki/TASK.md/.mabf-bg 以显式豁免清单排除。
4. **攻击点 #4**：审计表加「pos 槽位」列后逐格 ✓；本报告 §0 V3 已示范负例列的独立重推导方法（不看 SA1 表、从 spec 重推后比对）。

---

## 5. 修订要求汇总（SA1 落实后可转 pass）

| # | 必须性 | 要求 | 涉及文件 |
|---|--------|------|---------|
| R1 | **必须** | 修复 E102-06-pos 为真正例 + 对照负例改名归位（测试文件内，+1 it）；设计 §4.1/§4.2/§4.3 修正「原样采纳不修改」「SA3 工作单 = 空」两处被证伪的表述，SA3 工作单改为「test-only 单文件小项（仍零 src、仍不 bump）」 | 矩阵测试文件 + 设计文档 |
| R2 | **必须**（随 R1 同次触碰） | §8 SA4 比对口径限定 `packages/**` + 显式豁免清单 | 设计文档 |
| R3 | **必须** | §2.2 审计方法论补 pos 槽位核验列，随 R1 重新出具该类目审计结果 | 设计文档 |
| R4 | 强烈建议（随 R1 同次触碰，成本 +2 it） | 纳入 G3 首记号位单元格（E103 双实现位之一的唯一覆盖机会）；G1/G2/G4 维持可选 | 矩阵测试文件 |

**对裁决路径的约束**：在 R1 落地并复绿（预计 161→162~164，全部 exit 0）之前，**Issue #8 不应按「测试资产交付」闭环**——AC-2 勾选框当前无法诚实勾上。R1~R3 落地后，本评审对 Path A 内核（零 src 改动、版本维持 0.1.2、中断/走完两路径边界）不再持异议。

## 6. 向总控的汇报

- 报告路径：`wiki/raw/task_vfsl-forbidden-syntax-matrix_sa2_review.md`（本文件）
- 裁决：**reject**——4 项修订要求（R1~R3 必须、R4 强烈建议），全部测试文本/设计文本级，零生产代码；SA1 设计主体（全绿属实、38 负例锚点规范可推导、无 parser 缺陷、零 src 改动不 bump）经独立攻击验证成立，修订成本低且路径明确。
- 驳回不改「中断门禁成立」的定性：SA6 的 A.2 分支声明与 SA1 的独立验证均属实，功能自 #5 存在的裁决 SA2 予以确认；但「矩阵资产合格交付」的前置条件是 R1 修复。

---
---

# SA2 R2 轮重审报告（2026-08-19）— 修订闭环核验与最终裁决

**Date**: 2026-08-19
**Verdict**: **pass**（R1~R4 四项修订要求逐条核验**全部闭环**，闭环证据均经 SA2 独立取证（非采信 SA1 自述）；另录 1 项**非阻塞勘误** F1，约束 SA3 执行口径，不构成驳回）
**评审对象**: `task_vfsl-forbidden-syntax-matrix_design.md` 修订 R1 版（含 §0 修订总览 / 「SA2 反馈逐条回应」/ 文末修订记录）
**方法**: 逐条攻击点 → SA1 落实条款 → SA2 独立取证复验；对修订新增的可执行规格（§4.3 工作单四条 it）做**矩阵外动态探针预执行**（临时 vitest 文件，跑完即删，typecheck 复跑坐实 exit 0）。

## 1. 修订闭环核验（R1~R4 逐条）

| 要求 | SA1 落实（条款位） | SA2 独立取证 | 闭环判定 |
|---|---|---|---|
| **R1** 修复 E102-06-pos + 对照负例归位；撤销「原样采纳」「工作单=空」两处虚假表述 | §4.3 工作单 R1-a/R1-b（it 名 + 块体 + 位置锚逐字给出）；§4.1 AC-2/AC-3 改判「⚠️ 当前不成立 → R1 落地后成立」；§4.2 显式撤销「原样采纳」、闭环时点后移；§0 修订总览自认 | ① 实读测试文件：`:185-188` 伪正例**原样在库**（SA1 纪律声明属实——工作单正确留待 SA3 执行，未越权触碰）；R1-a 位置锚 `:185-188` 精确（恰为伪正例块），「原 `:187` 断言原样保留」属实。② **探针预执行**：`expectOk(parseVfsl('type Foo = string; type T = Foo;'), 2)` 与对照体 `E301@1:10` 均**绿**——「预期全绿」非纸面推断。③ §4.1/§4.2 被证伪表述的撤销量与 R1 要求逐条对应，无残留（全文 grep「原样采纳」仅存于撤销声明语境） | ✅ **闭环** |
| **R2** SA4 比对口径限定 `packages/**` + 显式豁免清单 | §8 重写：基线钉 `b076d41`、范围 `-- packages/`、提交前（porcelain）/提交后（diff）双命令、豁免清单 3 类枚举 | 实测复验：`git log -1` = `b076d41`（HEAD 即任务起始 commit，**不可用 origin/main** 的论证成立——#5/#6/#7 交付全在本分支 lineage）；`git status --porcelain -- packages/` 实测**恰 1 行** `?? …forbidden-matrix.test.ts`（未跟踪文件对 `git diff` 不可见的附加坑是 SA1 超出 SA2 攻击面的自主发现，价值确认）；豁免清单与 `git status --porcelain` 实测逐项吻合（4 暂存 wiki + TASK.md + .mabf-bg/） | ✅ **闭环** |
| **R3** 审计方法论补 pos 槽位核验列并重新出具 | §2.2 五表各追加「pos 槽位（R3）」列 + 方法论补丁说明；结论 L2 重新出具（负例锚 38/38 维持 + pos 37/38、违规定位 E102-06-pos）；§7.1 第 3 层补教训注记 | 机械复扫（python3 解析 76 个 it 块）：`neg=38 / pos=38`，**恰 1 个 pos 块体不含 `expectOk`（E102-06-pos）**，38 个 neg 全部含 `expectSingleIssue`——与 SA1 awk 结论逐数一致，审计重出并非转述 | ✅ **闭环** |
| **R4** 纳入 G3 首记号位单元格（+2 it）；G1/G2/G4 维持可选 | §4.3 R4-a/R4-b 规格；§5 G3 升级基线 + E103 双实现位覆盖对照表；§2.2/§2.3 新增行；§4.5 否决备选「G3 维持可选」条目；§7.1 第 5 层反驳 | ① `grep -rn "=\s*extends" packages/vfsl/test/` 实测**零命中**（立项依据属实）。② 代码审读：parser.ts:336-337（parsePrimaryType case 'ident'）→ :351-353（首记号 `extends` → E103 锚 tok）路由与设计描述一致。③ **探针预执行**：`type T = extends B;` → 单条 issue、前缀 `VFSL-E103: `、**1:10**——规范推导（t1·y2·p3·e4·␣5·T6·␣7·=8·␣9·e10）与实际行为一致，配对 pos `type T = string;` → ok·1 绿。E103「一条条款 × 两个实现位」自此双位均有防护锚 | ✅ **闭环** |

**计数自洽复核**：76→79（+E102-09-neg / +E103-08-neg / +E103-08-pos，E102-06-pos 原位改写不计数）✓；161→164 ✓；§2.2「40 单元格 = 38 既有 + E102-09 对照行 + E103-08 对（一行两槽）」✓——三组计数在 §2.1/§2.2/§4.3/§6/§8 全文成对一致，无口径漂移。

## 2. 本轮新发现（非阻塞勘误）

| # | 严重度 | 位置 | 具体问题 | 处置 |
|---|---|---|---|---|
| F1 | **LOW-MED**（非阻塞） | 修订版 §4.3 R4-a/R4-b 的位置锚 | 「E103 describe 块末尾、**现 :205 `});` 之前**新增」自相矛盾：实测 `:205` 是 **E102** describe 块的闭合行（E102 块 = 140–205），E103 describe 块闭合在 **`:266`**（E103 块 = 210–266）。描述锚（「E103 describe 块末尾」）正确，行号锚错误。若 SA3 照行号字面执行：E103-08 对落入 E102 describe → 违反 AC-3 组织契约（describe 名 = 错误码类别），且两 it 仍绿（无失败信号），vitest 报告两行错类目 | **SA3 执行口径以描述锚为准：E103 describe 块末尾（现 `:266` `});` 之前）插入**。建议 SA1 下次触碰设计文档时勘误（:205 → :266），无需为此重开评审轮。注：§8 ALLOW LIST 条目未传播该错锚，不构成连带 |

不升级为驳回的量级依据：工作单的全部可执行内容（it 名、块体、锚点 1:10、断言形态、预期计数）正确且经探针逐字预执行验证；错的是辅助性行号，且其主锚（描述性）无歧义。

## 3. 独立取证记录（本轮全部命令与结果）

| 取证项 | 命令/方式 | 结果 |
|---|---|---|
| 工作单四条规格动态预执行 | 临时 vitest 探针文件（复刻矩阵文件三个 helper 逐字调用 §4.3 规格），后台独立进程 | **4/4 绿**；同轮全套件 165/165（161 既有 + 4 探针）exit 0——顺带三度复现全绿 |
| 探针无残留 | 删除临时文件 + `pnpm typecheck` 复跑 | exit 0；test/ 恢复 7 文件原状 |
| pos/neg 断言形态机械扫描 | python3 解析 it 块体 | 76 = 38 neg + 38 pos；pos 违规恰 1；neg 违规 0 |
| E103 首记号位零覆盖 | `grep -rn "=\s*extends" packages/vfsl/test/` | 零命中（exit 1） |
| R2 口径实测 | `git log --oneline -1` / `git status --porcelain [-- packages/]` | HEAD=`b076d41`；packages/ 恰 1 行未跟踪矩阵文件；豁免集与实测逐项吻合 |
| SA1 纪律（零触碰测试/源码） | git status（src 无任何修改）+ 实读 :185-188 | 伪正例原样在库、76 it 未变——工作单确为「规格」而非「已执行」 |

## 4. 协议假设依据 / 错误处理链路（R2 复核）

- **协议假设依据**：§9 章节在，本轮新增 4 行验证记录（pos 扫描 / 首记号零覆盖 grep / 基线与豁免集实测）全部可重跑、SA2 已重跑复现。无「应该/通常/预计」类无据推断。**pass**。
- **错误处理链路**：攻击点 #1 修复后，静默失败检查的最后缺口闭合——E102-06-pos 改写体带 `ok·2` 声明数断言，封死截断伪绿；R4 新增格沿用同构断言形态。其余三项（状态闭环/降级路径/虚假降级）结论与首轮一致。**pass**。

## 5. 红线测试思路（F1 对应，供 SA4）

- **SA4 静态检查项**：矩阵文件 diff 中 `E103-08-neg` / `E103-08-pos` 两条 it 必须位于 `describe('E103 — …` 块的行区间内（当前文件 E103 块 = :210–:266；SA3 改动后行号位移，以 describe 起止重新定位）。落在 E102 块内 = 位置错标（AC-3 违例），**即便全绿也判退回修正**。
- **SA7 动态检查项**（既有约束不变）：164/164 exit 0 + typecheck exit 0；`git diff --name-only b076d41..HEAD -- packages/` 恰 1 行（§8 R2 口径）。

## 6. 最终裁决与放行条件

**Verdict: pass。** SA1 修订版对 R1~R4 逐条闭环且闭环证据经 SA2 独立复核属实；「修正后采纳」的裁决路径（test-only 工作单 + 零 src + 不 bump 0.1.2 + 闭环时点后移至复绿后）SA2 无异议。放行附两条约束：

1. **SA3 执行口径**：§4.3 工作单按原文执行，唯 R4 插入位以「E103 describe 块末尾」描述锚为准（F1 勘误）；
2. **闭环时点**（与设计 §4.2.4 互为锚点）：工作单落地 + 164/164 + typecheck exit 0 之前，Issue #8 不得按「测试资产交付」闭环、AC-2 不得勾选。

`pass` 仅覆盖**设计层**；SA3 产出的 diff 合规（§8 R2 口径 + F1 位置检查）与复绿验证归 SA4/SA7。
