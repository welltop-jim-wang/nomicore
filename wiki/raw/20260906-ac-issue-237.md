# 红灯验收契约（SA6 固化）— issue #237：路径级校验替代完整 ROOT 复制与全量校验

- **阶段**：acceptance-contract（SA6，round 1, iteration 0；首次 dispatch `sa-c07c57ff-a690-487c-a287-a6a162d8a591` 因 Host observer schema failure 未产生有效 completion，2026-09-06 完整重跑，见 §运行与证据与 `wiki/raw/task_237_sa6.md`）
- **日期**：2026-09-06 | **worktree**：`/home/wangjian/nomicore-fix-issue-237`（HEAD `9e3f0bf`，branch `mabf/issue-237`）
- **输入**：issue #237 正文 + Owner 3 条评论（2026-09-05T16:01Z / 16:08Z / 2026-09-06T02:55Z）；SA5 报告 `wiki/raw/20260906-bug-237.md`；SA8 门禁 `wiki/raw/task_237_conflict_report.md`（verdict `clear`，E1–E4）+ `wiki/raw/task_237_relevant_decisions.md`
- **SA6 边界**：只固化并运行可执行红灯验收契约（test 文件），不实现生产改动（SA3 owned），不扩公共 API，不把 #238 replication latency 归因本 issue。

## 可执行契约（本阶段产物）

| 文件 | 层 | 角色 |
|---|---|---|
| `packages/doc-runtime/test/issue-237-path-localized-validation-red.test.ts` | doc-runtime `applyValidatedMutation` 内部管线 | 热路径去全量（seam 计数锚）、无关分支语义反转、零写入/fatal 失败面、路径 carrier topology、批量数组整体判定、行为等价 oracle |
| `packages/namespace-runtime/test/runtime-mutate-issue-237-hot-path-red.test.ts` | 唯一公共入口 `mutateData`（真实 Runtime seam + 真实 vfsl 编译） | 公共 interface/严格 FIFO/单事务不回归、零写入零 dirty 零事件、R2 反转、大 ROOT 五笔 instrumentation（dirty notification + wire update count） |

红灯纪律：断言全部锚定可观测行为（结果联合 / Yjs 状态字节 / update 事件计数 / dirty 计数 / 模块 seam 调用计数）；「必红」用例当前代码必须失败（本文件交付时即红，SA3 实现后转绿），「绿锁定」用例当前已绿、修复后不得回归；**零墙钟计时断言**（计数锚大小无关、零 flake）。

## Owner 要求 → 测试锚点映射（Owner requirements must be explicit in tests）

| # | Owner/issue 要求（任务材料原文要点） | 锚点（文件 · describe · 用例） | 当前状态 |
|---|---|---|---|
| 1 | phase-1 前置条件 = mutation 开始前 committed 文档合法（logical values + **carrier topology**），显式写入测试前置 | doc-runtime：`expectValidBaseline`（每个用例 mutation 前 extract + `validateLogicalSnapshot` 双断言）；runtime：`expectValidBaseline`（P0 ready 后） | 绿锁定 |
| 2 | 删除 ordinary-write 热路径旧完整 ROOT `validateLogicalSnapshot()`；不建 committed-generation/document-baseline 状态机；只提取/重建/校验 mutation path + 最近必要语义边界 | doc-runtime A-1【必红】：叶子 set/delete/批量 array-insert/array-delete 期间完整 `extractYjsSnapshot`/`validateLogicalSnapshot`/`verifySnapshotIntact` 调用计数 = 0（seam 包装计数；当前 extract×3/validate×2/verify×1） | **必红** |
| 3 | 无关 ROOT 分支不被访问/复制/校验；无关非法数据不再阻断路径外普通写（已声明语义，非疏漏） | doc-runtime A-2【必红】 + runtime B-3【必红】：live 写坏无关 `library[0].qty`（replication-unvalidated 形态）后，`['target','value']` 合法写必须 ok:true、无关分支原样保留、仍单事务恰一次 dirty | **必红** |
| 4 | mutation 路径导航检查局部 carrier topology；不实例化不匹配 carrier | doc-runtime A-4【绿锁定】：路径上 Y.Map↔Y.Array 拓扑违规 → 响亮 ok:false + 零写入（非 internal fatal、非 ok:true、禁 write 后 undo） | 绿锁定 |
| 5 | validation failure 在触碰 live Y.Doc 前决定（零写入、零 dirty、零 update）；禁止 live-doc write 后 undo | doc-runtime A-3/A-5（失败面断言 stateBytes 不变 + 0 update 事件）；runtime B-2：mutateData 失败 → ok:false + 字节不变 + notify=0 + 事件=0 + 槽不中毒 | 绿锁定 |
| 6 | set / delete / 批量 array-insert / 批量 array-delete 均走局部校验路径（成功写保持最小 edit、单 guarded transaction、同槽 dirty notification、保留不相关 carrier identity） | doc-runtime A-1（计数锚）+ A-5（批量整体判定、carrier identity、单事务）+ runtime B-1/B-4（{ok:true} 精确结果、notify=写数、事件=写数、单笔 <128B/256B、Y.Map/Y.Array identity 稳定） | A-1 **必红**；其余绿锁定 |
| 7 | 批量数组操作整体一次校验，不得逐元素独立校验（中间态/终态合法性可能不同） | doc-runtime A-5：批量中任一元素非法（含 union 成员错 `kind:"c"`、类型错 `x:string`）→ **整批**零写入拒绝；越界 index/count 不 clamp | 绿锁定 |
| 8 | 判别联合、Record、optional、数组边界、嵌套引用与完整快照校验行为一致 | doc-runtime A-6 行为等价：28 个场景（union 成员内/交叉/判别值非法/成员切换、Record 值内 set/键缺失/动态键 delete、optional 填充/删除、YArray 下标/append/insert/delete 边界、两级具名引用下钻）——增量判定 ≡ 完整 proposed ROOT 全量校验（决策 + 结果 doc 全等 + 失败零写） | 绿锁定（修复后持续守护 ADR-0007 行为等价硬前置） |
| 9 | schema 语义要求更大上下文时安全退化到更高边界直至 ROOT（行为覆盖闭包） | 设计约束锚（不可直接测“必要时”：空路径 set 整体替换 = 唯一合法全量形态——A-6 含等价用例保行为；create/replaceSchema 全量校验不动，由既有绿测试族锚定） | 绿锁定（引用） |
| 10 | 提交后 internal invariant 检查不得无条件重新提取并校验完整 ROOT（fatal 分类不削弱） | doc-runtime A-1 计数锚（`verifySnapshotIntact` = 0 for ordinary path mutation；替换后的边界级提交后验证由 SA1 设计 + 既有 fatal 测试族 `apply-validated-mutation-fatal-contract.test.ts` 守护 committed fatal 语义） | **必红** |
| 11 | 大 ROOT benchmark/instrumentation：连续五笔叶子 mutation（30s 周期 fake-clock 压缩），保持 dirty notification 与 wire update count；证明无关分支不被访问/复制/校验；不把 #238 归因本 issue；不扩公共 API | runtime B-4：8k 无关条目 × 5 笔顺序叶子 set → ok 全 true、notify=5、update 事件=5、单笔 <256B、Y.Array identity+规模+内容零触碰、尾部全局逻辑合法（30s 周期压缩为紧邻调用——写路径无墙钟依赖；wire 层冻结引用 `ws-replication-issue230-incremental-mutation.test.ts`） | 绿锁定 |
| 12 | 公共 `mutateData` interface / 结果联合 / 严格 FIFO / 最小 edit / 单 guarded transaction 全部保持 | runtime B-1 + B-2（逐笔 `toEqual({ok:true})`；失败零副作用；成功语义计数不变；FIFO 深度锚引用既有 `runtime-mutate-root-sequencer.test.ts`） | 绿锁定 |
| 13 | 零写入/fatal/嵌套路径/carrier identity 回归不削弱 | A-3/A-4/A-5 + A-6 失败面 + 既有 `apply-validated-mutation-operations.test.ts` / `apply-validated-mutation-fatal-contract.test.ts`（本套件运行不改动既有文件） | 绿锁定 |

## 随代码交付的强制文档义务（E1–E4，SA8 门禁登记；本文件只登记不实现）

SA3 实现必须同时交付（任务简报材料，SA6 不实现生产/文档改动）：

- **E1 — ADR-0007 显式修订节**：热路径旧 ROOT 全量校验删除；新写前置假设（committed ROOT 符合 active schema：logical values + carrier topology）；边界级 proposed 校验；提交后局部验证替代完整一致性校验；「当前 ROOT 已损坏时普通 mutation 失败」条款收窄为 mutation 路径/边界内损坏。
- **E2 — ADR-0008 §ROOT write 镜像句同步修订**（保持与 ADR-0007 单一真相源一致；槽序/公共 interface/fatal/「非空路径不重建完整 ROOT」其余约束不动）。
- **E3 — ADR-0010 L107 后备句修订 + follow-up 显式登记**：后续普通业务写按路径级/边界级校验；replication / 损坏存量 / 不可信恢复的合法性重建与 carrier validation 覆盖面审计（Owner 2026-09-05T16:08Z §1/§6 拆分方向）登记为 follow-up，不在本票静默留白。
- **E4 — CONTEXT.md 词条修订**：「逻辑快照校验」（写入前校验用法清单删除热路径调用）、「复制未校验」（后果句收窄）、「载体投影读取」（不变量职责以 phase-1 前置假设为条件）。

Follow-up 边界（本 issue 不承担）：#238 replication apply/sequencer 分段延迟调查独立进行，本 issue 证据不得归因 #238；上层同值重复写/五笔合并需集成方 origin/path instrumentation 另行确认。

## 运行与证据

- 命令：`pnpm exec vitest run packages/doc-runtime/test/issue-237-path-localized-validation-red.test.ts packages/namespace-runtime/test/runtime-mutate-issue-237-hot-path-red.test.ts --typecheck=false`（`NODE_OPTIONS=--conditions=nomicore-source`）
- 重跑结果（2026-09-06，HEAD `9e3f0bf`，worktree 本机运行，start 22:17:40，9.38 s）：**45 tests：4 failed（全部为预期「必红」锚）+ 41 passed**，`vitest` exit 1（红灯契约现状即红）。日志：`.mabf-bg/sa6-237/acceptance-red-rerun.log`（`.mabf-bg/` 被 .gitignore 排除，仅留盘证据；首次 attempt 日志 `.mabf-bg/sa6-237/acceptance-red.log` 结果一致）。
  - doc-runtime 文件（41 tests | 3 failed）：A-1×2 红因 = 单笔普通叶子写仍触发 `extractYjsSnapshot`×3（1×prepare + 2×verify）、`validateLogicalSnapshot`×2、`verifySnapshotIntact`×1（断言 0）；A-2 红因 = 无关分支非法数据仍导致 ok:false（断言 ok:true）。A-3/A-4/A-5/A-6 共 38 项绿锁定全绿（含 28 个等价性场景：判别联合/Record/optional/数组边界/嵌套具名引用/空路径替换）。
  - namespace-runtime 文件（4 tests | 1 failed）：B-3 红因 = mutateData 级 R2 反转（当前被旧 ROOT 全量校验拒绝）。B-1/B-2/B-4 绿锁定全绿（B-4：8k 无关条目 × 5 笔 = notify 5、update 事件 5、单笔 <256B、carrier identity/规模/内容零触碰、尾部全局合法）。
- typecheck（重跑，exit 0 双绿）：`tsc -p packages/doc-runtime/tsconfig.json --noEmit`（含本测试文件，类型洁净）与 `tsc -p packages/namespace-runtime/tsconfig.json --noEmit`（结果见 `.mabf-bg/sa6-237/tsc-doc-runtime-rerun.log` / `tsc-ns-runtime-rerun.log`）。
- 无生产代码/公共 API 改动（`git status` 仅新增两个 test 文件与 wiki 文档；既有 doc-runtime/namespace-runtime/vfsl/ws-replication 测试文件零改动）。
- SA6 重跑报告：`wiki/raw/task_237_sa6.md`。

SA3 修绿目标 = 上述 4 个「必红」锚转绿且 41 个「绿锁定」锚保持全绿；随后按 E1–E4 交付 ADR/CONTEXT 修订（见上）。
