# Dispatch log — issue #164

- Task type: Feature development. Workflow: SA6 → SA1 → SA2 → SA3 → SA4 → SA7 → AC → final dual-axis review.
- 2026-08-30 | Controller | initialized task archive; GitHub full issue view needs specialist-compatible read-only query due to CLI Projects (classic) GraphQL deprecation.
- 2026-08-30 | SA6 | Phase 0 acceptance/red-test anchor dispatched | agent: 40d331ca-3928-41a1-9017-58b25b323828
- 2026-08-30 | SA6 | Phase 0 verdict: PASS —— 红灯契约已固化（apps/yjs-server/test/issue164-slice9-red.test.ts 9 用例 + issue164-transport-faces-red.test.ts 3 用例 + harness.ts；简报见 wiki/raw/task_issue-164-on-docs-phase-5-websocket-replication.md §SA6 红灯契约报告）。红灯验证真实：`npx vitest run apps/yjs-server/test` exit 1 / 2 files failed（Cannot find module '../src/index.js'——切片 9 未交付）；全量 `pnpm test` 既有 193 文件 / 2166 测试零回归（2 failed 仅新增红灯文件；Type Errors no errors）。SA3 前置：创建 apps/yjs-server/package.json（ws + @nomicore/* + yjs 依赖）并 pnpm install | agent: 40d331ca-3928-41a1-9017-58b25b323828
- 2026-08-30 | SA1 | Phase 1 design dispatched | agent: 3c791426-1be3-4698-bcb6-27d18ae03e2b
- 2026-08-30 | SA1 | Phase 1 design complete | verdict=ready-for-SA2 —— 产出 wiki/raw/task_issue-164-on-docs-phase-5-websocket-replication_design.md（R0，~815 行）。亲读：issue #164 全文（gh）、SA6 三件红灯测试、ws-replication（hub-connection/validate/defaults/liveness/types）、namespace-registry（types/testing/shutdown 测试先例）、protocol §2/§17/§18/§19/§21、ADR-0010 L165-182、phase-5 §9 裁决。核心结论：切片 9 = 纯宿主接线层，零包修改——401/403/101 升级前验证 + ws.Socket→DuplexTransport 适配（三面 + text 帧拒绝 + 'error' 事件吸收）+ 装配期三面响亮断言（缺省 alert=重抛 TypeError）+ §21 停机编排（hub.close→socket 清扫→registry.shutdown）；12 红灯用例逐条映射到设计条目（FS5/FS5b/FS7/FS8/FS9/FS2 全部由既有包承载，亲核源码行号）。协议假设 12 条全部附依据（含设计期实测：npm registry 可达 ws@8.21.3；'./harness.js' 红灯日志直证 .js→.ts 解析生效；registry-shutdown.test.ts 同款 fixture 直证 shutdown 可结算）。文件清单 ALLOW 13 条（含 [SA6 owned] 4 条）/DENY 9 条；契约改动=无（纯新增公共面）。残余风险 3 项均有单向退路（P7 网络安装/P10 @types/ws 摩擦桥接预案/§6.2 冻结测试严格编译降级预案）。等待 SA2 破壁攻击评审 | agent: 3c791426-1be3-4698-bcb6-27d18ae03e2b
- 2026-08-30 | SA2 | Phase 2 design attack review dispatched | agent: cad34e4b-48f4-41fa-8761-dc16f69b1d72
- 2026-08-30 | SA2 | Phase 2 verdict: **REJECT（局部修订，架构不返工）** —— 报告 wiki/raw/task_issue-164-on-docs-phase-5-websocket-replication_sa2_review.md。亲读 issue #164 全文 + SA6 三件红灯逐行 + 设计全文 + protocol §2/§17/§18/§21 + ADR-0010 + phase §9；SA1 源码引用行号全部核对零虚报；SA2 自有实测（Node v24.13.0，/tmp/sa2-upgrade-test*.mjs）证实「upgrade→handleUpgrade 窗口字节零丢失」断言成立。攻击点 4 项：**A1 CRITICAL**——§4.4(f) 的 catch 会吞掉 §4.5 notify 缺省重抛的 TypeError，缺省配置（无 alert）下缺面 transport/工厂抛错零宿主可见信号，违反 SA6 冻结「缺省=抛 TypeError」语义与 protocol §17 响亮断言立法，且冻结 12 用例测不到（TF3 恒传 alert）；**A2 MEDIUM**——预验证 await verifyToken 无界，与包内同类 auth-timeout（hub-connection.ts:183-188）先例不对称，须表态（推荐复用 helloTimeoutMs 封顶→503 或显式登记分歧）；**A3 LOW**——窗口字节保真是运行时假设但未入 §8 P 表，须补 P13；**A4 LOW**——start 失败态/wss error 监听/host 缺省绑全接口三行级注记。未成立攻击 6 项已记录排除依据（§21 顺序、FS6 destroy 竞态、TF3 泄漏、ping 无 closed 门、eOPT 摩擦、窗口丢字节）。协议假设审查：§8 存在且可验证性优良，不触发缺章节条款。错误链审查：唯一静默失败=A1；无伪降级。SA1 须提交 R1（A1 必改+A2 表态+A3 补条+A4 注记+填 §11 回应表）后 SA2 复审增量 | agent: cad34e4b-48f4-41fa-8761-dc16f69b1d72
- 2026-08-30 | Controller | SA2 R0 rejection returned to existing SA1 session for a single consolidated R1 design revision; fixed re-review scope: A1–A4.
- 2026-08-30 | SA1 | Phase 2 R1 revision complete | verdict=ready-for-SA2-incremental —— design.md R0→R1（971 行，架构面零改动，SA2 认定总体成立）。**A1（CRITICAL，甲案采纳）**：新增 escalate/runLoud 双原语单一逃逸机制（§4.5）——queueMicrotask-throw 直达 uncaughtException 域：异常身份零改写（SA2 §4.1 红线可锚原 message 'bufferedAmount'）、绕开一切中间 catch、策略无关（P14 SA1 自跑实测：microtask-throw → UNCAUGHT_EXCEPTION；对照组 async-throw → UNHANDLED_REJECTION 可被 --unhandled-rejections=warn 熄灭）；cb 经 `runLoud(() => wireConnection(...))` 包装（§4.4(f)）；该层 catch 收窄专职 ws 畸形握手（destroy 即可零 notify）；外层 .catch 改 escalate(err)（D3 与 cb 路径同语义）；wireConnection totality 声明 + 清理先行不变式（transport→真 socket(1011)→notify）；notify 保留 SA6 冻结「缺省=抛 TypeError」就地语义、边界纪律二分（同步 EventEmitter 上下文天然 uncaught / 异步上下文必须包装）；R0 缺陷根因留档防重蹈。**A2（MEDIUM，甲案采纳）**：pre-auth 封顶复用 timeouts.helloTimeoutMs（零新 knob；hub-connection.ts:183-188 立法先例镜像；超时 → respondHttp(503,'Auth Timeout') 不污染 403 凭据语义；timer 句柄全出口必清；迟归不复活镜像包内 authRejected；乙案否决理由落文 §4.4 决策记录）。**A3（LOW）**：§8 补 P13——SA1 自跑 `node /tmp/sa1-p13.mjs` 两次复跑 `{"headLen":5,"receivedLen":7,"totalPreserved":12,"lost":false}` exit=0（同段字节入 head、窗口字节暂停缓冲后投递），与 SA2 独立实测交叉印证。**A4（LOW）**：§4.3 重写——构造期单点相位路由 error 订阅（相位1 start reject / 相位2 notify，废除 R0 once('error') 残留缺陷）+ listen 失败 started=false 复位（重试报真实根因）+ wss.on('error') 订阅（D14）+ bind 全接口生产注记 §10(6)；D13/D15 新增。§11 逐条回应表已填（4/4 ✅）+ 修订后全文一致性自检过（原语引用/悬空声明 this.timer・resolvedTimeouts・maxFrameBytes 已补 §4.1 私有装配面/import 补 DEFAULT_REPLICATION_TIMEOUTS/章节编号零残留矛盾）。等待 SA2 增量复审（§6 复审条件：仅审增量，架构面不重开） | agent: 3c791426-1be3-4698-bcb6-27d18ae03e2b
- 2026-08-30 | SA2 | Phase 2 incremental re-review verdict: **PASS（附 1 条强制 SA3 约束 A5；SA3 可开工）** —— 报告 §R1（wiki/raw/task_issue-164-on-docs-phase-5-websocket-replication_sa2_review.md，R0 存档保留）。范围仅 A1–A4 + §11（架构面不重开）。**A1–A4 全部核销**：A1 escalate/runLoud 单一逃逸机制逐调用点核实（notify 全部 5 处边界纪律合规；wireConnection totality 复推成立；TF3 不受影响）；**P14 经 SA2 独立复测证实**（node /tmp/sa2-p14-verify.mjs 四例：microtask-throw 必达 uncaughtException 且策略无关，async-throw 走 UHR 可被 --unhandled-rejections=warn 吸收——runLoud 通道选择正确）。A2 甲案 race 模式核实（executor 同步武装/全出口清句柄/迟归不复活零 UHR；DEFAULT_REPLICATION_TIMEOUTS 确在包公共出口 index.ts:8-11，resolveTimeouts 确不在——私有合并正当；非法 timeouts 仍构造期响亮）。A3 P13 格式合规可复核。A4 三则落文 + 连带修复 R0 潜伏 once('error') 残留缺陷（加分）。§11 四行齐备；一致性自检经 grep 复核属实（旧表述零残留、§3/§5.1 与 R0 逐行一致）。**新发现 A5（MEDIUM，R1 引入、伪码级）**：§4.4(d) 的 `verifyToken(token).then(ok, fold)` 只折异步 throw——同步 throw 的 verifier 在实参求值点逃逸 → escalate → 进程崩溃（SA2 实测 /tmp/sa2-a5-check.mjs：sync-throw→ESCAPED；R0 形态与包 gate 4 均两者皆折 403/invalid-credentials）；违反设计自身契约文本「verifier 抛错→403」且 token 为攻击者可控输入 = 或有远程崩溃向量；**不构成 reject**——契约文本已正确，以 §R1.3 约束 2 钉死 SA3（调用求值包入 promise executor 或 try/catch 折入 verifier-threw→403；SA4 静态锚 + SA7 红灯锚已定义 §R1.4），建议 SA1 择机单行编辑同步伪码（无需再评审）。SA3 就绪约束十条见报告 §R1.3（约束 2/A5 强制） | agent: cad34e4b-48f4-41fa-8761-dc16f69b1d72
- 2026-08-30 | SA3 | Phase 3 implementation dispatched after SA2 incremental PASS; mandatory A5 synchronous verifier-throw to 403 folding included | agent: 9f3992bd-c841-4c8e-b30e-9c02bd8fa477
- 2026-08-30 | SA3 | Phase 3 implementation complete —— 报告 wiki/raw/task_issue-164-on-docs-phase-5-websocket-replication_sa3_impl.md。交付 = ALLOW LIST 全项：apps/yjs-server（package.json + tsconfig + src/transport.ts + src/index.ts + AGENTS.md）+ 根 typecheck 追加 + pnpm-lock 增量（+ws@8.21.3/@types/ws@8.18.1）。十条 SA2 就绪约束逐条落实：A1 runLoud/escalate 单一逃逸机制（微任务 throw → uncaughtException 域；清理先行；(f) catch 专职 ws 畸形握手）；**A5 强制落实**——verifyToken 调用求值包入 `new Promise(resolve => resolve(verifyToken(token)))`，同步 throw 折入 .then rejection → verifier-threw → 403（SA2 R1 伪码字面的远程崩溃向量已消除）；A2 pre-auth 封顶（helloTimeoutMs race + 全出口清句柄 + 503 'Auth Timeout'）；§4.1 eOPT 细则；§3.2 adapter 精确行为；§4.5 顺序与零协议分配；§4.6 停机全序；§4.3 相位路由；§6.2 降级预案触发（见下）。**验证**：`npx vitest run apps/yjs-server/test` → 11/13 绿（FS1-FS5/FS6-FS9/TF1/TF2），2 败；`pnpm test` → 191 files / 2177 tests 通过，2 failed 仅新增红灯文件，**零回归**，Type Errors no errors；`pnpm typecheck` → 0 errors。**缺陷登记（3 项，SA6-owned，SA3 未动测试文件）**：缺陷 A（FS5b）——客户端编码器 encodeHello 先抛 MALFORMED_FRAME（'Peer_Alpha!' 违反文法），帧从未到达 wire，hub 侧帧级拒绝不可达，任何合规实现无法转绿，需 SA6 用 RawWsClient.sendBinary 直发手工帧；缺陷 B（TF3）——ws completeUpgrade 与组合根拒绝 close 同 tick 写 101+1011，loopback 合并单 segment，RawWsClient 在 PeerWire 订阅前消费 close 帧且不回放（真实 ws 客户端亲证 close(1011)+alert+零 HELLO_ACK 均正确，纯冻结夹具观察窗竞态），需 SA6 为 RawWsClient 增加已关闭状态回放（一行级）；缺陷 C（§6.2 降级预案触发）——SA6 冻结测试在 TS5.9 严格编译下 9 处类型错误（泛型 TypedArray/exactOptionalPropertyTypes/noUncheckedIndexedAccess），tsconfig include 已收窄为 src/** 并显式登记。等待 SA4 静态验收 | agent: 9f3992bd-c841-4c8e-b30e-9c02bd8fa477
- 2026-08-30 | SA4 | Phase 3 static implementation review dispatched | agent: be293706-3cf7-4159-84fb-0e51f582bae6
- 2026-08-30 | SA4 | Phase 3 verdict: **pass（SA3 实现侧零剩余工作）+ SA6 回流 2 硬阻断（缺陷 A/B）+ 1 推荐（缺陷 C）** —— 报告 wiki/raw/task_issue-164-on-docs-phase-5-websocket-replication_sa4_review.md。**SA3 声称的三项 SA6-owned 缺陷全部独立复核成立，责任归属裁定：A/B/C 均归 SA6**。决定性证据：①FS5b 失败栈全程在冻结客户端（PeerWire.send→encodeMessage→encodeHello→checkInstanceId 同步抛 MALFORMED_FRAME，帧从未上线——E5 tsx 直调实锤）；**反向实验 E6**（同长度字节补丁 'peer-alpha'→'Peer_Alph!' 经 RawWsClient.sendBinary 直发 wire）证明服务器侧契约完全成立（ERROR 帧 + close(1002,'protocol-error') + 零 HELLO_ACK + code∈[1002,1008]）——测试构造性缺陷，SA3 受 DENY LIST 双重锁定（packages/** + SA6 冻结件）零责任；②TF3 的 alert/零 HELLO_ACK 断言均已通过、仅卡 wire.closed 观察窗（loopback 101+close(1011) 同 segment，RawWsClient 在 PeerWire 订阅前消费 close 帧且不回放——harness.ts:296-300/324-333 亲读）；**反向实验 E7**（真实 ws 客户端）观测到 close(1011,'transport-faces-missing') + 零协议帧 + alert 含 'bufferedAmount'——夹具观察窗竞态，服务器零责任；③缺陷 C 实锤（临时 tsconfig 加 test/** → 14 处类型错误全在冻结测试文件、src 零错误；§6.2 降级预案正当且三处登记）。**SA2 R1 十条约束逐条静态核对全过（含 A5 强制：verifyToken 调用求值包入 promise executor :259-266，同步 throw 折 403）**；scope creep 零（16 文件全映射 ALLOW，DENY/BLACKLIST 零触碰）；触发门禁过（ci.yml 每 PR 跑 pnpm test + typecheck，apps 测试已接通）——**后果：缺陷 A/B 不修则 CI test job 恒红，回流为硬阻断非卫生项**。独立复跑：apps 11/13 + 全量 191 files/2177 tests 零回归 + typecheck 0 errors（与 SA3 报告逐字一致）；§1.7 零源码 grep 断言。**SA6 一轮回流清单（一次性）**：A=FS5b 改原始帧发送（断言强度不降）；B=RawWsClient 关闭状态回放（断言零改动）；C（推荐同轮）=清 14 处类型错误 + tsconfig include 恢复 src+test。SA6 diff 允许范围仅 4 个测试侧文件，禁触 src/packages/vitest.config；SA4 复验固定范围 = SA6 diff 逐行 + 断言强度核对 + 四命令（13/13、typecheck 0、全量 0 failed/2179 passed）。SA3 无需重新提交；SA7 动态重点 7 项见报告 §5（A5/A1/A2 红灯、maxPayload、EADDRINUSE、GOAWAY 深水、CI 复跑）| agent: be293706-3cf7-4159-84fb-0e51f582bae6
- 2026-08-30 | SA6 | Consolidated test-fixture remediation dispatched after SA4; fixed scope: FS5b raw frame, TF3 close replay, strict test typing/include restoration | agent: 40d331ca-3928-41a1-9017-58b25b323828
- 2026-08-30 | SA6 | Phase 0 remediation verdict: **PASS（SA4 缺陷 A/B/C 全部核销，SA4 §4 固定复验范围 (i)–(iv) 全过）** —— diff 仅限 4 个测试侧文件：`apps/yjs-server/test/harness.ts`（+RawWsClient.lastClose 关闭状态回放 / +PeerWire.sendRaw / +makeGrammarViolationHelloFrame 原始帧构造 / +waitUntil 缺省预算 / 严格类型修复）、`test/issue164-slice9-red.test.ts`（FS5b 改 sendRaw 直发 wire，断言零削弱：零 HELLO_ACK + close code∈[1002,1008]）、`test/issue164-transport-faces-red.test.ts`（严格类型修复，断言零改动）、`apps/yjs-server/tsconfig.json`（include 恢复 `["src/**/*.ts","test/**/*.ts"]`，SA1 §6.2 原始意图兑现）。**零触碰**：apps/yjs-server/src/**、packages/**、vitest.config.ts。复验（后台独立进程）：apps `npx vitest run apps/yjs-server/test` **13/13 绿**；`npx tsc -p apps/yjs-server/tsconfig.json --noEmit` 0 errors；`pnpm typecheck` 0 errors；`pnpm test` 全量 **Test Files 193 passed（193）/ Tests 2179 passed（2179）/ Type Errors no errors**（CI test job 恒红根因消除）。SA7 动态重点（SA4 §5 七项）交接给 SA7 滚动验证；本 SA6 修复 commit 引用 #164，未 push/未建 PR | agent: 40d331ca-3928-41a1-9017-58b25b323828
