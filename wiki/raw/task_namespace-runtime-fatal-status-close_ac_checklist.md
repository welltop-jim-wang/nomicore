# AC 逐条核对清单 — namespace-runtime：fatal、capability status 与 close 生命周期（issue #92，round 1）

核对人：总控（Phase 3.5 AC 门禁）。核对时点：SA4 verdict=pass + SA7 verdict=pass 之后。
证据基线：commit 91103db（SA3 交付）+ SA7 新增动态锚；全量 `pnpm test` 87 files / 1093 tests exit 0（含 typecheck 通道）。

| AC# | 描述 | 状态 | 证据 | 处理 |
|---|---|---|---|---|
| AC1 | 所有 internal fatal 永久关闭两类写但保留 read，稳定摘要不暴露原始 Error/stack/SCHEMA 全文/ROOT 数据 | ✅ | 实现：`write.ts rejectWithWriteFatal/markWriteFatal`（fatal 一经置位永久，S1 gate 关两类写、read 不 gate）；`errors.ts` FATAL_*_MESSAGE 恒定文案零插值。测试锚：`runtime-write-fatal-message-rev1.test.ts`（3 用例：notify-dirty-failed/unknown-pipeline-throw/P0 fault 的 message 无 sentinel 泄漏、status.fatal 无 stack/cause、rootWrite/schemaWrite=false 且 read.enabled=true）；`runtime-mutate-root-sequencer.test.ts` AC9 两用例（committed:true/false fatal 后「写永久禁用、读取保留」）；`runtime-close-lifecycle.test.ts` case 7（fatal 后 read 保留 ok:true） | SA3 实现（fatal 域 #90 既有，本任务零字节改动——SA4 §Scope 核实） |
| AC2 | committed fatal 在当前槽中 best-effort dirty notification，始终 reject 原始 RuntimeWriteFatalError 且明确 committed | ✅ | 实现：`write.ts rejectWithWriteFatal`（committed:true → 槽内 `await env.notifyDirty?.()` 恰一次、失败吞没、throw 稳定 RuntimeWriteFatalError{phase,committed,cause}）。测试锚：`runtime-mutate-root-sequencer.test.ts`「AC9 + fatal 通道（committed:true）」（best-effort notifier 恰一次 + reject RuntimeWriteFatalError committed=true）；`runtime-write-fatal-message-rev1.test.ts` 两用例（cause 严格相等原始实例）；`runtime-close-lifecycle.test.ts` case 8（排空期内 committed:true fatal 照常：reject + notifier 恰一次 + 不虚假回滚） | SA3 实现 |
| AC3 | committed:false 不通知 dirty；未知异常保守按可能 committed 处理 | ✅ | 实现：`write.ts` S5 catch（DocRuntimeFatalError 透传 err.committed；非 branded → `rejectWithWriteFatal(env, true, 'unknown-pipeline-throw', err)` 保守 true）；rejectWithWriteFatal 仅 committed 时调 notifier。测试锚：`runtime-mutate-root-sequencer.test.ts`「AC9 + fatal 通道（committed:false）」（notifier 0 调用、零写入）；`runtime-write-fatal-message-rev1.test.ts` unknown-pipeline-throw 用例（committed=true 保守 + notifier 恰一次） | SA3 实现（#90 既有通道，本任务验收收口） |
| AC4 | fatal 后已排队任务按 FIFO 取得槽，不访问输入并返回零写入 RUNTIME_WRITE_DISABLED | ✅ | 实现：`write.ts`/`schema-write.ts` S1 fatal gate（零输入访问 → disabled()）；`sequencer.ts` 链尾恒绿（单项 reject 不断链）。测试锚：`runtime-mutate-root-sequencer.test.ts`「AC2 + AC4：fatal 状态 gate——write-disabled 后仍 FIFO 取得槽、零访问输入、零写入返回 RUNTIME_WRITE_DISABLED」 | SA3 实现（#90 既有，本任务零回归——SA4 核实断言面无弱化） |
| AC5 | getStatus 结构化表达 lifecycle/read/rootWrite/schemaWrite 与 schema/fatal/close 摘要，不暴露队列长度或任务类型 | ✅ | 实现：`status.ts` 七键 `NamespaceRuntimeStatus`（lifecycle 三态 + close 摘要键；closing/closed 期 handle 观察短路三能力位恒 false）。测试锚：`runtime-close-lifecycle.test.ts` case 6（七键恰集 + closed 期三能力 false + 无 queue/sequence/taskType/数组值字段）；`runtime-close-lifecycle-type-guard.test-d.ts` 3 用例（lifecycle 三态联合 + close 键类型面） | SA3 实现 |
| AC6 | close 首次调用同步进入 closing 并立即停止新 read/write，close 前已接纳任务无条件排空 | ✅ | 实现：`runtime.ts` close()（同步 `state.lifecycle='closing'` → 队尾 `sequencer.enqueue(runCloseBarrier)`；read 同步结果联合 RUNTIME_READ_DISABLED；mutateRoot/replaceSchema 接纳门即时 `Promise.resolve(disabled(...))` 零入队零输入访问；槽内不设 lifecycle gate = 无条件排空的结构性保证）。测试锚：`runtime-close-lifecycle.test.ts` case 2（同步 closing + read 立即同步拒 + 并发重复同实例）、case 3（排空：在槽写 ok:true 完成、barrier 最后、release 恰一次、close 后新写不入队零写入）、case 4（P0 未结算时 close：P0 排空于 barrier 前） | SA3 实现 |
| AC7 | close barrier 只 release 一次；release 失败时 close reject 但 Runtime 仍 closed，后续 close 返回同一 Promise | ✅ | 实现：`close.ts runCloseBarrier`（release 恰一次；thenable 守卫；成功/失败两路都写 lifecycle='closed'；失败路 closeIssue 冻结注册 NSRT-CLOSE-RELEASE-FAILED + throw NamespaceRuntimeCloseError{cause}）；`runtime.ts` closePromise 闭包缓存（同一实例含已结算后）。测试锚：`runtime-close-lifecycle.test.ts` case 5（release 失败：reject + 仍 closed + 摘要稳定无原始 Error/stack + 后续 close 同 Promise 同 reason）；`runtime-close-sa7-dynamic.test.ts` DV-6（非 thenable 契约违背同通道） | SA3 实现 |
| AC8 | v1 不提供公共事件订阅，队列进度只走内部 observability | ✅ | 实现：公共面恰十键（owner/namespaceId/read/getSchemaEnvelope/getMetadata/getActiveSchema/getStatus/mutateRoot/replaceSchema/close），无 on/off/subscribe/emit。测试锚：`runtime-close-lifecycle.test.ts` case 1（十键恰集 + 事件键负向断言） | SA3 实现 |
| AC9 | 确定性测试覆盖 fatal、dirty notification、排队结算、幂等 close 和 release failure，并通过全量 typecheck/test、Node 20/24 CI | ✅（本地门禁全绿；CI 双矩阵待发布后取证） | 确定性锚：fatal（rev1 3 用例 + sequencer AC9×2 + close-lifecycle case 7/8）、dirty notification 计数（上述各用例 notifier 计数断言）、排队结算（sequencer AC2+AC4、close-lifecycle case 3/4）、幂等 close（case 2 并发/重复同实例 + DV-3 真实 handle 幂等）、release failure（case 5 + DV-6）。本地门禁：总控亲跑 `pnpm test` 87 files / 1093 tests exit 0（verify-sa3.log + SA7 收尾复跑）、`pnpm typecheck` exit 0、`tsc -p tsconfig.typecheck.json --noEmit` exit 0（SA3/SA4/SA7 三重复跑一致）。Node 20/24 CI：ci.yml `pnpm test` Node 20/24 matrix 接通本包（SA4 §1.4 / SA7 Step 4 触发证据）；双矩阵实际 run 证据属发布后 Host/CI 阶段（SA7 §重点1 环境阻塞注记 + 发布后 CI 取证清单已交接） | 本地部分总控亲跑验收；CI 部分按职责边界交 Host 发布后进行 |

**结论：9/9 ✅（AC9 的本地可验证部分全绿；Node 20/24 CI run 证据随发布由 Host 接管，SA7 已附取证清单）。无 ❌ 条目，进入 Phase 4 收尾。**
