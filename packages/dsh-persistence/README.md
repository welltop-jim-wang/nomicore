# @nomicore/dsh-persistence

DSH development/profile adapter and inspector probe around `@nomicore/persistence`. This is a development and debugging profile, not the default production persistence choice; see the [repository README](https://github.com/welltop-jim-wang/nomicore#readme).
