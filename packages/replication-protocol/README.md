# @nomicore/replication-protocol

Instance replication v1 wire codec shared by Hub and Peer implementations. Ordinary consumers use `@nomicore/ws-replication` or `@nomicore/yjs-server` instead of this package directly; see the [repository README](https://github.com/welltop-jim-wang/nomicore#readme).
